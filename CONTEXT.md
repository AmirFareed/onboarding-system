# Project Context – FinTech Document Verification System

<!-- git identity fix verified 2026-08-22 -->

Last updated: 2026-08-23. This file is the single source of truth for project state across sessions/tools — keep it current rather than trusting an earlier assistant summary in chat history.

## 📍 Session picks up here (2026-08-22 end-of-day) — read this first

Everything below is re-verified against actual repo state today (`git log`, `git branch -a`, live commit contents), not carried forward from an earlier chat's memory — a prior triage note in this same session had gone stale on several points (see the correction at the bottom of this section) and that mistake shouldn't repeat.

**Done today, in order (all on `main`, all pushed):**
1. `organization_name` garbage-capture fix on a line-wrapped "on behalf of the" clause (`6759c21`, closed `3b1cd41`).
2. Tripartite `party_1link` regression fix — excluded Authority Letter preamble from the match (`6c26f8d`, closed `110f961`).
3. `BLUR_THRESHOLD` false-positive fix — global blur variance replaced with tiled 95th-percentile (`5e20a81`, closed `ee6aeb1`).
4. `audit/backend-verification-2026-08-20` merged into `main` (`904ab84`); branch deleted both local and remote (absent from `git branch -a`).
5. Modal focus-trap fix cherry-picked (`9c28012`, adds focus traps to `SessionTimeoutModal`/`ConfirmDialog` + an environment startup log warning). Note: `4f8c390` (a stale `revenue_services_listed` BRD-test-assertion fix) landed adjacent to this in the log but is an unrelated, independent fix — not actually tied to the focus-trap change despite landing close together.
6. `BilateralAgreementExtractor` rewritten against real content and real-sample validated for the first time ever (`fc27a24`).
7. `CrossAccountHolderRule` unregistered for `BILATERAL_AGREEMENT` — `account_holder` is a structural honest-miss, matching the `CrossBranchCodeRule` precedent (`19a2e29`, fixtures updated `c56efee`).
8. Tripartite `branch_code` investigated and accepted as a documented Bucket B gap — no real sample with a filled branch code exists yet (`106f757`).
9. Login page background video asset committed, 41MB (`3230882`).
10. CONTEXT.md docs cleanup batch: stale header date, missing merge-resolution entry, 5 stale Known Gaps entries corrected, `FormalRequestLetterExtractor` documented (`7859d68`).
11. Repo-root `pytest.ini` added — fixes bare `pytest` crashing on collection of `backend/scripts/*.py` (`cb84b08`).
12. `feature/afsana-validation-logs` fully reconciled: 2 real pieces landed (`6c794c9` — `OTHER_SUPPORTING_DOCUMENT`/`BULK_UPLOAD` frontend catalogue entries; `e2ce416` — hyphenated date format support in `_parse_date`), the rest correctly identified as superseded or unsafe and dropped (see the dated entries later in this file for the full reasoning, including the misrouted-`ONE_LINK_LETTER` finding that caught one drop). Branch deleted both local and remote.
13. `feature/samad-doc-splitter` closed: one real bug recovered (`frontend/.env.example`'s active `VITE_API_BASE_URL` line, silently dropped in an earlier refactor, breaks the Vite dev-server CORS proxy — restored, `2dc9d5c`), the other 3 commits confirmed stale docs/config. Branch deleted both local and remote.
14. PyMuPDF AGPL licensing decision logged and closed (`49e9b62`) — see the section directly below.
15. `SCHEDULE_OF_CHARGES` removed from the required document checklist — `DocumentScheduleRule` was an unconditional, unsatisfiable `overall_status` blocker (any FAIL forces overall FAIL; zero real samples of this type exist in ~21 real files checked all session), not just a missing extractor. Its real content overlaps `BILATERAL_AGREEMENT`'s already-extracted Transaction Charges section (`6cd94ee`, decision documented `433ab2c`).
16. `app.validation` module (`ValidationTask`/`ValidationRun`/`ValidationLog`) deleted outright — confirmed zero real callers anywhere, superseded in practice by `app.operator_workflow` (real, live triage queue) + `app.human_verification` (final decision), which already form the two-stage workflow this module was scaffolded for but never wired into. 18 files removed, 7 edited, one new Alembic migration (`fa41896`). CLAUDE.md and CONTEXT.md both updated to document `operator_workflow` and this module's fate (`89dbcb4`).
17. AuditLog viewer decision investigated and closed — the "write-only, never read back" premise from the 2026-08-15 foundation audit was **wrong on the backend half**: `app/system_logs/` already implements a complete, tested, IT-role-gated read API (`GET /system-logs`, `GET /system-logs/{id}`) directly over `AuditLogRepository.search()`; nobody had ever built the frontend that calls it. Only the frontend was actually missing. Built and shipped: `services/systemLogs.js`, `hooks/useSystemLogs.js`, `pages/SystemLogs/SystemLogsPage.jsx` (filterable table + detail panel, mirrors `ApplicationHistoryPage`'s shape), new "System Logs" nav entry (`strictRoles: ['IT']`), route registration, 9 new unit tests plus updated role-gate coverage in `Phase1Routes.test.jsx`/`roles.test.js` (`9c33446`). Frontend suite: 108 passed (up from 97), 0 lint errors, build clean. See the corrected Known Gaps entry below for the full investigation.
18. `ImageEnhancer` dead code decision investigated and closed — deleted `backend/app/preprocessing/enhancer.py` outright. The decision had actually already been made and documented: `app/preprocessing/__init__.py`'s own docstring states "Image enhancement intentionally lives in `app.document_processing.utils` ... and is not duplicated here" — someone had already decided this file was superseded, the cleanup just never removed it. Git provenance confirms the shape: `enhancer.py` was added in `32171b8` (2026-08-12, Samad's original synchronous splitter + CV preprocessing), superseded the same week by Zarghuna's async `bulk_queue` redesign; the real, live equivalent (`document_processing/utils.py::preprocess_image`, deskew → denoise → CLAHE → adaptive threshold) actually predates it by a day (`726a607`, 2026-08-11) and runs on every real document via `processors.py`. Zero callers, zero tests, confirmed by fresh grep before deletion. See the corrected Known Gaps entry below.
19. **System Logs frontend (item 17, `9c33446`) reconciled against a prior removal — kept, not reverted; a real process gap found and documented.** `8219c8b` (2026-08-21, Zarghuna, merged into `main`) had already deleted an earlier System Logs frontend (`SystemLogsPage.jsx`/`useSystemLogs.js`/`services/systemLogs.js`/its test, 1,218 lines) as an explicit, user-approved scope decision — "remove System Logs from the frontend UI only... replaced by the new Application History and Performance features" — while deliberately leaving the backend `system_logs` module/`audit_logs` table/`test_system_logs_api.py` untouched. Today's `9c33446` rebuilt essentially the same frontend files one day after they'd been deliberately removed, because the investigation behind it never checked git history for prior System Logs frontend work — it only grepped the live tree, found nothing, and concluded "never built." **Decision (Abdul, 2026-08-22): keep `9c33446`, do not revert.** Not actually a contradiction once the scope difference is understood: `system_logs` reads the raw `AuditLog` table across all 17 real write sites (confidence, normalization, rule_engine, bulk_queue, operator_workflow, human_verification, upload); Application History/Performance are sourced from different, narrower models (`ValidationHistoryEntry`/`Document`/`QueueJob`/`HumanReview`, and timing data respectively) and don't cover the same ground. The 2026-08-20 removal was about avoiding a redundant nav entry at that specific moment, not a permanent ban on ever having a raw audit-log view — a real, separate use case (IT/debugging visibility into every audited action, not just application lifecycle/timing) genuinely exists and isn't covered by the other two. **The process failure is real and is the actual lesson here, independent of the outcome being fine**: any investigation concluding a UI/feature was "never built" must check `git log --all -- <path>` for prior existence/removal before recommending a (re)build, not just grep the current tree — see the dedicated process-lesson entry in Known Gaps below.

**Correction to a stale in-session recollection**: earlier today's own triage note described items 12 ("afsana's remaining 3 commits — not yet reconciled"), 14 ("PyMuPDF — still open"), 15 ("Schedule of Charges — zero-sample gap still open") and CNIC_BACK ("still open") as outstanding. All four were already resolved by the time that note was written — re-verify against `git log`/`git branch -a` before trusting any in-session "still open" list, this file included, rather than propagating one forward.

**Still genuinely open** (re-verified against the current Known Gaps section below, not assumed):
- **CNIC_BACK** — not open as a *decision* (that was closed 2026-08-17: deliberately out of scope), but the standing condition (zero real `CNIC_BACK` samples exist) remains, so it stays unmapped until one turns up.
- Rule-engine coverage gaps vs. `docs/Master_Rules_Combined.md`: **platform terminology (PayMin/Digital Muhasil/Paymere BCX) CLOSED 2026-08-23** (`FieldBilateralPlatformNamePresenceRule`, see dated entry below); **CNIC expiry/completeness already closed** (pre-existing `DateCnicNotExpiredRule`, found already registered during 2026-08-23 re-check, this list was stale on that point); **Bilateral `effective_date` presence CLOSED 2026-08-23** (`FieldBilateralEffectiveDatePresenceRule`, see dated entry below — a scope decision was made: require presence, not blankness). Still genuinely open: **organization-name cross-document consistency — re-investigated 2026-08-26, still blocked, but the precise reason has changed: extraction now exists for every relevant field (it didn't when this note was first written), the real blocker is that no normalization exists to bridge legitimate abbreviation/full-form variance ("TMA X" vs "Tehsil Municipal Administration X") that appears in every real multi-document application checked — see the dated entry below**, per-page 1-Link signature, layout/point-numbering conformance, E-Stamp/Notary Public visual checks (structural/text checks, not the CLOSED visual-detection-pipeline decision below — that's a separate, resolved question), Formal Request Letter subject-line (already partially covered by `FieldFormalRequestSubjectPresenceRule`, presence-only). Don't add a rule before extraction exists for the field it targets.
- **Visual-detection pipeline (signature/stamp auto-detection) is CLOSED, not open** — decided 2026-08-23 not to build one; see the dated entry below for the full investigation. Not a gap awaiting work: the manual checklist in `human_verification` already enforces the equivalent confirmation server-side, so this is deliberately staying manual. Listed here only so it isn't mistaken for a still-open item.
- Master Rules §3's AMC↔Tripartite/Bilateral branch-name consistency check — investigated 2026-08-22, still blocked, not built: needs real-sample coverage on two independent fronts (Tripartite's unfilled `branch_code`, Bilateral's apparent total absence of any branch field) before a rule can be safely registered. See the corrected Known Gaps entry below.
- **AMC `issue_date` gap — CLOSED 2026-08-26.** The old "bare Date label fixes 2, breaks 2" note undersold both the risk and the real fix; see the dated entry below. 6 of 7 real samples now resolve correctly; the 7th has no safe fix (OCR corruption, not a labeling problem) and is documented as such, not silently left vague.
- 1-Link Letter checklist-meaning mismatch (real samples are Participation Memoranda, not Section 4's actual required content) — flagged, needs a product/ops scope decision, not a code fix.
- `fcc9fda`'s blank-page technical-validation check causes the 12 pre-existing `test_document_analysis_api.py` failures (previously logged elsewhere in this file as "13" — today's own suite runs isolated the 13th as an unrelated environmental artifact, a stale local `worker.heartbeat` file, not a 13th named code failure; the real, stable code-level count is 12). Needs to be raised with Zarghuna, not touched reactively.
- **Teammate bug triage: CLOSED — 28 of 28 confirmed issues in `docs/TEAMMATE_BUG_TRIAGE.md` fixed as of 2026-08-23** (see the dated batch-3 entries below for the full breakdown: the `8f5fce7` batch fixed 6, `b770551`/`74cf5b5` turned out to already fix 2 more (#8/#24) without doc reconciliation until today, then #21/#23 (`88ecb7c`/`10aafa8`), #22 (`3a45132`), and finally #16/#13/#12 (`c6b4a12`/`a182fde`/`76d7209`) closed out the last 3 after the pre-demo audit). Nothing remains open in this doc.
- **`detect_document_type`'s `ID_DOCUMENT` collision — CLOSED 2026-08-26, resolved incidentally, not by a direct fix.** The 2 originally-flagged real samples (`GDC_Alpurai_Shangla`, `schdule_of_charges_or_bilateral`) no longer reach this code path at all -- both now title-match to a real checklist type upstream in the splitter (unrelated fixes landed earlier the same session: `040b4f6`, `35c0317`). A full 66-file real-corpus re-scan confirms zero real samples currently trigger it. The keyword table itself is unchanged and still fragile in principle -- see the dated entry below for the precise, honest framing.
- `GDC Alpurai Shangla .pdf`'s genuine Account Maintenance Certificate page doesn't independently strong-match `"ACCOUNT MAINTENANCE CERTIFICATE"` even after the 2026-08-22 Application Form boundary fix (see the dated entry below) — a different, newly-found boundary on the same file, not investigated further, out of scope for that fix.
- `GDC Alpurai Shangla .pdf`'s "Ann-07" Business Certificate / Participation Memorandum boundaries remain absorbed into the `ONE_LINK_LETTER` group even after the 2026-08-22 fix above — same already-known, deliberately-deferred gaps (unmapped type, visual-not-textual signal) as documented elsewhere in this file. **The Formal Request Letter boundary on this same file is CLOSED as of 2026-08-23** — see the dated entry below; it was the idiosyncratic-subject-line gap, now fixed by the structural letterhead+Subject: fallback, not still open.

## ✅ Legal/Compliance Decision: PyMuPDF Licensing (closed 2026-08-22)

`PyMuPDF` (imported as `fitz`) is used for PDF rendering/extraction throughout `document_processing` and `preprocessing`. The installed package is **dual-licensed AGPL-3.0 / Artifex Commercial License** — confirmed directly from the installed package's own metadata (`License-Expression`/`License` field), not from memory (foundation audit, Section 8). AGPL is the strongest copyleft license that exists: its network-use clause requires making the source code of the **entire application** available to any network users of this service, unless a commercial license has been obtained from Artifex.

**Decision (Abdul, 2026-08-22): staying on the free AGPL license, no commercial Artifex license.** Explicitly accepted: this means the full application source must be made available to anyone using the app over the network, per AGPL's network-use clause. Closed; no further legal review pending on this point.

## What this project is

A full-stack system for banks/KPITB to onboard merchants: upload financial onboarding documents, run them through OCR → field extraction → confidence scoring → normalization → a business-rule engine → human review, and curate corrected data into a versioned dataset for continuous learning. Backend: FastAPI + PostgreSQL (SQLAlchemy 2 + Alembic). Frontend: React 19 + Vite.

**⚠️ The GitHub repo (`abdulsamad-codes/Document-Validator-Model`) is public.** Real onboarding documents (real IBANs, CNICs, officer names) belong in `Confidential Data/` at the project root, which is gitignored — never loose in the repo. On 2026-08-13 a real onboarding PDF was found committed and pushed un-ignored (fixed by untracking + gitignoring it, commit `bc57cff`); git history was deliberately left as-is rather than rewritten, so that one file is still recoverable from an old commit if anyone goes looking. See `Confidential Data/` for the ~19 real documents already handled correctly.

**⚠️ Never run `pytest` while real data is loaded in the dev database.** `tests/conftest.py`'s autouse `isolated_database` fixture runs `DELETE FROM applications` (cascading to documents/queue_jobs) before AND after **every single test**, against whatever database `SessionLocal`/`DATABASE_URL` points at — which locally is the same Postgres database the running backend and queue worker use. On 2026-08-13 this wiped a real uploaded application while it was mid-processing (see below). Before running the suite locally, check the `applications` table is empty first, or point `DATABASE_URL` at a separate test database. (Independently confirmed by a parallel session working on `feature/zarghuna-bulk-queue`: a 10-minute full-suite run wiped all users mid-run, breaking login until reseeded — same root cause.)

**⚠️ This includes running two `pytest` processes at the same time, not just pytest + a live app operation.** Found 2026-08-16 (third collision of the day, after the destroyed application 8547 that morning): a background full-suite run was already in progress, and a second, separate `pytest` invocation was started to check a small set of new tests — both hit the shared dev DB via the same autouse `isolated_database` fixture, which wipes tables before/after *every* test regardless of whether that specific test touches the DB. This can produce a spurious failure in whichever run loses the race, indistinguishable from a real regression without isolating and re-running the failing test alone. **Mandatory pre-flight before running pytest, every time, no exceptions: `ps aux | grep python` first, confirm nothing else is already running against the same database** (another pytest process, a live upload/queue drain, a script using `SessionLocal`). Splitting-only or pure-OCR work that never touches the DB (e.g. `backend/scripts/ocr_cache.py`) is exempt — it's safe to run alongside pytest, only CPU contention applies there.

## Current state: everything merged to `main`, test suite green, Operator Dashboard + Validation Report + Human Review + Enterprise Settings (functional workspace preferences) + Processing page + Application History + Performance shipped

Three teammates' branches (`feature/samad-doc-splitter`, `feature/zarghuna-bulk-queue`, `feature/afsana-validation-logs`) are all merged into `main`. Work happens directly on `main`.

**Backend test suite: 709 passed, 0 failed as of the last measurement** (`cd backend && .venv/Scripts/pytest.exe -q`, reported to take up to ~10 min on some machines). **Re-verify the `applications` table is empty before running it** — see the pytest warning above — and re-run to confirm the number if it's been a while.

### What each branch shipped
- **Samad**: original bulk PDF splitter + CV preprocessing + bulk-upload endpoint (later superseded by the async redesign below).
- **Zarghuna**: async bulk upload — `upload_bulk` stores the raw PDF as a `BULK_UPLOAD`-typed placeholder `Document` and enqueues it on a Postgres-backed job queue (`app/bulk_queue/`, `FOR UPDATE SKIP LOCKED` claiming). A background `BulkQueueWorker` later splits/classifies pages (`DocumentSplitter.split_bulk_pdf`, PaddleOCR fallback for scanned pages) — this fixed the original synchronous-splitter HTTP timeout on large/scanned bulk uploads.
- **Afsana**: the `validation` module — `ValidationTask`/`ValidationRun`/`ValidationLog` models plus a full API (`app/validation/routes.py`): task lifecycle (create/start/complete/reject/request-correction), stored check results, immutable audit log, review-time field/evidence logging. This is the backend half of the human-review workflow.

### 2026-08-13 stabilization session (this session)

A prior session (different tool) had merged everything but left the suite at "89 failing tests, never root-caused," and had introduced a rule-registry regression while trying to fix an unrelated test. This session found and fixed the real causes:

- **Missing migration**: `DocumentType.BULK_UPLOAD` existed in Python but was never added to the Postgres enum — every bulk upload failed. Added `alembic/versions/02ffa030b9e1_add_bulk_upload_document_type.py`.
- **Wrong repository method call**: `upload_bulk` called a nonexistent `QueueJobRepository.enqueue()`; fixed to call the real `enqueue_uploaded_documents(...)`, removed a dead import, rewrote the stale docstring.
- **Real atomicity bug**: `DocumentRepository.create()` commits immediately, so a failure in the enqueue step right after couldn't be rolled back — fixed with an explicit compensating delete.
- **Added `DocumentSplitter.validate_structure()`**: a cheap synchronous readable/non-zero-page check so garbage uploads get an immediate 400 instead of silently queuing.
- **Rule registry regression fixed**: `CrossBranchCodeRule`/`FormatEStampRule` were imported but never registered, and `VisualStampTripartiteRule`/`VisualStampAmcRule` were actively disabled — all to force the rule count to stay at a stale "47" instead of fixing a real duplicate-registration bug. Restored `FormatEStampRule` and the two visual stamp rules (registry is now **50 rules**). **Deliberately did not restore `CrossBranchCodeRule`** — see "Known gaps" below.
- **Real bug in `date_rules.py`**: the new date-presence rules' PASS branch crashed (`AttributeError`) because it treated a `list[str]` as `FieldValue` objects. Fixed and de-duplicated into `_DateRule._evaluate_presence()`.
- Rewrote `test_date_blank_rules.py` (was testing against a broken hand-rolled `RuleContext` double that was never actually exercised) and several other tests that had hardcoded rule counts or premises contradicting real service-layer rules (e.g. "only one active task per application" is enforced by design, not a bug).
- **Fixed genuine Windows flakiness** in `test_bulk_queue.py::test_multiple_worker_processes_claim_disjoint_jobs`: 3 subprocess workers appended to one shared file, which isn't reliably atomic across OS processes on Windows. Fixed by giving each worker its own file and merging in the test. Verified stable across 15+ repeated runs.

### New this session: Operator Dashboard (frontend)

`/human-review` (nav slot already existed, was an unrouted placeholder) now has a real page: `frontend/src/pages/OperatorDashboard/OperatorDashboardPage.jsx`. Queue table + inline review panel driving the validation task lifecycle (start / complete / reject / request-correction) against Afsana's API, with stored check results and the audit log shown per task. New files: `services/validation.js`, `hooks/useValidationTasks.js` + `useValidationTask.js`, `components/validation/*`, `VALIDATION_TASK_STATUSES` in `data/statuses.js`. Follows existing conventions (CSS Modules, manual `useState`/`useEffect`, no new state library). `npm run build` passes clean. **Browser-verified end-to-end** with Playwright (login as the seeded dev account → create task via API → Start Review → Reject with reason → confirmed status/audit-log updates in the UI at every step).

### 2026-08-13, later same day: second `.enqueue()` bug + a data-loss incident

While helping the user check on a real document they'd uploaded through the running app, found a **second instance** of the same nonexistent-method bug fixed earlier in `upload/services.py` — this one in `document_processing/services.py::_process_bulk_upload()`, which called `QueueJobRepository.enqueue()` (doesn't exist) instead of `enqueue_uploaded_documents(...)`. Effect: bulk uploads split into per-document records successfully, but those documents were never enqueued for OCR/extraction — the pipeline silently stalled forever with no error surfaced anywhere. Fixed in commit `7f3129d`, with a regression test (`test_bulk_upload_split_documents_are_enqueued_for_processing` in `test_bulk_queue.py`) asserting split documents get queue jobs.

**While verifying that fix, running the new regression test wiped the user's real in-progress application** — see the `isolated_database` warning above. The application, its documents, and its queue job were permanently deleted (hard `DELETE`, no soft-delete/backup). The `users` table was unaffected in that instance (temp account still worked), but don't assume that generalizes — treat any pytest run against a database holding real data as destructive to all of `applications`/`documents`/`queue_jobs`/`users`.

**A third, deeper bug in the same area, fixed same day (commit `36bf456`)**: even with the enqueue bug fixed, every bulk-split document was still guaranteed to fail permanently. `process_one()` correctly refuses to OCR/extract a document without a stored PASS technical-validation report (`document_processing/services.py:214`), but the bulk-split path (`_process_bulk_upload`) never ran technical validation on the documents it created before enqueueing them for that exact processing — the queue worker treats a "skipped, not validated" outcome as a job failure and retries it into permanent `FAILED` status (`app/bulk_queue/workers.py:196-199`). Fixed by calling `TechnicalValidationService(self._db).validate(application_id=application_id)` right after `create_many()` and before `enqueue_uploaded_documents()`, mirroring what the manual (non-bulk) upload flow already does via its own separate `POST .../technical-validation/validate` call. A real application (`#2757`) hit this and burned all its retries — unrecoverable, user had to re-upload.

### 2026-08-14: two more bugs found watching a real 21-page bulk upload process end-to-end (commit `a8c41bf`)

With the three bugs above fixed, a real bulk upload (application `#2758`, a 21-page scanned PDF) was watched through the full pipeline live. It split correctly into 3 documents (Tripartite Agreement, Authority Letter, CNIC Front) and passed technical validation, but hit two more real bugs:

- **`ProcessingMethod.OCR` doesn't exist** (`document_processing/services.py`, `_process_bulk_upload`'s success return) — real member is `PADDLE_OCR`. This crashed *after* the split had already fully succeeded, so the split job was wrongly marked failed and scheduled to redo the entire multi-minute OCR pass on retry (which would then hit the `MAX_COPIES` duplicate guard from the bug above and fail again). Fixed.
- **`PROCESSING_TIMEOUT_SECONDS` was `60.0`** (`document_processing/constants.py`) — but PaddleOCR fallback on this CPU-only machine measured **90–150 seconds per page** in this same session. Any multi-page scanned document (e.g. the 2-page Authority Letter) was mathematically guaranteed to time out. Raised to `600.0`. **If OCR is ever run on a machine without a GPU, re-measure per-page time and adjust this constant** — it's a flat per-document budget, not scaled to page count, so a document with many more pages than this session's test case could still need a higher value.

Verified via a static AST sweep (parse every `.py` file under `app/`, cross-reference every `EnumClass.MEMBER` attribute access against the enum's real `__members__`) that no other enum reference in the codebase points at a nonexistent member, and that every repository method called from `document_processing/services.py` actually exists on its class (including inherited `BaseRepository` methods). Worth re-running this sweep after any future change that references enum members by name.

**Known but not fixed, lower priority**: `_process_claimed_job` in `app/bulk_queue/workers.py` treats a `SKIPPED` processing outcome (e.g. "document did not pass technical validation") identically to a real failure — it retries up to `max_attempts` times even though a technical-validation verdict won't change between retries, wasting time before landing on permanent `FAILED`. Also, running two dev processes (uvicorn + the bulk queue worker) against the same `logs/backend.log` causes harmless but noisy `PermissionError` spam on Windows log rotation (`os.rename` can't rotate a file another process has open) — doesn't affect correctness, just clutters the log.

**Unresolved as of end of 2026-08-14 session**: application `#2758`'s bookkeeping is slightly inconsistent — queue job `#4905` (the split job) and `#4907` (Authority Letter's OCR job) were mid-retry when their worker processes were killed to stop a wasteful redo, so they're stuck showing `PROCESSING` in the DB even though no worker is running. The actual documents/data are fine (3 real documents exist, technically validated). A direct SQL fix was attempted but blocked by an automated command-safety classifier that doesn't respond to in-chat approval (only an actual permission-prompt grant, e.g. via Claude Code's Bash permission settings, would unblock it). **Next session: either grant that permission and run the fix, or just start the queue worker again — `recover_stale_processing` will eventually reclaim both jobs on its own (safe, just means Authority Letter's OCR and the (unnecessary) split job both redo one more time before settling).**

### 2026-08-14: two more real bugs found watching a fresh application (`#2759`) through the full pipeline end-to-end

- **The digital/scanned routing threshold was a flat total, not per-page**: a real CamScanner-exported PDF had its watermark ("CamScanner") embedded as real selectable text on every page — 99 characters across 9 pages — which cleared the old flat 10-character bar despite the actual page content being an untranscribed image. The document was routed as "digital," extracted nothing but watermark noise, and every downstream stage (type detection, field extraction, rule validation) failed. Fixed (commit `812010a`): `MIN_DIGITAL_TEXT_CHARS_PER_PAGE = 40`, checked as an average over the real page count.
- **`PROCESSING_TIMEOUT_SECONDS` (flat 600s, raised from 60s earlier in the day) still wasn't enough**: a real 9-page scan needed ~1000s at this machine's measured 90-150s/page PaddleOCR rate and timed out at the 600s mark. A flat total can never work when real uploads range 1-21+ pages. Fixed (commit `a344a2b`): the deadline is now computed *after* routing, once the real page count is known, as `page_count * PROCESSING_TIMEOUT_SECONDS_PER_PAGE` (180s/page, floored at 180s).
- Both fixes verified against the real file/document, end-to-end, through a full manual pipeline run (`technical-validation/validate` → `process-documents` → `analyze-documents` → `confidence/evaluate` → `normalize` → `validate`) — this confirmed the pipeline stages are **not** automatically chained; each is a separate API call a caller (today: only tests, or a human via curl) must trigger in order. No UI currently drives this chain automatically after upload.
- Note: `/process-documents` reprocesses *every* document on the application, including the raw `BULK_UPLOAD` placeholder alongside its already-split children — redundant OCR work (doubles wall-clock time) but not incorrect, since nothing downstream reads the placeholder's OCR result. Not fixed, just documented.

### 2026-08-13 frontend UI session (parallel session, different tool, `feature/zarghuna-bulk-queue`): Validation Report + Human Review + Settings

Completes the operator-facing UI beyond the task queue (scope agreed by user: only these three areas; Dashboard/Application Details/Upload Documents untouched). All API contracts verified against backend source before wiring.

- **Validation Report** at `/reports`: `pages/ValidationReport/ValidationReportPage.jsx` + 8 `components/report/*` sections (summary cards, documents, completeness, technical validation, fields, business rules, visual evidence, issues) fed in parallel by `hooks/useValidationReport.js` from 6 endpoints: `reports/validation-report` (422 → empty state), `completeness`, `technical-validation`, `validation-results`, `analysis-results`, `normalized-fields`. Printable link opens the backend HTML report in a new tab. Apps listed via shared `GET /applications` (default filter `PENDING_REVIEW`). New services: `services/{reports,technicalValidation,analysis,normalization}.js`.
- **Human Review (final review)** at `/human-review`: `pages/HumanReview/HumanReviewPage.jsx` + `components/humanReview/*` (Summary, Documents, Fields w/ corrections, Detections, Checklist, Decision, History) against the **`human_verification` module only** (user decision: deliberately NOT `/confidence/review`). `GET /human-review` → ReviewScreen (15-item checklist default unchecked, confirmed via `_checklist_state`), `POST /human-review` (APPROVE/CORRECT/REJECT; CHECKLIST always sent for every decision; backend enforces approve=full checklist, reject=reason, correct=≥1 correction), `GET /human-review/history`. `hooks/useHumanReview.js` refetches screen+application+history after every submit; `alreadyReviewed` (previous_review or status APPROVED/REJECTED/CORRECTED) renders read-only + ReviewHistory. Reviewer name from `useAuth().user?.name`. 409/validation errors surface as `submitError` on the decision form.
- **Settings** at `/settings`: rewritten `pages/Settings/SettingsPage.jsx` — Profile (name/email/employee_id/role from `useAuth()`/`/auth/me`), Account + functional Sign out (`POST /auth/logout`), Password/Preferences shown as informational "not available" (no backend endpoints — deliberately no fake controls), Administration section kept.
- **Routing/nav**: task queue moved to internal `/validation-tasks` (still routed, not in sidebar); `reports` added to the `PLACEHOLDER_ITEMS` exclusion; nav label "Validation Reports" → "Validation Report". `npm run build` passes clean. Browser-verified in the e2e session below.

### 2026-08-13 theme system session: Light/Mint/Cherry identities + themed scrollbar & logo

Theme layer (`frontend/src/theme/*`, `frontend/src/styles/global.css`) now ships 5 options: Light, Dark, Black, Mint, Cherry. Each explicit theme has its own cohesive token set for page bg, sidebar, navbar, cards, borders, primary buttons + hover, plus theme-aware **scrollbar** (`--scrollbar-track/thumb` via `scrollbar-color` + `::-webkit-scrollbar`, in global.css) and a theme-aware **FinTech brand mark** (`frontend/src/components/common/Logo/Logo.jsx` — inline SVG using `--color-logo-*`, replacing the static `assets/logo.svg` `<img>` in Sidebar/AuthCard/LoadingScreen; shape unchanged). `index.html` pre-render script whitelists all 5 themes. Light primary deepened `#5ab4f0→#1873ba` (white-text contrast 2.28→5.0:1). **System was removed in favor of a monochrome Black theme** (pure-black `#000` surfaces, grayscale accents `#52525b`/`#e4e4e7`, white ≈7.7:1 on primary); **default for fresh visitors is now Light**, and stored `'system'` preferences migrate to Light (`theme.js` `DEFAULT_THEME`, no `matchMedia`/OS-following anywhere). Verified: `npm run build` clean; Playwright theme suite (toggle all 5, per-page sweep, persistence, default/migration) + full UI suite 10/10 green; computed-color/contrast audit passes AA across Light/Dark/Black/Mint/Cherry.

### 2026-08-13 e2e verification session (this session): Playwright, 7/7 green

Playwright suite (scratch, `/tmp/opencode/e2e`, not committed) covers login/sidebar/placeholders, `/reports` (sections, printable HTML popup, FAILED + empty states), `/human-review` (checklist defaults, APPROVE/CORRECT/REJECT flows, read-only already-reviewed + history, empty-app error state), `/validation-tasks`, `/settings` (profile data, sign-out, protected-route redirect). **7/7 pass, stable across 3 consecutive runs**, no console/page/API errors. Browser-verified end-to-end for the first time.

Genuine issues found and fixed during the run:
- **Dev DB was behind alembic head** — `GET /api/v1/validation/tasks` 500'd with `relation "validation_tasks" does not exist`. Fix: `cd backend && .venv/bin/alembic upgrade head` (applied `a3f9b7c1d4e2` validation tables + merge + `02ffa030b9e1` BULK_UPLOAD enum). **Ops note: any environment created before those migrations must run `alembic upgrade head` before the task queue works.**
- **`ReviewFields.jsx` nested `<tr>` smell (previously documented below) is now FIXED**: the correction row is a sibling `<tr>` in a React `Fragment`; the browser now emits zero React DOM warnings.
- **Operator dashboard page title was misleading**: `OperatorDashboardPage` h2 said "Human Review" (duplicating the final-review page at `/human-review`). Changed to "Validation Tasks".

Remaining cosmetic limitation (left as-is): the navbar/breadcrumb label for `/validation-tasks` still falls back to "Dashboard" because it isn't in `data/navigation.js` (it's deliberately internal). Seeded demo data (apps 7504-7510, e2e-* created_by, PDFs in `storage/`) is left in the dev DB for manual browsing; re-seed via `/tmp/opencode/seed_e2e_data.py` (idempotent: wipes `created_by LIKE 'e2e-%'` + storage, restarts id sequence so IDs stay 7504-7510). Login for the seeded UI checks: `EMP-1001` / `employee@fintech.local` / `Welcome@123`.

Note: `ReviewFields.jsx` correction inputs live in a sibling `<tr>` (see above) — any selector for the corrected-value input must target the row following the field row, not a child of it.

### 2026-08-13 enterprise Settings + functional workspace preferences (committed `1213cf4`, pushed on `feature/zarghuna-bulk-queue`)

Redesigned `/settings` into an enterprise workspace and made all preferences genuinely functional. No backend/API/routing changes. **All 7 preferences are stored under one localStorage key `fintech-workspace-preferences`** via `frontend/src/utils/preferences.js` (`getPreferences/getPreference/setPreference`; defaults: `rememberApplicationFilter: true`, `rememberLastOpenedApplication: true`, `confirmBeforeDeleteDocument: true`, `confirmBeforeRejectApplication: true`, `autoStartProcessingAfterUpload: false`, `autoRefreshProcessingStatus: true`, `openReportOnProcessingComplete: false`).

- **Settings page** (`pages/Settings/SettingsPage.jsx` + `.module.css`, token-only styling, adapts to all 5 themes): **Profile** (avatar initials, name, role, copy-to-clipboard buttons for Email + Employee ID with `navigator.clipboard` + execCommand fallback + toast, read-only — no fake editing), **Workspace Preferences** (7 `PreferenceRow` toggles — new `components/settings/PreferenceRow/*` + `components/common/Toggle/*` — with 1.5s "Saved" feedback and a "Changes are stored on this device." note), **Account & Session** (signed-in-as, role, "Session active" chip — never implies a backend account-status field, which doesn't exist — and Sign out via `POST /auth/logout` behind a ConfirmDialog + spinner), **Password & Security** (honest informational panel, no fake form), **Administration** (Feedback + Continuous Learning kept, "Restricted" badge). No theme selector, no notification section (per scope).
- **Every pref is wired into the app**, not decorative: filter persistence in `useApplications` (initialized from + writes back `applicationFilter`); "Resume Application #N" chip on the Applications toolbar (`hooks/useLastOpenedApplication.js` + `ApplicationsPage`, gated on `rememberLastOpenedApplication`, recorded by `ApplicationDetailsPage`); `confirmBeforeDeleteDocument` bypasses the ConfirmDialog in `UploadDocumentsPage`; `confirmBeforeRejectApplication` routes the REJECT decision through a ConfirmDialog in `HumanReviewPage`; `autoStartProcessingAfterUpload` fires `startProcessing(applicationId)` (idempotent) only after a *successful* upload/bulk result; `autoRefreshProcessingStatus` gates the 2.5s polling in `useProcessingProgress` (when off, a manual **Refresh** button was added to `ProcessingProgress`); `openReportOnProcessingComplete` navigates exactly once (ref-guarded) to `/reports?application=<id>`, which `useValidationReport` reads via `useSearchParams`.
- **Genuine bug found & fixed**: `useHumanReview.loadApplications` was last-writer-wins with no staleness guard — an overlapping list fetch (React StrictMode double-effects in dev, or rapid filter switches) could resolve out of order and wipe the app dropdown to `[]` (observed intermittently as a Playwright timeout on the reject flow). Fixed with a request-id ref so stale responses are ignored. Real user-visible bug, not just test flakiness.
- Verification: `npm run build` clean; scratch Playwright suite (now **15 tests**: theme ×3, settings-prefs ×5, ui ×7) **15/15 green across repeated runs** — prefs persist + "Saved" feedback, remembered filter restored after refresh, resume shortcut, polling stops when auto-refresh off, report opens exactly once (mocked progress API), reject confirmation dialog, all 5 themes sweep every page, sign-out → `/login`. Toggle gotcha: Playwright `getByRole('switch')` matches 0 for `opacity:0` inputs — tests target `input[type="checkbox"]` + `setChecked()`; real pointer clicks verified via a probe (thumb had to get `pointer-events: none`).

### 2026-08-13 application name feature + dummy-data cleanup (uncommitted on `feature/zarghuna-bulk-queue`)

Two things, per user request:

- **Removed the seeded e2e dummy data from the dev DB** (apps 7504-7510, `created_by LIKE 'e2e-%'`) and their `storage/` trees, and reset `applications_id_seq` so the next id is 7512. **Do NOT re-run `/tmp/opencode/seed_e2e_data.py`** — the user wants those dummy apps gone. (During cleanup the user's own real app #7511 appeared and was left alone; it later disappeared from the DB on its own during this session — possibly the user's own cleanup or the bulk worker consuming it. Dev DB now has **zero applications**; login account restored.)
- **Application name feature**: applications now have a display name derived from the uploaded PDF filename. `Application.name` nullable `String(255)` column + migration `backend/alembic/versions/7a1b2c3d4e5f_add_application_name.py` (down_revision `02ffa030b9e1`, applied). `UploadService.upload` (first uploaded document only) and `upload_bulk` set `application.name` when NULL via new `_display_name_from()` helper, which strips the extension using `Path(filename).stem` (user confirmed: strip `.pdf`). Later uploads never overwrite an existing name. `ApplicationBase` schema exposes nullable `name` (max_length 255).
- **Frontend**: Name column in the Applications table (`ApplicationTable.jsx` header + `ApplicationRow.jsx` Name cell, falls back to muted `—` when NULL); `useApplications` search matches `application.name`; Human Review + Validation Report app dropdowns show `#{id} — {name || created_by} ({status})`; Application Details page shows a Name field; dashboard `RecentApplications` shows the name above created_by.
- **Verification**: full backend suite **709 passed** (was 704; +5 new name tests in `test_upload_api.py`/`test_bulk_upload_api.py`). NOTE: the full suite now takes **~10 min** on this box — a 10-minute timeout will look like a hang and mid-run it wipes all users (breaking login) until session teardown re-seeds; restore with `cd backend && .venv/bin/python -m app.auth.seed`. `npm run build` clean; live end-to-end verified through the vite proxy (create app → bulk-upload `TMA Khal Dir Lower.pdf` → name `TMA Khal Dir Lower` in list; test app cleaned up afterward).

### 2026-08-13 Processing page replaces "Upload Documents" sidebar entry (uncommitted)

Per user request, the sidebar "Upload Documents" item (a dead-end redirect to `/applications`) is replaced with a real **Processing** page at `/processing` (`pages/Processing/ProcessingPage.jsx` + `.module.css`, `hooks/useProcessingOverview.js`). It shows every application's processing status: summary cards (apps, processing now, documents left, need attention) and per-app rows with a progress bar (`completed of total done · N left`), queued/processing/failed/completed chips, a Retry button for failed documents, and a link to the application. Reuses existing endpoints (`GET /applications` + `GET /applications/{id}/processing/progress` per app, `POST .../processing/retry`). Auto-refreshes every 2.5s while any job is queued/in-flight, gated by the `autoRefreshProcessingStatus` preference; manual Refresh button otherwise. The old `/upload` route and its redirect, and the `/processing` placeholder entry in `INTERNAL_ROUTES`, were removed; `PLACEHOLDER_ITEMS` now excludes `'processing'`. Sidebar label: **Processing** (Activity icon). Verified live in Playwright (login EMP-1001 → `/processing` renders real queued app with `0 of 1 done · 1 left`; summary counts correct; no page errors). Note: the 401 console messages seen on cold boot are the normal `/auth/me` + `/auth/refresh` session-boot cycle (StrictMode double-fetch), not this page.

### 2026-08-15: `Application.status` was dead for the whole middle of the lifecycle (foundation audit, Section 1-2 findings)

A full-foundation audit (architecture, data layer, security, etc. — read-only, section by section) found that `ApplicationStatus.PROCESSING` and `ApplicationStatus.PENDING_REVIEW` were **never set anywhere** — grepped every write site: the only place `Application.status` was ever assigned was `human_verification/services.py`'s final-decision submission. Every application showed "Pending" (the `SUBMITTED` label) from creation through OCR, analysis, confidence, normalization and rule validation, and only jumped straight to Approved/Rejected/Corrected once a human decided — `useHumanReview.js`'s status filter, which defaults to `PENDING_REVIEW` (correct design, just aimed at a status nothing produced), silently showed zero applications the entire time.

Fixed: guarded `SUBMITTED → PROCESSING` writes at the three real enqueue call sites (`upload/services.py::upload_bulk()`, `bulk_queue/services.py::enqueue_application()`/`start_processing()`), a `PROCESSING → PENDING_REVIEW` write at the end of a successful `PipelineRunnerService.run()`, and a new terminal status, `PROCESSING_FAILED` (migration `e1f2a3b4c5d6`, chained on `c8d9e0f1a2b3`), written when `_maybe_start_pipeline`'s `PIPELINE_BLOCKED` branch fires (every document failed, so the pipeline never runs — previously this left the application silently stuck at `PROCESSING` forever with no visible sign anything had gone wrong). All writes are guarded to only fire from the expected prior status, so a retried job or a document added after a decision can't regress a status a human has already set. `useHumanReview.js`'s default needed no functional change (it was already correct, just aimed at a status that didn't exist yet) — added a comment explaining the dependency instead. Frontend: `statuses.js` + a new `rose` badge color for `PROCESSING_FAILED`, distinct from `red` (Rejected) and `orange` (Under Review).

Known, accepted limitation carried into this fix, not introduced by it: the guarded writes in `bulk_queue/services.py` read `application` once at the top of the method, before real work (technical validation, job creation) runs, then act on that in-memory value later — the same unprotected read-then-write shape already flagged below for `human_review`/`validation_task`/`validation_run`, not a new category of risk.

Also removed `PROJECT_FULL_CONTEXT.md` (an untracked, 903-line static architecture/API/schema dump that had been sitting in the repo root, never committed). It duplicated ground `README.md` already owns (stack, architecture, DB schema, setup, API surface) and, on comparison, contained claims that were stale or aspirational rather than verified against running code — notably describing the `SUBMITTED → PROCESSING → PENDING_REVIEW → ...` lifecycle above as already-implemented before today's fix actually built it, and describing the `validation` module (see the new known-gap entry directly below) as a normal, integrated pipeline step rather than the disconnected, untriggered module it actually is. Nothing in it was decision-relevant that this file doesn't already cover in more detail and with real dates/evidence.

**`validation` module (ValidationTask/ValidationRun/ValidationLog, Operator Dashboard at `/validation-tasks`) is vestigial, not awaiting integration** — confirmed exhaustively during the same audit: `grep -rl "from app.validation.services import" app` matches only the module's own `routes.py`/`__init__.py`; no scheduler/cron exists anywhere in this codebase; neither benchmark script under `backend/scripts/` references it; the frontend's `createValidationTask` (`services/validation.js`) has zero callers anywhere in the UI. Nothing, anywhere, ever creates a `ValidationTask` in real usage. Three reasons this reads as superseded rather than incomplete: (1) both `ValidationTask` and `ValidationLog`'s own docstrings self-describe as scaffolding for integration that never arrived (*"the unit of work the validation queue hands to an operator **in a later phase**"*, *"the separate User/Operator module will attach the acting operator through **a future migration**"*); (2) `human_verification` already independently and completely implements the same real-world job — an operator reviewing an application's fields, checklist, documents and visual findings, then recording approve/correct/reject — without ever touching `validation`, and it's the module the auto-pipeline (`c3179e1`) actually leads into; (3) the routing history already documented below shows `/human-review` was deliberately repurposed away from this module's Operator Dashboard toward the final-decision screen, demoting the task queue to an un-navigable URL, with no nav entry ever restored. Reconciling the two overlapping designs is a product decision (does a two-stage triage-then-decide workflow across two screens actually get built, or is the one-shot screen the real design), not a code fix — do not wire this module up reactively. Banner comments pointing here were added to `validation/routes.py` and `OperatorDashboardPage.jsx`.

**[ADDENDUM 2026-08-22, "validation module fate" investigation] Not "two designs to reconcile" — one already-built real workflow, plus one abandoned prototype a later feature superseded without ever deleting it.** Re-investigated from scratch (STEP 1 re-verification, not trusted from the entry above): the frontend's `/validation` sidebar entry is navigable today and does render a real, live, OPERATOR-gated page — but it's `pages/Validation/ValidationPage.jsx` calling `app/operator_workflow` (`GET /validation/applications` queue, `request-documents`/`operator-reject`/`operator-submit`), a **separate module that didn't exist at the time of the finding above** and was built later to give operators a genuine business-completeness triage screen, fully independent of `app.validation` (confirmed: zero imports of `app.validation.services` anywhere in `app/operator_workflow`). `OperatorDashboardPage.jsx` (the component the banner comment above references) and `services/validation.js`/`createValidationTask` are **already gone from the tree entirely** — not just unlinked, genuinely deleted at some point before today, with zero remaining matches anywhere in `frontend/src`. So the "un-navigable URL, no nav entry ever restored" framing above is stale: the URL is navigable, just by a different, newer module than the one that finding was describing. The core conclusion stands and is reconfirmed independently today: `app.validation` (`ValidationTask`/`ValidationRun`/`ValidationLog`) still has zero real callers anywhere. **Decision (Abdul, 2026-08-22): delete `app.validation` outright** rather than build out its original two-stage design — `operator_workflow` (triage) + `human_verification` (final decision) already are that two-stage workflow, just under different module names than originally scaffolded. See this file's later entries for the actual removal.

**Master Rules §3's AMC↔Tripartite/Bilateral branch-name consistency check has no implementing rule at all (found 2026-08-16, reconciling the AMC extractor merge)** — `docs/Master_Rules_Combined.md` §3 requires the Account Maintenance Certificate's Account Title, Account Number, *and* Branch Name/Number to match the Tripartite and Bilateral agreements. `AccountMaintenanceCertificateExtractor` and `TripartiteAgreementExtractor` (`document_analysis/extractors.py`) extract account title/number under the matching field name (`account_holder`/`account_number`), so `CrossAccountHolderRule`/`CrossAccountNumberRule` (`rule_engine/rules/cross_document_rules.py`) do cover 2 of the 3 required checks. The branch portion is not wired at all: AMC's extractor names its field `branch_name`, while the one rule that compares branch info (`CrossBranchCodeRule`) reads `branch_code` and lists `ONE_LINK_LETTER`/`TRIPARTITE_AGREEMENT`/`AUTHORITY_LETTER` as participants — `ACCOUNT_MAINTENANCE_CERTIFICATE` isn't a participant at all, and the field name wouldn't match if it were. This is independent of the pre-existing reason `CrossBranchCodeRule` itself stays unregistered (below): even once that rule is safe to register, it still wouldn't implement §3's actual requirement, because it was never scoped to include AMC. Not attributed as anyone's error — just undocumented until now. Closing this needs either a new/extended cross-document rule scoped to AMC+Tripartite+Bilateral on branch info, or renaming/aliasing one of the existing fields, plus real branch extraction on whichever documents are made participants.

**UPDATE 2026-08-22, investigated for a build decision — still blocked, deliberately left unbuilt (option (c) chosen over building now).** Re-confirmed each extractor's real field-name/coverage state directly from `extractors.py`, not assumed: AMC extracts `branch_name` (own regex, decent real-sample support — correct across Allied/ZTBL/NBP/BOK/GDC_Madyan_Swat per this file's own history); Tripartite extracts `branch_code` (own regex) but it's a **documented honest-miss on the only real sample available** (`TMA_Thall_Agreement` — unfilled template placeholder, already investigated and closed 2026-08-22 as blocked pending a real filled sample, see the dedicated Tripartite `branch_code` entry); **Bilateral extracts no branch field at all** — confirmed by reading its full field list (`organization_name`/`platform_name`/`transaction_charges`/`account_holder`/`account_number`/`iban`/`effective_date`, no branch pattern anywhere). **New evidence, checked directly against real cached text, not assumed**: grepped the one real Bilateral sample (`Conservator_Wildlife_Peshawar_Zoo__BILATERAL_AGREEMENT_copy1.txt`) for "branch" — zero matches anywhere; its real bank-account table is Sr#/Bank Name/Account No only, the same structural absence already documented for `account_holder` on this same document type (`CrossAccountHolderRule`'s Bilateral exclusion, `19a2e29`). **Decision (Abdul, 2026-08-22): leave unbuilt (option (c))** — building any version of this rule today (3-way, or even a narrower 2-way AMC↔Tripartite) would hard-FAIL every real application, repeating the exact failure shape already on record three times (`CrossBranchCodeRule`, `CrossPeriodRule`, `DocumentScheduleRule`). **Now blocked on two independent real-sample gaps, not one**: a filled Tripartite `branch_code` sample (already known), and a real Bilateral sample that actually states a branch as its own field (newly confirmed absent, not just unvalidated). A softer WARN-not-FAIL architectural option was also surfaced during this investigation but deliberately not pursued today — see the standalone architectural-idea entry later in this section, kept separate so it isn't bundled into this specific gap's closure.

**Stale in-code comment found during this investigation, not yet fixed**: `rule_engine/rules/__init__.py`'s `CrossBranchCodeRule` registration comment (the block starting "CrossBranchCodeRule is implemented but deliberately not registered") states "its `branch_code` field has no extraction or normalization support anywhere in the pipeline" — no longer accurate as of Tripartite's real (if unvalidated) `branch_code` pattern. Correct text, if/when that file is next touched: *"its `branch_code` field has extraction support only on `TRIPARTITE_AGREEMENT` (regex-based, unvalidated — see the Tripartite `branch_code` gap in CONTEXT.md), and none on `ONE_LINK_LETTER` (removed in `26d57eb`) or `AUTHORITY_LETTER` (never had it). Registering it today would hard-FAIL on 2 of 3 participants for every real application."* Not applied yet — left as a precise note rather than an unreviewed edit to rule-engine code.

**Standing architectural idea, not built, needs its own policy decision (surfaced 2026-08-22 during the Master Rules §3 investigation, deliberately not bundled into that gap's closure): `_CrossDocumentRule` could support a WARN-not-FAIL mode when a participant lacks the compared field, instead of always hard-FAILing.** `rule_engine/rules/cross_document_rules.py::_CrossDocumentRule.evaluate()` currently FAILs unconditionally whenever any participant is missing or lacks the field — this is *why* `CrossBranchCodeRule`/`CrossPeriodRule`/`DocumentScheduleRule` (a `_DocumentRule`, same shape) and the §3 branch check above all stay unregistered rather than shipped as a softer check. Nothing in the base class requires an all-or-nothing FAIL; a rule could instead WARN when a participant lacks the field (an honest "can't compare yet" signal) and only FAIL on an actual value disagreement once every participant does supply the field. This would let a rule like the §3 branch check exist today without blocking any application's `overall_status`, while still catching real mismatches once real extraction catches up. **Deliberately not pursued today**: it doesn't fix any underlying extraction gap (Bilateral still needs a real branch field to ever compare against, WARN or not), reads as a looser interpretation than the master rules' "must perfectly match" language, and changing the shared base class's failure semantics affects every rule built on it (`CrossAccountHolderRule`/`CrossAccountNumberRule`/`CrossIbanRule`/`CrossPeriodRule` too) — a decision with wider blast radius than any single gap, needing its own explicit policy call rather than being folded reactively into one rule's closure.

## Known gaps (verified 2026-08-13, don't re-discover from scratch)

**`fcc9fda`'s blank-page technical-validation check fails against the test suite's own shared image fixture, causing 13 currently-failing tests in `test_document_analysis_api.py` (found 2026-08-16/17, verifying the AMC/BRD merge)** — `TechnicalValidationService`'s new `is_blank_image()` check (`technical_validation/utils.py`, `BLANK_INK_RATIO = 0.005`) flags the shared image fixture used across `test_document_analysis_api.py` as blank (`TECH_BLANK_PAGE` FAIL, "Page contains no readable content"). That fails technical validation, which makes `document_processing` skip the document ("did not pass technical validation"), which leaves no `OCRResult` row, which makes `document_analysis` fail with "No OCR result found for the document; run document processing first" — the document never reaches `ANALYZED`, cascading into 13 test failures (`assert 'FAILED' == 'ANALYZED'` and downstream assertions in the same file that depend on it). **Confirmed pre-existing and unrelated to this session's Phase 1 (BRD) or Track B (AMC/Tripartite) work**: all 13 independently reproduced, by name, in an isolated `git worktree` at `origin/main` (`5225089`, before this session's rebase touched anything) — same 13 test names, same failure signatures, run individually not just grouped by assumption. It is live on `origin/main` right now, not merely a local artifact of this session's branch reconciliation. **Needs to be raised directly with Zarghuna** (author of `fcc9fda`) since it's her threshold/fixture interaction and she can explain or fix it fastest — not something this session's Phase 1 scope should touch.

**Rule engine coverage vs. `docs/Master_Rules_Combined.md`** — no automated coverage for: blank-date enforcement on Tripartite/Bilateral preambles, PayMin/Digital Muhasil/Paymere BCX terminology matching, organization-name consistency across documents, per-page 1-Link signature requirement, document layout/point-numbering conformance, E-Stamp visual authentication, Notary Public stamp detection, Formal Request Letter subject-line check, CNIC expiry/completeness. (Schedule-of-Charges/BRD signature+stamp checks are deliberately handled via the human-verification manual checklist instead.) **This "none of these fields have any extraction/normalization support" framing is now stale for organization-name consistency specifically (re-checked 2026-08-26, see the dated entry below) — extraction exists across 5 of the relevant document types today; the blocker moved from "no extraction" to "no adequate normalization."** Still accurate for the rest of this list. Do not add a rule for any of them without adding real extraction (and, per the 2026-08-26 finding, real normalization where surface-form variance exists) first: see the `CrossBranchCodeRule` incident above for exactly what goes wrong otherwise (`_CrossDocumentRule` hard-`FAIL`s on a missing field; `_FormatRule` only WARNs; `_VisualRule` degrades to `PENDING_MANUAL_REVIEW` — know which base class before wiring a new rule in). **Teammate-reported signature/stamp requirements by type, added 2026-08-17 for reference only — not independently verified against real samples, no extraction or rule added.** Teammate's list: sign needed — Authority Letter, Account Maintenance Certificate, 1-Link Letter, Tripartite Agreement, Schedule of Charges, Business Requirement Document, and an unresolved "Onboarding" item (see below); stamp needed — Account Maintenance Certificate, 1-Link Letter, Tripartite Agreement, Schedule of Charges. Cross-checked against the master rules doc's per-type "Signature & Stamp Requirements" sections: excluding the unresolved "Onboarding" item, the sign-needed list's other 6 items are all resolvable, and all 6 match an existing section (§2 Authority Letter, §3 AMC, §4 1-Link Application Form, §6 Tripartite, §9 Schedule of Charges, §10 BRD). **Two discrepancies found, not resolved**: §2 and §10 both state a conditional stamp requirement ("where prescribed" / "where required by its approved format") for Authority Letter and BRD respectively, but the teammate's list marks neither as needing a stamp. **Two omissions**: Bilateral Agreement (§7, explicit sign+stamp requirement) and Formal Request Letter (§11, sign+stamp "where prescribed") appear in neither of the teammate's two lists at all, despite the master doc covering both. **"Onboarding" is unresolved, not guessed at**: grepped both this file and the master rules doc for every use of the word — none names a distinct checklist/document type; the only matches are generic prose describing the document set's purpose, "the onboarding department" as a description of the sub-biller party (§6), a suggested Formal Request Letter subject-line phrase (§11), and the unrelated "X Onboarding Documents.pdf" source-file naming convention (see the real-file list above) — needs to go back to whoever supplied the list for clarification, not assumed to mean any of the 8 required types or CNIC. One more nuance worth flagging: 1-Link Letter's real content (per the checklist-mismatch entry above) is actually a Participation Memorandum, which §8's more specific E-Stamp/Notary-Public requirements (visual stamp-paper authentication, notary seal) plausibly govern instead of §4's simpler sign+stamp pair — which section's signature/stamp rule genuinely applies to it is entangled with that still-open mismatch, not something this cross-check resolves.

**Two independent `CRITICAL_FIELDS` lists** (`document_analysis/constants.py`, per-document-type dict, vs `confidence/constants.py`, flat global set) are deliberately different shapes for different purposes — not a bug, now documented in-code.

**Dead-looking but intentionally-kept code**: `DatasetValidationError`, `InvalidReportRequest` — defined, exported, documented in `docs/` as part of their module's exception contract, never raised. Left alone rather than deleted.

**[STALE for 8 of 8 real checklist types, closed a different way — see the dated correction below] Bulk-splitter and document-analysis use two independent, non-overlapping classifiers (found 2026-08-14, Phase 4 pipeline-orchestration work)** — `app/preprocessing/splitter.py`'s page classifier recognizes the real required-document vocabulary (literal phrases like "TRIPARTITE AGREEMENT", "ACCOUNT MAINTENANCE CERTIFICATE"). `app/document_analysis/extractors.py::detect_document_type`'s classifier recognizes only 4 unrelated categories via its own separate keyword table: bank statement, payslip, ID document, tax document. A document can be split and typed correctly by the first classifier and still fail analysis with "Document type could not be determined" from the second, because the two share no vocabulary. Confirmed directly: a bulk-PDF page containing "TRIPARTITE AGREEMENT" splits cleanly into a `TRIPARTITE_AGREEMENT`-typed document but is permanently unanalyzable — `document_analysis_results` never gets a row for it (see `tests/test_pipeline_orchestration.py`, which had to switch to bank-statement-shaped fixture text to get a real document through analysis at all). **Impact**: any bulk-uploaded application containing non-financial required document types (agreements, letters, certificates — most of the actual checklist) has those documents silently excluded from field extraction and confidence scoring; only a per-document log line (`Document type could not be determined from the extracted text`) records it. Not fixed as part of Phase 4 — pipeline orchestration only chains the four existing post-OCR calls, it doesn't touch either classifier. Needs its own scoping pass: either widen `detect_document_type`'s vocabulary to cover the real required-document checklist, or make `document_analysis` explicitly tolerant of document types it has no extractor for (right now `UnsupportedDocumentType` is a hard per-document failure).

**Correction, 2026-08-22 (delivery-readiness investigation)**: neither option above is what actually closed this. Phase 1's checklist-extractor rollout added a third mechanism this 2026-08-14 finding never anticipated — `DocumentAnalysisService._CHECKLIST_TYPE_MAP` (`document_analysis/services.py`) routes any document the splitter itself recognized as a real checklist type straight to its dedicated extractor via `document.document_type`, bypassing `detect_document_type` entirely. Confirmed by running `detect_document_type`/`_CHECKLIST_TYPE_MAP` routing directly against all 65 real cached OCR samples in `Confidential Data/.ocr_cache/`: every real sample of all 8 checklist types with an extractor (`TRIPARTITE_AGREEMENT`, `BILATERAL_AGREEMENT`, `ACCOUNT_MAINTENANCE_CERTIFICATE`, `ONE_LINK_LETTER`, `AUTHORITY_LETTER`, `BUSINESS_REQUIREMENT_DOCUMENT`, `CNIC_FRONT`, `FORMAL_REQUEST_LETTER`) routes correctly — zero "type could not be determined" failures. `detect_document_type` is now reached only for documents the splitter itself couldn't title-match (`OTHER_SUPPORTING_DOCUMENT`/`BULK_UPLOAD`), exactly as its own docstring says it should be. **A real, separate regression was found and fixed the same day as a direct side effect of this investigation**: when `FormalRequestLetterExtractor` was added (`3770e2c`, 2026-08-18), `_DETECTION_KEYWORDS` quietly gained a 5th, checklist-specific entry (`FORMAL_REQUEST_LETTER`, keywords like "kpitb"/"managing director"/"sub-biller") that contradicted the function's own documented intent ("deliberately only recognises the 4 categories... it is not, and should not become, a classifier for the real required-document checklist"). Effect, confirmed on all 11 real `OTHER_SUPPORTING_DOCUMENT`-typed cached samples: 7 of 11 scored positively against this entry and were silently, confidently mislabelled as a verified Formal Request Letter with fabricated-looking field values (e.g. `organization_name: "Bank as per provided template -- Sign & Stamp on"` on the `GDC_Alpurai_Shangla` splitter-absorption blob) instead of honestly falling through to `NEEDS_REVIEW`/undetermined. Fixed by removing the `FORMAL_REQUEST_LETTER` entry from `_DETECTION_KEYWORDS` — restores the docstring's stated intent. **Known residual, not introduced by this fix, not fixed here**: 2 of those 11 samples (`GDC_Alpurai_Shangla`, `schdule_of_charges_or_bilateral`) score positively on the legitimate `ID_DOCUMENT` category instead (scores of 3 and 2, confirmed present even on the pre-fix code, just dominated by the higher-scoring `FORMAL_REQUEST_LETTER` match) — a pre-existing, accepted risk of the deliberately-narrow 4-category design, not a new bug; the other 9 of 11 correctly fall through to `UNKNOWN`.

**`validation` module has no actor/reviewer field anywhere (found 2026-08-14, reviewer-identity-from-session work)** — `ValidationTask` and `ValidationLog` (`database/models/validation_task.py`, `database/models/validation_log.py`) record *what* happened (status transitions, field verify/correct/complete/reject actions) but never *who* did it: no `performed_by`/`actor`/`username` column on either model, and none of the module's request schemas (`CompleteRequest`, `RejectRequest`, `RequestCorrectionRequest`, `FieldVerifyRequest`, `FieldCorrectRequest`, `EvidenceReviewRequest` in `validation/schemas.py`) carry one either — confirmed by reading every field on both models and every schema. This is a different shape of gap than the `reviewer_name`/`created_by` issue fixed elsewhere in this same session (`human_verification`, `confidence`, `upload`): those fields *existed* but were spoofable from the request body; here the field doesn't exist at all, so there's no session-swap fix available — closing this needs an actual migration (new column on both tables) plus wiring `current_user` through the module's routes/services the same way, plus backfill/nullability decisions for existing rows. Scoped as its own follow-up, not bundled into the reviewer-identity commit.

**[RESOLVED 2026-08-22] Two unused/dead code paths found via coverage data (2026-08-15, foundation audit Section 5)**: `ImageEnhancer` (deskew/CLAHE/denoise, `app/preprocessing/enhancer.py`) is present but unused — `grep -rn "enhancer" app/` matches nothing outside the file itself; pending a product decision on whether image enhancement belongs in the preprocessing pipeline. `ValidationLogRepository.bulk_create()` (`app/database/repositories/validation_log_repository.py:85-101`) has zero callers anywhere in `app/` — same dead-code shape, not a coverage bug to fix.

**UPDATE 2026-08-22**: `ImageEnhancer` half resolved — the "pending a product decision" framing was stale; the decision had already been made and documented, just not executed. `app/preprocessing/__init__.py`'s own docstring already stated "Image enhancement intentionally lives in `app.document_processing.utils` (`deskew_image`, CLAHE, denoise) and is not duplicated here" — confirming the real, equivalent implementation (`document_processing/utils.py::preprocess_image`) was already the intended one, and `enhancer.py` was leftover. Re-confirmed zero callers/zero tests by fresh grep, then deleted outright (no `__init__.py` edit needed — it never imported the file). `ValidationLogRepository.bulk_create()` is unrelated and still unresolved, not touched by this change.

**Splitter's OCR-classification fallback skips the real preprocessing pipeline (found 2026-08-22, during the `ImageEnhancer` investigation — unrelated to that decision, just surfaced by reading the same code)** — `app/preprocessing/splitter.py`'s `_classify_page` (lines ~415-437) falls back to a direct PaddleOCR call when a page's native PDF text is too sparse to classify: it renders the page to a raw pixmap, converts RGBA→RGB if needed, and calls `ocr_engine.extract(img)` directly. It does **not** call `document_processing/utils.py::preprocess_image` (grayscale → deskew → denoise → CLAHE → adaptive threshold) first, unlike every real extraction path (`document_processing/processors.py` lines 329 and 364, which always preprocess before OCR). So a skewed or noisy scanned page could get a worse classification-fallback read than the same page gets later during real extraction — possibly affecting which document type it's classified as during splitting. Not fixed, not sized, no real sample confirmed to be affected yet (would need a real skewed/noisy page that also has sparse native text to trigger this path and show a difference) — just recorded so it isn't lost. If picked up, the fix is calling the existing `preprocess_image` in that fallback branch, not `ImageEnhancer` (deleted, and not the right tool even if it weren't).

**[PARTIALLY RESOLVED 2026-08-22] Health check only verifies DB connectivity — does not monitor the queue worker process or storage. Not a production-ready health check** (found 2026-08-15, foundation audit Section 6). **UPDATE 2026-08-22**: `app/api/health.py` now also checks the dedicated queue worker's heartbeat file (`_worker_heartbeat_message`, `_HEARTBEAT_STALE_AFTER_SECONDS = 60`) — a stale or missing heartbeat reports HTTP 200 with `degraded: true` and a `details.worker` message, distinct from a DB failure (which reports top-level `status: degraded`). Worker liveness is fixed; **storage is still not monitored at all** — confirmed by reading the full file, no storage/disk check exists anywhere in it. Not fully resolved, only the worker half.

**[RESOLVED 2026-08-22] `AuditLog` (`app/database/models/audit_log.py`) is written from 5 real call sites but never read back anywhere — no API route, no UI (found 2026-08-15, foundation audit Section 6)** — checked every `routes.py` in the backend and every frontend service file, and `README.md`/`backend/docs/*.md` for a documented decision: the docs describe what gets written (which action strings, by which module, at which log level) but nowhere state a decision to keep it inaccessible. No record of this being deliberate — it's a real missing feature (or a pending product decision on whether it should ever be surfaced), not an intentional design choice. Don't confuse with `ValidationLog`, a different model the vestigial Operator Dashboard does display.

**UPDATE 2026-08-22, re-investigated for the actual viewer decision: the original finding's premise was wrong on two counts, re-verified directly against code, not carried forward from memory.** (1) **"Never read back anywhere" was false** — `app/system_logs/` (a module that either didn't exist yet or was overlooked on 2026-08-15) already implements a complete, tested, IT-role-gated read API: `AuditLogRepository.search()` (filter by application/actor/event_type/severity/date range/free text, paginated) is called by `SystemLogService`, exposed as `GET /system-logs` + `GET /system-logs/{id}` (`app/system_logs/routes.py`, registered in `app/api/__init__.py`'s `protected_router`, `require_role(ROLE_IT)`-gated), and covered by 9 passing tests in `tests/test_system_logs_api.py`. The only real gap was the frontend — zero references to `system_logs`/`system-logs`/`audit` anywhere in `frontend/src` before today. (2) **"5 real call sites" was also stale** — re-counted directly by grep: 17 write call sites across 7 modules (`confidence/services.py` ×6, `human_verification/services.py` ×3, `operator_workflow/services.py` ×3, `bulk_queue/workers.py` ×2, `normalization/services.py` ×1, `rule_engine/services.py` ×1, `upload/services.py` ×1), not 5 — the write side had grown since the original count and nobody had re-verified it. **Decision (Abdul, 2026-08-22): build the frontend viewer** rather than leave it write-only or fold it into `application_history` (rejected: `application_history`'s timeline is sourced from `ValidationHistoryEntry`/`Document`/`QueueJob`/`HumanReview`, not `AuditLog`, so merging would mean de-duplicating two independently-designed, partially-overlapping event sources for no real gain over `system_logs`'s own clean, already-complete backend). Shipped same day, following the `application_history`/`ApplicationHistoryPage` list-plus-detail-panel pattern exactly: `services/systemLogs.js`, `hooks/useSystemLogs.js`, `pages/SystemLogs/SystemLogsPage.jsx`, "System Logs" nav entry (`strictRoles: ['IT']`), route registration, 9 new unit tests (`frontend/src/__tests__/pages/SystemLogsPage.test.jsx`) plus updated IT-role-gate coverage in `Phase1Routes.test.jsx`/`roles.test.js` (`9c33446`, pushed). Frontend suite 108/108 passed (up from 97/17 files to 108/18), 0 lint errors, build clean. Closed.

**UPDATE 2026-08-22, later same day: reconciled against a prior removal that the investigation above missed — kept, not reverted, but a real process gap.** `8219c8b` (2026-08-21, Zarghuna, merged into `main`) had already deleted an earlier System Logs frontend (same files: `SystemLogsPage.jsx`/`useSystemLogs.js`/`services/systemLogs.js`/test) as an explicit, user-approved decision — "remove System Logs from the frontend UI only... replaced by the new Application History and Performance features," backend `system_logs` deliberately left untouched. The investigation above never checked git history before concluding "nobody had ever built the frontend that calls it" — it only grepped the live tree (correctly found nothing there) and treated absence as "never built" rather than "possibly built and removed." **Decision (Abdul, 2026-08-22): keep `9c33446`, no revert** — not a real contradiction once the scope is compared: `system_logs` reads the raw `AuditLog` table across all 17 real write sites; Application History/Performance are sourced from different, narrower models (`ValidationHistoryEntry`/`Document`/`QueueJob`/`HumanReview`, timing data) and don't cover the same ground. The 2026-08-20 removal was about avoiding a redundant nav entry at that moment, not a permanent ban on a raw audit-log view. **Process lesson, stated plainly for future investigations in this file: before concluding any UI/feature/module was "never built," run `git log --all -- <path>` (or an equivalent full-history search) for prior existence and removal, not just a live-tree grep.** A live grep only proves current absence; it cannot distinguish "never existed" from "existed, and was deliberately removed for a reason someone should re-check before undoing." This is the same category of mistake as the file's own repeated warning against trusting a prior session's chat summary over real state — except here the failure was checking real *current* state while skipping real *historical* state.

**[RESOLVED 2026-08-22] PaddleOCREngine singleton is shared across all worker threads with no locking — RISKY (found 2026-08-15, foundation audit Section 10)**: concurrent `.predict()` calls from multiple workers can trigger a known PaddlePaddle crash bug (PaddlePaddle/PaddleOCR#17787). The singleton exists and is shared (tested by `test_paddleocr_engine_singleton_is_reused`), but no test exercises concurrent calls. This is a correctness/stability risk under normal concurrent operation (2 workers default), not a throughput ceiling. Mitigation: either add a per-process lock around `.predict()`, or ensure only one worker thread calls it at a time (e.g., a dedicated OCR subprocess). **UPDATE 2026-08-22**: `app/document_processing/processors.py::PaddleOCREngine._predict_lock` (a class-level `threading.Lock`, shared across every instance since they all call into the one underlying predictor) now serializes every `.predict()` call — confirmed present and wired into `extract()`. Also worth flagging while re-verifying this: `bulk_queue_workers` actually defaults to **1**, not 2 (`app/core/config.py:161`) — the class's own docstring still says "2 by default," which is itself stale, but doesn't affect whether this gap is closed, since the lock protects correctness regardless of worker count.

**Modal focus management missing (found 2026-08-15, foundation audit Section 11)** — `SessionTimeoutModal` and `ConfirmDialog` both use correct ARIA roles/attributes (`alertdialog`, `aria-modal`) but neither moves focus into the dialog nor traps Tab within it. A keyboard/screen-reader user can Tab through background content behind a visually-obscured dialog. Present in both modal components; affects every modal interaction in the app.

**[RESOLVED 2026-08-22] Zero automated frontend tests (found 2026-08-15, foundation audit Section 11)** — `package.json` has no Jest, Vitest, Playwright, or Testing Library. No `.test.`/`.spec.` files anywhere in `src/`. All prior Playwright verifications (theme suite, settings-prefs, Operator Dashboard, Processing page) were ephemeral, run from `/tmp` and never committed — this file confirms that explicitly elsewhere in its own history. ESLint catches static issues but nothing runs the app behaviorally. This means bugs found and fixed in those sessions (`useHumanReview.loadApplications` race, `Toggle` `aria-hidden`) have zero regression protection. **UPDATE 2026-08-22**: no longer true — `package.json` has `vitest`/`@testing-library/react`/`@testing-library/jest-dom` (`npm run test` watch, `npm run test:ci` single-pass), and 17 `.test.jsx` files exist across `src/` today (confirmed by direct count, not by memory of an earlier session). Last full-suite count on record: 97 passed across those 17 files (2026-08-20 entry above, Application History work) — not re-run as part of this correction, so treat that count as the last-known, not freshly re-verified today.

**[RESOLVED 2026-08-22] ENVIRONMENT single point of failure (found 2026-08-15, foundation audit Section 7)** — `ENVIRONMENT` defaults to `"development"` (`app/core/config.py:100`). All three production safety mechanisms (DEBUG guard, SECRET_KEY guard, seed-script password guard) depend on it being explicitly set to `"production"` — nothing enforces this or checks it. A deployer who forgets to set `ENVIRONMENT` will silently deploy with `DEBUG=True`, the dev `SECRET_KEY`, and the dev seed password. This was documented nowhere in the repo before this entry — only in the audit transcript that found it. **UPDATE 2026-08-22**: fixed — `environment: Environment = Field(default="production")` (`app/core/config.py:117`), fail-closed by design per its own docstring ("an omitted `ENVIRONMENT` must never silently enable development behaviour"). An omitted value now defaults safe; local dev/CI set it explicitly instead (`.env.example` ships `ENVIRONMENT=development`, CI uses `testing`). Confirmed directly in code, not from memory.

**[RESOLVED] Missing worker-process documentation (found 2026-08-15, foundation audit Section 12)** — `backend/docs/phase15e-reliability.md` documents the dedicated worker process (`python -m app.bulk_queue`) and `bulk_queue_background_drain=False` for production, but README.md's "Running the application" section and "Module documentation" table do not reference this file. A new developer following README's documented path would never discover the worker is a separate process that must be run. **UPDATE 2026-08-22**: no longer true, found already fixed while auditing this file for stale entries (no code/docs change made today, no commit attributed) -- `README.md`'s "Running the application" section (line ~330) states plainly that local dev "requires **two backend processes**, not one" and links `phase15e-reliability.md` for the full worker execution model; its "Module documentation" table also lists `phase15e-reliability.md` ("Phase 15E — bulk queue worker execution model"). Both references this entry said were missing are present.

**2026-08-16: Phase 0 (splitter fix, `b040927`) and Phase 1 start — Bilateral Agreement extractor (`08bec31`) — both committed to `main`, not yet pushed.** Phase 0's watermark/OCR-fallback threshold fix (see the CamScanner entries above) is verified end-to-end on one real file (`TMA Khal Dir Lower .pdf` — splits into multiple distinct types instead of collapsing into one blob) plus the full regression suite (746 passed). Phase 1 added the first real checklist-type field extractor (`BilateralAgreementExtractor`, `document_analysis/extractors.py`), fixed a routing-precedence bug (the splitter's classification must be checked before the generic keyword-based `detect_document_type`, not after — otherwise a real Bilateral Agreement's own "Account Number"/"IBAN" fields misroute it as a fake bank statement), and unregistered `CrossPeriodRule` (rule registry now 49, was 50) after confirming it would otherwise permanently block every real application from reaching `APPROVED` — see the two-classifier gap entry above, which Phase 1 is now actively closing one document type at a time rather than all at once.

**21-real-file splitting verification was started for extra confidence beyond the one file already proven, then deliberately stopped 2026-08-16 at 2/20 files complete to reclaim CPU for Phase 1 work ahead of a demo deadline — this is honestly partial, not completed, coverage.** Files 1-2 (`Conservator Wildlife Peshawar Zoo.pdf`, `DG Sports KP Onboarding Documents.pdf`) both split correctly into 7 documents each, confirming the Phase 0 fix generalizes beyond the original test file. The other 18 files were never run. Phase 0's fix does not depend on this job finishing — it was already independently proven via one real file plus the full regression suite before this verification job was even started. Also notable: neither of these first two real files produced a `BILATERAL_AGREEMENT`, `TRIPARTITE_AGREEMENT`, `ONE_LINK_LETTER`, `SCHEDULE_OF_CHARGES`, or `FORMAL_REQUEST_LETTER` classification at all — every page in both files matched one of only 4 types (`ACCOUNT_MAINTENANCE_CERTIFICATE`, `AUTHORITY_LETTER`, `CNIC_FRONT`, `BUSINESS_REQUIREMENT_DOCUMENT`). Unclear yet whether that's because those two specific organizations' packets genuinely lack those document types, or whether the splitter's title-phrase matching has uncovered variants for them the way it did for Authority Letter (see below) — worth checking against a file that's supposed to have those types before assuming the splitter is fully correct on them.

**2026-08-16, same day: checklist/manifest cover-page false-positive found and fixed (`68d47d6`).** Investigating why the 21-file job's `DG Sports KP Onboarding Documents.pdf` run (above) showed `AUTHORITY_LETTER: 2` — via the new OCR cache, not a raw re-OCR — found the "second copy" is actually a checklist/manifest cover page whose own first table row literally reads "Authority Letter of officer – Signed" (listing every required document by name); that row fell inside the splitter's header zone and strong-matched as if it were the document's own title. The real Authority Letter is a separate, genuine single document later in the file. Fixed generically in `preprocessing/splitter.py`: any page whose header zone starts with "CHECKLIST" is now excluded from all title matching (both the strong per-line match and the weak substring fallback), not just for Authority Letter — the same false-positive risk exists for any of the 9 checklist type names appearing in such a table. `upload/constants.py::MAX_COPIES_BY_DOCUMENT_TYPE` did not need changing; this was never a real multi-copy case. Regression test added and verified (via `git stash`) to genuinely fail without the fix. Full suite: 747 passed. This also plausibly explains the paragraph above's open question about why `TRIPARTITE_AGREEMENT`/`BILATERAL_AGREEMENT`/etc. never appeared in the first 2 files' classification — worth re-checking once there's time, not confirmed yet.

**New reusable dev tool: `backend/scripts/ocr_cache.py`** — OCR is the dominant cost of any real-file work on this CPU-only machine (minutes/page); this caches each split document's OCR'd text on disk at `Confidential Data/.ocr_cache/` (already covered by the existing `/Confidential Data/` gitignore rule, verified with `git check-ignore`), keyed by source file + document type + copy number, so a given (file, type, copy) is only ever OCR'd once. `get_ocr_text(source_file, document_type, copy_number)` is the one path any future real-sample validation should go through instead of an ad-hoc one-off script that throws the OCR'd text away afterward. **Correction found 2026-08-16 late session**: caching is incremental, not all-or-nothing — `_split_and_cache_all` writes each split document's cache file as soon as that document's OCR finishes, not only when the whole source file is done. A large scanned file (e.g. `GDA Abbotabad.pdf`, 41 pages) can run for 90+ minutes, but partial results are readable from `Confidential Data/.ocr_cache/` well before the job completes — `ls`/`grep` the cache directory instead of assuming a still-running job has produced nothing yet.

**2026-08-16 end of day: Phase 0 and Phase 1 (Bilateral Agreement + Authority Letter) both shipped and pushed to `origin/main` (`f7183ed`, 6 commits ahead of the last-pushed `bf39ad2`).** Full combined-range secrets sweep (`git diff bf39ad2..HEAD`) came back clean before pushing — no `Confidential Data/` PII, only fabricated placeholder names and already-public org/file names. Backend suite: **752 passed, 0 failed** (last measured; unchanged since, later work this session was read-only).

- **Live demo, done and real**: application `11189` (`TMA Khal Dir Lower .pdf`, bulk-uploaded through the actual running dev server against the real dev DB) reached `PENDING_REVIEW` automatically once OCR finished — the queue's `PipelineRunnerService` chains analysis → confidence → normalization → rule validation on its own, no manual per-stage calls needed. `overall_status: FAILED`, correctly: 5 of 8 required types are genuinely absent from this real file (`FORMAL_REQUEST_LETTER`, `SCHEDULE_OF_CHARGES`, `ACCOUNT_MAINTENANCE_CERTIFICATE`, `BILATERAL_AGREEMENT`, `TRIPARTITE_AGREEMENT`). `AUTHORITY_LETTER` came back `PARTIALLY_VERIFIED` with real fields extracted live (`focal_person_name`/`focal_person_designation`/`organization_name`), proving today's extractor end to end on real data, not just fixtures.
- **Honest-fallback behavior confirmed on this same real application**: `ONE_LINK_LETTER` and `BUSINESS_REQUIREMENT_DOCUMENT` (recognized checklist types, no extractor yet — `DocumentAnalysisService._persist_recognized_unsupported_type`) and `OTHER_SUPPORTING_DOCUMENT` (genuine catch-all, `_persist_undetermined_type`) all came back `NEEDS_REVIEW` with an honest message, never silently missing and never falsely approved.
- **Bilateral Agreement real-sample validation: CONFIRMED BLOCKER, still open as of 2026-08-17 (second search round).** First round: `GDA Abbotabad.pdf` (41 pages, ~2 hours to OCR) finished: 10 documents split out (`ACCOUNT_MAINTENANCE_CERTIFICATE` ×4, `AUTHORITY_LETTER` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `ONE_LINK_LETTER` ×3, `OTHER_SUPPORTING_DOCUMENT` ×1), zero `BILATERAL_AGREEMENT`, across the first **5 real files checked** (`Conservator Wildlife Peshawar Zoo.pdf` -- split-checked only, not full-text cached: see the 21-file splitting-verification entry above, 7 documents split and classified into 4 types, no `BILATERAL_AGREEMENT` among them; `DG Sports KP Onboarding Documents.pdf`, `TMA Khal Dir Lower .pdf`, `GDC Alpurai Shangla .pdf`, `GDA Abbotabad.pdf` -- all 4 fully OCR-cached end to end). **Second round, same day**: a 3-file split-only checkpoint (title/type classification only, not full OCR-text caching -- cheaper but still a real per-page OCR cost) worked through the untouched-file list in order -- `GDC Madyan Swat.pdf` (9 pages: `ACCOUNT_MAINTENANCE_CERTIFICATE` ×1, `ONE_LINK_LETTER` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1), `GDC Nathiagali Abbottabad.pdf` (10 pages: `AUTHORITY_LETTER` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1), `GGDC No 2 Haripur .pdf` (10 pages: `ACCOUNT_MAINTENANCE_CERTIFICATE` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1) -- again zero `BILATERAL_AGREEMENT`, and notably thin packets (only 3 documents split out per file, vs. 7-10 in the richer files checked earlier). That's 8 real files checked end to end now, zero Bilateral Agreement samples found in any of them. **Third round, next batch same investigation**: a further 3-file split-only checkpoint continued through the untouched-file list in order -- `GPGC Mardan.pdf` (10 pages: `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `ONE_LINK_LETTER` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1), `GPGC no 1 Abbottabad.pdf` (11 pages: `ACCOUNT_MAINTENANCE_CERTIFICATE` ×1, `AUTHORITY_LETTER` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1), `KP Workers Welfare Board.pdf` (23 pages, notably bigger than the rest of this batch: `AUTHORITY_LETTER` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `CNIC_FRONT` ×2, `OTHER_SUPPORTING_DOCUMENT` ×1) -- again zero `BILATERAL_AGREEMENT` in any of the three. **That's 11 real files checked end-to-end-or-split-checked now, zero Bilateral Agreement samples found anywhere.** Stopped deliberately at this checkpoint rather than continuing unprompted -- per-file OCR cost is real (up to ~2 hours for the largest file seen) and this session's demo deadline is time-sensitive, so continuing to search the remaining **10 of 21 real source PDFs** still genuinely unprocessed (`KP-PPRA Onboarding Documents.pdf`, `Onboarding Documents of KP-PSRA.pdf`, `TMA Lal Dir Upper.pdf`, `TMA Lal Qilla Dir Lower.pdf`, `TMA Samarbagh Dir Lower.pdf`, `TMA Thall, Hangu.pdf`, `TMA Thall Agreement.docx`, `UAD Mardan Onboarding File.pdf`, `WSSC Kohat Onboarding File.pdf`, `WSSC Mingora Swat Onboarding file.pdf` -- this list corrects the previous round's vaguer "three more TMA files, two WSSC files, one .docx" grouping, which silently omitted `TMA Thall, Hangu.pdf` and `UAD Mardan Onboarding File.pdf` entirely; the "13 of 21" count itself was arithmetically correct, only the accompanying named list was incomplete) is an explicit call for whoever owns the demo timeline, not something to keep doing automatically. `BilateralAgreementExtractor` (`08bec31`) is built and unit-tested against `docs/Master_Rules_Combined.md`'s spec and synthetic fixtures only; it has never been validated against a real sample, and none of the 11 files on hand contain one.
- **Formal Request Letter real-sample validation: CONFIRMED BLOCKER, found 2026-08-17 -- unlike Bilateral Agreement above, no extractor has been written for this type at all.** Checked *before* writing any code, per the real-sample-validation rule: zero `FORMAL_REQUEST_LETTER` hits anywhere in `Confidential Data/.ocr_cache/` across the 4 fully OCR-cached real sources (`DG Sports KP Onboarding Documents.pdf`, `TMA Khal Dir Lower .pdf`, `GDC Alpurai Shangla .pdf`, `GDA Abbotabad.pdf`); the 5th real source checked so far, `Conservator Wildlife Peshawar Zoo.pdf`, was never full-text cached (split-checked only -- see the Bilateral Agreement entry above), but its own classification list (21-file splitting-verification entry above: 7 documents across 4 types) independently confirms the same zero result for this file too, and `git log --all -S "FORMAL_REQUEST_LETTER" -- backend/app/document_analysis/` confirms no branch, ever, has touched this type in that module. **This is not a fully exhausted search**: `Confidential Data/` holds **21 real source PDFs total, and only these 5 have ever been split or OCR'd this session** -- the other 16 (`GDC Madyan Swat.pdf`, `GDC Nathiagali Abbottabad.pdf`, `GGDC No 2 Haripur .pdf`, `GPGC Mardan.pdf`, `GPGC no 1 Abbottabad.pdf`, `KP Workers Welfare Board.pdf`, `KP-PPRA Onboarding Documents.pdf`, `Onboarding Documents of KP-PSRA.pdf`, three more TMA files, two WSSC files, one `.docx`) have never been processed at all. Treat this as a standing, reportable gap, same tier as the Bilateral Agreement blocker above -- **no `FormalRequestLetterExtractor` should be written until a real sample turns up**, either by OCRing one of the 16 untouched files (real CPU cost, minutes/page on this machine) or a manually-sourced sample. **Correction, 2026-08-18: a real sample has now been found -- see the dedicated entry near the end of this section.** The "zero hits anywhere" premise no longer holds; the "no extractor exists yet" half of this blocker is unchanged.
- **[SUPERSEDED 2026-08-22 -- see the dated decision entry near the end of this section] Schedule of Charges real-sample validation: CONFIRMED BLOCKER, found 2026-08-17 -- same investigation, no extractor written.** Same checks as Formal Request Letter, same result: zero `SCHEDULE_OF_CHARGES` hits in `Confidential Data/.ocr_cache/` across the 4 fully OCR-cached sources (see the same Conservator Wildlife Peshawar Zoo.pdf split-checked-only caveat documented in the Formal Request Letter entry above -- that file's own classification list independently confirms the same zero result), and `git log --all -S "SCHEDULE_OF_CHARGES" -- backend/app/document_analysis/` shows no branch has ever touched this type in that module. This is consistent with, not just asserted alongside, two things already on record above: the live-demo application (`11189`, `TMA Khal Dir Lower .pdf`) listed `SCHEDULE_OF_CHARGES` as one of the "5 of 8 required types genuinely absent from this real file," and the first-two-files splitting job found only 4 types anywhere in `Conservator Wildlife Peshawar Zoo.pdf`/`DG Sports KP Onboarding Documents.pdf` (`ACCOUNT_MAINTENANCE_CERTIFICATE`, `AUTHORITY_LETTER`, `CNIC_FRONT`, `BUSINESS_REQUIREMENT_DOCUMENT`), i.e. not this type either. Same **not a fully exhausted search** caveat as Formal Request Letter -- the same 16 of 21 real source PDFs remain unprocessed. **No `ScheduleOfChargesExtractor` should be written until a real sample turns up.** One forward-looking clue, flagged as speculation, not a finding: `upload/constants.py::MAX_COPIES_BY_DOCUMENT_TYPE` allows up to 6 copies of this type (the highest cap of any required document), which is suggestive of the real documents arriving per-service rather than once per application -- worth keeping in mind when a sample does surface, not something to design around yet.
- **CNIC scope decision made 2026-08-17: build front-only extraction now.** `CnicFrontExtractor` (Phase 1, sixth checklist type) is built and real-sample validated -- see the findings entry directly below for the facts that informed the decision. `CNIC_BACK` stays deliberately unmapped: zero real `CNIC_BACK` samples exist anywhere, so back-face extraction has no real-sample validation and remains out of scope until one turns up; a back upload still falls through to the generic `ID_DOCUMENT` classifier exactly as before. Fields: `document_number` (critical, format-anchored on the canonical `NNNNN-NNNNNNN-N` shape with no label dependency, so it's position-independent -- extracts correctly on all 3 real samples including the one with scrambled OCR read order), `full_name` (non-critical -- reliable across all 3 real samples but validated against only one real organization), `date_of_expiry` (non-critical -- anchored on an exact two-label/two-value block that holds on 2 of 3 real samples, honestly misses on the scrambled one rather than guessing). `document_number` deliberately reuses the same field name `IdentityExtractor` already produces, so `FormatCnicRule` (`rule_engine/rules/format_rules.py`) validates it automatically, no new rule needed. **Real-sample validation result, run directly against all 3 real cached samples**: 2 of 3 (the clean-layout ones, including the one with the unrelated splitter-merge-artifact content trailing after the CNIC portion) come back fully `VERIFIED` (all 3 fields present); the scrambled-order sample comes back `PARTIALLY_VERIFIED` (`document_number`/`full_name` present, `date_of_expiry` honestly missing) -- matching designed behavior exactly, not a surprise. `father_name`/`date_of_birth`/`date_of_issue` were not attempted this pass, scoped to what docs/Master_Rules_Combined.md Section 12 asks for by name (format, expiry, readability, consistency) rather than extracting every field the layout partially allows. The splitter merge-artifact bug noted below is unaffected by this change -- still not investigated, still a real bug worth its own look.
- **CNIC (front/back) findings, gathered 2026-08-17, informed the scope decision directly above.** Before this fix, `CNIC_FRONT`/`CNIC_BACK` were not `AnalyzedDocumentType` members at all (confirmed by grep, zero hits in `document_analysis/constants.py`/`extractors.py`) and were not in `_CHECKLIST_TYPE_MAP` -- no analysis-layer slot, no presence rule, no completeness check existed for CNIC anywhere in the codebase. **Unlike every other unbuilt checklist type, a real CNIC upload did not honestly fall to `NEEDS_REVIEW` -- it got silently misclassified.** Because `CNIC_FRONT` wasn't in `_CHECKLIST_TYPE_MAP`, it fell through to the generic keyword classifier (`detect_document_type`) instead of being intercepted first; unlike Schedule of Charges/Formal Request Letter/1-Link Letter's real content, CNIC's real content does score positively against that classifier's `ID_DOCUMENT` keyword table (`"identity card"` weight 3, `"date of birth"` weight 2). Empirically confirmed by running `detect_document_type`/`extract_fields` directly against all 3 real cached samples before the fix: **all 3 classified as `ID_DOCUMENT` and ran through the generic `IdentityExtractor`**, persisting as `document_type: "ID_DOCUMENT"`, not `"CNIC_FRONT"` -- this is now fixed for front-facing uploads by `CnicFrontExtractor` above; back-facing uploads still hit this exact misclassification, since `CNIC_BACK` deliberately stays unmapped. **Real samples**: 3 real `CNIC_FRONT` samples exist in `Confidential Data/.ocr_cache/`, all from one organization (`DG_Sports_KP_Onboarding_Documents__CNIC_FRONT_copy1/2/3.txt`); **zero `CNIC_BACK` samples exist anywhere in the cache.** Two of the three front samples (~30 lines each) are clean, standard, clearly labeled CNIC fronts. **The third (~1,036 lines / ~40KB) is a genuine splitter merge-artifact**: the first ~29 lines match the same clean CNIC-front structure as the other two, then a page-break marker appears, then ~1,000 lines of unrelated organizational-registration content (organization name/registration number, nature of organization, executive committee members, bank name/branch) is merged into the same document boundary. **Flagging this as a real splitting bug worth its own investigation, independent of the CNIC extraction work above** -- it's structurally similar in *shape* to the checklist/manifest cover-page false positive already fixed in `68d47d6` (unrelated content bleeding across a page boundary into the wrong document), but **the actual root cause was not checked this session** -- don't assume it's the same mechanism until someone reads the raw splitter output around that boundary. `CnicFrontExtractor`'s patterns are anchored on the CNIC-front portion only, so the trailing contamination did not need to be worked around to validate successfully against that sample -- but the bug itself is still unfixed and still worth a look. **Resolved, 2026-08-18: root cause found -- the ~1,000 lines of "unrelated organizational-registration content" are the genuine 1-Link Application Form** (docs/Master_Rules_Combined.md Section 4), silently absorbed as a continuation because its own title never matched any `ONE_LINK_LETTER` phrase. See the splitter fix entry near the end of this section -- fixing that phrase gap also splits this sample cleanly, closing this bug as a side effect.
- **2026-08-17: the "1-Link Letter" checklist slot is not populated by what docs/Master_Rules_Combined.md Section 4 describes -- flagged prominently, needs a product/ops decision, not a code fix.** `DocumentType.ONE_LINK_LETTER` now has a real extractor (`OneLinkLetterExtractor`), but built against what's actually there: **every one of the 4 real cached samples (Confidential Data/.ocr_cache/, 2 independent organizations) is a signed "PARTICIPATION MEMORANDUM FOR BILLER/SUB-BILLERS/BILL AGGREGATOR MEMBERS,"** matching Section 8's "E-Stamp Papers & Notary Public (Participation Memorandum & SLA)" content and Section 6 Tripartite Agreement's three-party/witness shape. **None contain Section 4's actual required content** -- Organization Name/NTN/Country of incorporation/Proprietor-Directors-Partners-with-CNICs/Nature-of-Business classification/license details/Account Match check. The genuine 1-Link Application Form has never turned up in any real file processed this session. **Correction, 2026-08-18: it has now -- see the dedicated entry near the end of this section.** Practical effect: **`ONE_LINK_LETTER` verification today checks Participation Memorandum content (organization name, one non-critical branch-code field), not Section 4's literal requirement** -- real applications will show `VERIFIED`/`PARTIALLY_VERIFIED` against a document that legally may not be what the checklist item is meant to require at all. Root cause found, not fixed: `app/preprocessing/splitter.py`'s `ONE_LINK_LETTER` title-match list mixes the correct strong phrase ("1LINK APPLICATION FORM") with bare fallback keywords ("1LINK", "ONE-LINK", "ONELINK") that also appear constantly in Participation Memorandum boilerplate, plausibly weak-matching it into this slot instead. **This is a scope question for whoever owns the department/checklist relationship, not something this session is resolving in code** -- either the checklist item's real-world meaning needs correcting, or the splitter needs tightening to stop conflating the two documents, or both.
- **2026-08-17: latent bug found in `confidence/services.py` while validating the 1-Link Letter extractor's test suite -- flagged, not fixed, out of scope for Phase 1.** `ConfidenceService.evaluate()`'s completion log line (`services.py:337-346`) formats `overall` with `%.3f`, but `overall` comes from `_overall_confidence(entries)` (`services.py:523`), typed `float | None` and genuinely `None` whenever `entries` is empty -- i.e. an application whose analyzed documents collectively produced zero extracted fields. `str.__mod__`/`logging`'s `%`-formatting raises `TypeError: must be real number, not NoneType` on `None`, which is unhandled, so `POST /confidence/evaluate` 500s instead of returning a valid (if degenerate) response for that application. Found because `test_confidence_api.py::add_scanned_statement` (reused by `test_feedback_api.py`, `test_normalization_api.py`, `test_rule_engine_api.py` -- 20 test failures total, see the git history around this entry's commit) fed `BANK_STATEMENT_TEXT` through a document type (`ONE_LINK_LETTER`) that had just gained a real extractor it doesn't match, producing exactly this zero-fields case; fixed the test fixture (swapped to `DocumentType.SCHEDULE_OF_CHARGES`, still unmapped in `_CHECKLIST_TYPE_MAP`) rather than the production code, since the production bug is real but predates this session's changes and reproducing it doesn't require any document type in particular -- any application whose every document analyzes to zero fields hits it. Needs its own fix (e.g. guard the log call, or have `_overall_confidence` return `0.0` instead of `None`) plus a regression test; not attempted here to keep the 1-Link Letter commit scoped to what it was reviewed for. **Resolved, 2026-08-18 (`e7b475a`), confirmed still passing 2026-08-19**: guarded the log line rather than changing `_overall_confidence` to return `0.0` -- `None` is the deliberate, documented "nothing was scored" contract (`EvaluateResponse.overall_confidence: float | None`, same None-not-zero convention `reports/services.py` independently uses), so collapsing it to `0.0` would misrepresent an unscored application as a zero-confidence one. Regression test `test_evaluate_with_zero_extracted_fields_does_not_500` added (`caplog.set_level(logging.INFO)` is required, not decorative -- pytest's default WARNING capture level otherwise leaves the crash dormant during a normal test run).
- **Process lesson, 2026-08-17: a shared test fixture that uses a `DocumentType` as a generic "no real extractor yet" placeholder breaks every test file that reuses it, not just the extractor's own test file.** This happened three separate times this session, each time only found by actually running the full suite rather than the newly-touched file in isolation: (1) BRD -- `test_reports_api.py`'s `build_full_application` fed `BANK_STATEMENT_TEXT` through `DocumentType.BUSINESS_REQUIREMENT_DOCUMENT` as a stand-in for "any required type with no extractor," broke once BRD got one; (2) 1-Link Letter -- `test_document_analysis_api.py`'s `test_analyze_payslip_from_scanned_image` used `DocumentType.ONE_LINK_LETTER` the same way, single-test blast radius; (3) 1-Link Letter again, same session -- `test_confidence_api.py`'s `add_scanned_statement` used the identical pattern, but this helper is imported and reused by `test_feedback_api.py`, `test_normalization_api.py` and `test_rule_engine_api.py`, so one placeholder-type choice cascaded into 20 failures across four files at once, plus surfaced the `confidence/services.py` latent bug directly above as a side effect. **Recommended practice for any future extractor added to `_CHECKLIST_TYPE_MAP`** (next candidates: Formal Request Letter, Schedule of Charges, CNIC front/back): `grep -rn "DocumentType\.<TYPE>" backend/tests/` before wiring the type in, not just re-running that type's own test file -- a placeholder usage anywhere in `tests/` is a future break, and shared helpers (anything imported by more than one test file) carry a wider blast radius than a local fixture does.
- **Teammate bug triage, corrected 2026-08-17**: a teammate's independent review (32 reported issues) was verified read-only against the current codebase. First-pass tally was 29 confirmed / 3 false; **Critical #1 ("missing auth on 40+ endpoints") was then found to be a false positive on a second pass** — the original grep checked each route file individually for `_CURRENT_USER` and missed that `app/api/__init__.py` already wraps every flagged router in a single `protected_router = APIRouter(dependencies=[Depends(get_current_user)])`, applied globally on 2026-08-14 (see `tests/test_auth_enforcement.py`, which passes 3/3 and empirically confirms all 53 protected endpoints reject unauthenticated requests today). **Corrected tally: 28 confirmed, 4 false, 0 unverifiable.** Full findings and the correction note in `docs/TEAMMATE_BUG_TRIAGE.md`; none of the 28 confirmed items have been fixed yet, pending an explicit scope decision. Lesson for any future automated code-review pass: a per-file grep for a dependency can miss centralized/router-level wiring — check the aggregation point (`app/api/__init__.py` here) before concluding a dependency is absent.
- No `origin/track-a-*`/`origin/track-b-*` branches exist yet (checked repeatedly through the session).
- **Full-stack trust audit, 2026-08-17** — every claim on record above re-derived from the actual repo state (git, both codebases, the contract between them, both test suites, all docs), not assumed from this file. Full findings, all four buckets, in `docs/FULL_STACK_AUDIT_2026-08-17.md`.
- **2026-08-18: `TMA Lal Dir Upper .pdf` fully OCR'd (9 pages, full pipeline) -- confirms the audit's B.9 continuation-absorption limitation on a second real file, and casts doubt on every prior split-only "zero found" search.** Two human-identified page ranges (pages 4-6, pages 8-9) were checked against the actual OCR'd text, not assumed from the page numbers alone. The splitter itself produced only **2** documents from this file's 9 pages: page 1 (a checklist cover page, correctly excluded and typed `OTHER_SUPPORTING_DOCUMENT` by the existing `68d47d6` fix) and a single 8-page blob typed `AUTHORITY_LETTER`. Reading that blob's actual content shows it silently contains **5 distinct real document types**, not one: a genuine Authority Letter (page 2, correctly the one page that triggered the strong title match), a bank-account-details page (page 3), a 3-page 1-Link Application Form (pages 4-6, see below), a Formal Request Letter (page 7, see below), and a Participation Memorandum (pages 8-9, see the Tripartite Agreement entry below) -- none of the latter 7 pages independently triggered a header-zone strong-title match, so each was silently absorbed as a "continuation" of whatever document came before it, exactly the false-negative mechanism the audit (`docs/FULL_STACK_AUDIT_2026-08-17.md`, B.9) identified by reading the splitter's code but had not yet confirmed against a live example. **Methodology caveat, applies retroactively**: every split-only "zero `BILATERAL_AGREEMENT`/`SCHEDULE_OF_CHARGES`/etc. found" conclusion in this file (the 11-file Bilateral Agreement search above, the Formal Request Letter and Schedule of Charges blocker entries) trusted the splitter's own type label as ground truth for "this file doesn't contain that type." This file proves that assumption can be wrong -- a real Formal Request Letter and a real Tripartite-shaped document were both present in a file whose split-only classification would have reported neither. **Not asserting the prior 11-file search's zero results are wrong** -- no re-check was done here, only this one new file -- only that they can no longer be treated as conclusive proof of absence, just as "not independently title-matched." Worth a real re-check with full OCR-text reads (not split-only counts) before treating any of those "zero" conclusions as final.
- **2026-08-18: the genuine 1-Link Application Form (docs/Master_Rules_Combined.md Section 4) has now turned up in a real file -- corrects the "never turned up" claim in the 2026-08-17 entry above.** `TMA Lal Dir Upper .pdf` pages 4-6 (human-identified, confirmed by direct OCR read) are a genuine "Application Form (In-Direct Customer)" package -- Form A (Organization Name, NTN/Registration No., Country of Incorporation, Bank Account Number, Title of Account) on page 4, a continuation with the license-status table, Politically-Exposed-Person/foreign-national declarations, and the Proprietor/Directors/Partners/Trustees table (name, CNIC, nationality, function) on page 5, and Form B (business-model details, transaction volumes, declarations, signature blocks) on page 6 -- matching Section 4's spec field-for-field, the first time this shape has appeared in any real file processed this session (previously 4/4 samples, 2 orgs, were all Participation Memorandum content instead, see above). **The splitter did not independently classify these 3 pages at all** -- see the absorption entry above; they were silently absorbed into the `AUTHORITY_LETTER` blob. **`OneLinkLetterExtractor` run directly against this content extracts nothing** (`organization_name`/`branch_code` both empty -- confirmed empirically, not assumed): its patterns are anchored on Participation Memorandum boilerplate phrasing ("<ORG> hereby authorizes 1LINK to take actions") that does not appear anywhere in the genuine Application Form's text. **This changes the shape of the standing 1-Link Letter scope question**: it is not simply "the splitter always mis-routes the same wrong document into this slot" -- different real onboarding packages contain genuinely different documents for the same checklist item (Participation Memorandum from 2 orgs across 4 samples; the real Application Form from a 3rd org here). Any future fix needs to handle both shapes, or the checklist/splitter needs a way to tell them apart, not just point at one as "the real one." No code changed this step, per instructions -- flagged for a product/ops scope decision, same as the entry above.
- **2026-08-18: Tripartite Agreement's first-ever real-sample validation -- partial match, not fully validated.** `TMA Lal Dir Upper .pdf` pages 8-9 (human-identified as Tripartite Agreement, confirmed by direct OCR read) are the same "PARTICIPATION MEMORANDUM FOR BILLER/SUB-BILLERS/BILL AGGREGATOR MEMBERS" pattern already documented above under the 1-Link Letter mismatch (clause-numbered (i)-(xii), three named parties, witness sections) -- confirming that in practice this document substantively fulfills both checklist slots' real-world content, in different files. **The splitter did not independently classify these 2 pages either** -- absorbed into the same `AUTHORITY_LETTER` blob (see the absorption entry above). `TripartiteAgreementExtractor` (`08bec31`, unit-tested against the spec and synthetic fixtures only until now) run directly against this content: **3 of 6 fields extract correctly** (`party_1link`, `party_kpitb`, `party_subbiller` -- all match the real named parties: 1LINK, Khyber Pakhtunkhwa Information Technology Board, and the sub-biller department). **2 of 6 garbage-capture** -- `account_holder` captures the bank-details table's column header ("IBAN/Account No") instead of the real account title, and `account_number` captures the row index ("01") instead of the real account number; both patterns assume a same-line "Label: value" shape, but the real document's bank details are a multi-column table where headers and values land in different OCR lines -- the same failure class as the already-documented AMC `/IBAN` garbage-capture bug. **1 of 6 honestly misses** -- `branch_code`'s pattern requires a colon-delimited "Branch:"/"Branch Code:" label; the real sample states the branch parenthetically ("Branch (0312)"), the same shape `OneLinkLetterExtractor`'s own `branch_code` pattern was already built to expect elsewhere. **Honest verdict: first real validation, 3/6 correct, 2/6 silently wrong (worth prioritizing over the 1 honest miss, since a garbage value can pass downstream as if it were real data), 1/6 honest miss.** Not rounding this up to "validated" -- the two garbage-capture fields need fixing before this extractor's output can be trusted on real Tripartite Agreement data.
- **2026-08-18: Formal Request Letter's first real sample found -- corrects the "zero hits anywhere" claim in the 2026-08-17 blocker entry above; the "no extractor exists" half was closed the same day, see the dedicated entry directly below.** `TMA Lal Dir Upper .pdf` page 7 (found while reading the absorbed `AUTHORITY_LETTER` blob above, not independently classified by the splitter) is a genuine formal letter to "The Managing Director, KPIT Board" requesting a digital account for online payments -- substantively matches Section 11's requirement (a formal request explicitly asking KPITB to onboard the organization), though its literal subject line ("REQUEST FOR DIGITAL ACCOUNT AND FOR ONLINE PAYMENTS") differs from the master rules' suggested example phrasing ("Onboarding as a sub-biller with KPITB"). This is a real sample now sitting in `Confidential Data/.ocr_cache/` (inside the `AUTHORITY_LETTER_copy1` cache file, not its own).
- **2026-08-18: `FormalRequestLetterExtractor` added (commit `3770e2c`), closing the "no extractor exists" half of the blocker above -- undocumented in this file until now (found 2026-08-22 while auditing for missing entries).** Built against the one real sample above, all 3 fields honestly scoped to what that one sample actually supports: `organization_name` originally captured only the office-holder title on the anchor's own line ("OFFICE OF THE <title>") instead of the real organization name one line below it -- pattern now explicitly discards the anchor's own line and captures the next one; not validated against any other anchor variant (DEPARTMENT OF/GOVERNMENT OF/TO THE/FROM) since none has a real sample yet, so that shape is assumed to generalize, not confirmed. `focal_person_designation` previously bled across a line boundary (a bare "Title" inside the real sample's "Account Title" table header, no colon, satisfied the old permissive gap and crossed into the next line's unrelated "IBAN/Account No" content) -- now requires an explicit `:`/`|`/`-` delimiter on the same line, correctly leaving the field missing on this sample (it never states a designation for its focal person) rather than papering over it. `date` is a known, still-open honest miss on the one real sample: the real text reads "Dated Dir(U) 13/07/2026" (unrelated words between label and date) and separately "Dated: 14-04-2026" referring to a third-party letter being cited, not this letter's own date -- neither fits the strict label-then-date shape, and matching the second would be actively wrong. Only 1 real sample exists for this type total -- same single-organization caveat as every other extractor built against exactly one real file.
- **2026-08-18: splitter fix -- the genuine 1-Link Application Form's own title now correctly starts a document, instead of being silently absorbed.** Root-cause diagnosis of the 3 missed boundaries in `TMA Lal Dir Upper .pdf` (entries above) found one safely fixable case: `_STRONG_TITLE_PHRASES`'s `ONE_LINK_LETTER` entry only listed brand-prefixed variants ("1LINK APPLICATION FORM", "ONE-LINK", "1LINK", etc.), but the real form's own page title is "Application Form (In-Direct Customer)" with no brand prefix at all -- a pure wording mismatch, not a position or missing-entry issue (confirmed at line 0 of its page, well inside the header zone). **Evidence base is n=4, not n=1**: checking this candidate phrase against every already-cached real sample (before applying it) found the same real title, previously unidentified, already hiding inside 3 other cached documents from 2 other files -- see the CNIC merge-artifact resolution above (`DG_Sports…CNIC_FRONT_copy2`), plus `GDA_Abbotabad…ACCOUNT_MAINTENANCE_CERTIFICATE_copy4` and `TMA_Khal_Dir_Lower…AUTHORITY_LETTER_copy2`. Fixed by adding `"APPLICATION FORM (IN-DIRECT CUSTOMER)"` to the phrase list (`app/preprocessing/splitter.py`). **Compatibility-checked before applying, not assumed safe**: truncating each of the 3 already-cached samples at the new boundary and re-running their current extractor showed zero regression for `CnicFrontExtractor` and `AuthorityLetterExtractor` (identical fields and values). **One real, accepted regression**: `AccountMaintenanceCertificateExtractor` on `GDA_Abbotabad…ACCOUNT_MAINTENANCE_CERTIFICATE_copy4` loses `branch_name` -- that field's value was being pulled from the absorbed Application Form content, not the genuine AMC portion, so losing it is a correction, not a loss of real data; `branch_name` is not in AMC's `CRITICAL_FIELDS`, so this downgrades that one sample from fully-`VERIFIED` to missing one non-critical field, not a new blocker. **Correction, same day: the "3 separate copies" side effect noted here was itself wrong, and has been fixed.** It assumed the form is always exactly 3 pages (true for 3 of the then-known samples, but a 4th -- `GDA_Abbotabad…ACCOUNT_MAINTENANCE_CERTIFICATE_copy4` -- turned out to be 4 pages), and it produced a real regression: `TMA Khal Dir Lower .pdf`'s 3 form pages plus 1 pre-existing genuine `ONE_LINK_LETTER` sample totaled 4, exceeding `MAX_COPIES_BY_DOCUMENT_TYPE`'s cap of 3 and hard-failing that file's entire bulk upload. Fixed via a new `_CONTINUATION_TITLE_PHRASES` mechanism in `splitter.py`: a repeat of this exact phrase on the immediately following strong-matched page now extends the current document instead of starting a new copy -- page-count-agnostic by design (confirmed against both the 3-page and 4-page real shapes), and deliberately scoped to only this phrase, so the brand-prefixed `"1LINK APPLICATION FORM"` phrase's existing repeated-copies behavior (genuinely separate real forms, per this constant's own documented intent) is untouched. Regression tests updated accordingly (`tests/preprocessing/test_document_splitter.py`): the 3-page and 4-page cases both now assert exactly 1 document, plus a new test confirming the group still ends cleanly before a genuinely different document. **The other 2 of tonight's 3 missed boundaries were left unfixed, genuinely inconclusive**: Formal Request Letter's real subject line is idiosyncratic to one sample (n=1, no basis to generalize a phrase); the Participation Memorandum/Tripartite boundary's only candidate phrase ("PARTICIPATION MEMORANDUM") was confirmed, not assumed, to collide with 3 of the 4 already-working real `ONE_LINK_LETTER` samples (it would out-rank their current matching phrase in table order), and its position also independently fails the header-zone cutoff on this file regardless. Regression tests added (`tests/preprocessing/test_document_splitter.py`, synthetic fixtures, no real PII) covering both the new case and the documented 3-copies side effect. Full suite: 767 passed, 13 failed -- same 13 named failures as the established baseline, zero regressions.
- **2026-08-18: Bilateral Agreement / Tripartite Agreement splitting -- confirmed a wrong-mechanism problem, not a missing-phrase one. Stop chasing this with text-phrase changes.** Investigated whether the same phrase-table approach that fixed the 1-Link Application Form (above) could also detect these two types, before writing any code. Re-read `docs/Master_Rules_Combined.md` Sections 4, 6, 7, 8 directly rather than inferring from one real file: Section 8, "E-Stamp Papers & Notary Public," is explicitly titled *"(Participation Memorandum & SLA)"* -- naming Participation Memorandum and SLA (Bilateral Agreement, per Section 7's own title) as the two types its requirements govern. Its actual authenticating signal is **Visual Authentication** (the print must resemble genuine E-stamp paper texture/color, not plain A4) and **Watermark** validity -- both physical/visual properties, plus Notary Public stamp/seal/photo requirements, also visual. **Confirmed empirically, not just from the spec text**: the literal phrases "TRIPARTITE AGREEMENT"/"BILATERAL AGREEMENT" never appear in any real cached sample (already known); the more specific stamp-paper boilerplate text ("PRINT STAMP PAPER - VENDOR", "NON-JUDICIAL") does leave *some* OCR trace in 3 real files, but repeats across **6 separately-timestamped pages** in one real file (`DG Sports…`), almost certainly 6 separate verification-portal screenshots (consistent with the checklist's own "3 Sets" stamp-paper requirement for both this type and Schedule of Charges) rather than one page per logical agreement -- using it as a splitter boundary would risk the same over-fragmentation problem just fixed for the Application Form, likely worse, on top of visibly worse OCR quality on these specific pages. **Conclusion: even a perfect text-based splitter could only ever route this content to the right slot -- it could never itself satisfy Section 8's actual requirement, since that requirement is defined as a visual property no OCR text pipeline can verify.** No further attempts to fix this by adding or adjusting `_STRONG_TITLE_PHRASES` entries should be made -- real detection needs a different mechanism, closer to Phase 2's visual verification work (image-level stamp-paper color/texture/watermark detection), not a text phrase. Left undone, deliberately, as a scope boundary rather than an open bug to keep chasing.
- **2026-08-18: `DGAM_Onboarding_file.pdf` investigated -- a new file, not part of the original 21-file inventory above.** 17 pages. Split-only classification pass (cheap, no full-text caching): **3 documents** -- `ACCOUNT_MAINTENANCE_CERTIFICATE` x1, `AUTHORITY_LETTER` x1, `OTHER_SUPPORTING_DOCUMENT` x1. No `BILATERAL_AGREEMENT`/`SCHEDULE_OF_CHARGES`/`FORMAL_REQUEST_LETTER`/`TRIPARTITE_AGREEMENT`/`ONE_LINK_LETTER` label present. **Given the absorption-bug finding confirmed the same day on `TMA Lal Dir Upper .pdf` (above) -- where 8 of 9 pages containing 5 distinct real document types were compressed into a single label -- this "zero blocked types" result cannot be treated as reliable evidence of absence, only as "the splitter didn't independently title-match any of them."** 17 pages into 3 buckets is an even higher compression ratio than TMA Lal Dir Upper's. A full OCR-cache read (particularly of the `OTHER_SUPPORTING_DOCUMENT` bucket, the most likely dumping ground for anything that didn't strong-title-match) would be needed to actually confirm or rule out hidden content -- **deliberately not run yet, stopping here to report rather than spending that OCR cost unprompted.** Separately, whether this file's manual number redaction shows up as blank/garbled OCR (relevant to whether it's useful as an extraction sample vs. a structure-only reference) also could not be checked without that same full OCR pass -- also unanswered, pending a go-ahead.
- **2026-08-18, frontend session: role-specific UI shipped for Employee/Operator/Reviewer/IT, no backend changes. Frontend suite: 64 tests, 11 files, all green (`npm test`); `npm run build` clean; `npm run lint` 0 errors (4 pre-existing fast-refresh warnings).** Full role matrix (per user): Employee = everything; Operator = Applications, Processing, Validation, Settings; Reviewer = Applications, Processing, Validation Report, Human Review, Settings; IT = Applications, Processing, Settings + System Logs. Dashboard Quick Actions are role-aware; Settings admin section (Feedback/Continuous Learning/System Logs) shows for Employee-or-IT only; System Logs stays link-visible for Employee but the page itself returns the backend 403 / Access Denied (IT-only endpoint, authoritative). Implementation notes: `roles.js` adds `isEmployee()` (raw role not in {OPERATOR,REVIEWER,IT}) and `visibleNavigationFor(user)` (deep-filtered NAVIGATION copy); `navigation.js` nav items now carry a `roles` array (validation→OPERATOR, reports/human-review→REVIEWER, admin children→IT); `Sidebar.jsx` renders the filtered nav. Validation page gained `ValidationQueueSummary` counter cards (Pending/Missing documents/Needs attention/Rejected/Completed, exactly-one-bucket priority order) and the detail panel now separates a received-checklist + missing-checklist with an "All required documents received." complete state. Validation Report terminology de-technicalized (no OCR/extraction/confidence jargon: "Document quality", "Fields & Standardization", "Field Review Progress", "Needs attention" column) plus an `[Open Human Review]` button (hidden for OPERATOR) that deep-links `?application=<id>` which `useHumanReview` now pre-selects. System Logs gained a free-text event-type filter, prev→next Status column, expandable JSON details rows, and offset pagination (`useSystemLogs` now pages at 50/entry via `{offset,limit}`). `apiError.js` got a 403 fallback message. Test files: new `roles.test.js`, `QuickActions.test.jsx`, `ValidationQueueSummary.test.jsx`; updated `SystemLogsPage/ValidationPage/SettingsPage` tests. Known: seeded Employee account (`Verification Officer`) clicking IT-only endpoints 403s by design (Access Denied page) -- documented limitation, no backend change made. Nothing committed (user to review).

- **2026-08-18, later same day, dashboard role-specific session: Quick Actions and welcome description are now genuinely role-specific; frontend suite 69 tests, 12 files, all green (`npm test`); `npm run build` clean; `npm run lint` 0 errors (same 4 pre-existing fast-refresh warnings).** Per user's per-role spec: **Employee** sees all 6 actions (New Application, View Applications, Open Validation, Open Validation Report, Open Human Review, System Logs); **Operator** sees New Application, View Applications, Open Validation, Processing; **Reviewer** sees View Applications, Open Validation Report, Open Human Review, Processing — **no longer the previous bug of seeing "New Application"** (Reviewer can't create apps); **IT** sees View Applications, Processing, System Logs. Each action has a role-specific description (e.g. Employee View Applications = "Browse and manage existing applications." vs Reviewer = "Browse applications assigned for verification."), and the Dashboard welcome subtitle is now role-specific (heading "Welcome back!" unchanged; Employee = "Here's an overview of your financial document verification workspace.", Operator = "Manage document intake, completeness checks, and application processing.", Reviewer = "Review validated applications and make final verification decisions.", IT = "Monitor application processing and system activity."). Implementation: single source of truth `getDashboardActions(user)`/`getDashboardWelcome(user)` in `src/data/dashboard.js` (`DASHBOARD_ACTIONS`/`DASHBOARD_WELCOME` maps, Employee precedence, OPERATOR fallback — QuickActions renders one config, no per-role JSX duplication); `QuickActions.jsx` and `Dashboard.jsx` both slimmed to consume the config; `RecentApplications` empty-state "Create New Application" button now gated to Employee/Operator only. New tests: `src/__tests__/components/dashboard/QuickActions.test.jsx` (full 4-role matrix + no-role OPERATOR fallback; moved from the old `__tests__/components/QuickActions.test.jsx`, which was removed) and `src/__tests__/pages/Dashboard.test.jsx` (welcome text per role + shared heading; child sections mocked). **Also added `ocr_cache/` and `.ocr_cache/` to `.gitignore`** — `docs/ocr_cache/` was holding untracked real OCR'd document text files (same confidentiality class as `Confidential Data/`); `Confidential Data/.ocr_cache` was already covered by the `/Confidential Data/` rule, now explicit too. Nothing committed (user to review).

- **2026-08-18, late session: Tripartite + AMC garbage-capture bugs fixed with a structural bank-account-block parser (`backend/app/document_analysis/extractors.py`).** The two extractors' `account_holder`/`account_number`/`iban` values came from label-anchored greedy regexes that produced confirmed garbage on the real cache: Tripartite (`TMA_Lal_Dir_Upper__ONE_LINK_LETTER_copy2.txt`, the stacked column table) captured the header `IBAN/Account No` as account_holder and the row index `01` as account_number; AMC copy1 (Allied) captured `/IBAN` as account_number (the documented `/IBAN` bug); AMC copy2 (ZTBL) leaked the page-2 NRSP IBAN-shaped value into account_number because `ACCOUNT NUMBER:...` dotted leaders broke the regex separator; AMC copy3 (NBP) truncated the wrapped 3-line account title. **The fix is a shared structural parser, not more regexes**: `_extract_bank_account_block(text)` runs two passes — a stacked column-block pass (header block positionally mapped onto the value block by *recognized* header, skipping OCR-noise header lines like the real `IENT` column, with the account-number slot validated as structurally real before the block is accepted) and an interleaved label/value pass (same-line `Label: value`, dotted-leader `Label:... value`, and bare-label-then-next-line values with wrapped multi-line titles capped at 3 lines and stopped before the next field's label). Both passes emit `(key, value, line_index)` captures merged per field by earliest occurrence — so first-match-in-document-order makes the ZTBL page-1 account number win over the page-2 NRSP value. Normalization is shape-guarded, not blacklist-guarded: all-digit values of length ≤ 3 (row indexes 01/02/03...) are rejected, values that are themselves known field labels are rejected, combined `account/IBAN` values are split, parenthetical IBAN tails are stripped (bonus: AMC copy4/BOK now yields `iban` from `3005642921(PK06…)`, previously not extractable), and Tripartite's IBAN-only account slot is promoted to `account_number` only when no plain account number exists anywhere in the document. `AccountMaintenanceCertificateExtractor` and `TripartiteAgreementExtractor` now override `extract()` to merge the block; both keep their other patterns (bank_name/branch_name/issue_date, party_* / branch_code) untouched, and the Tripartite merge deliberately excludes `iban` (not in its `EXPECTED_FIELDS`). **Verified AFTER against the same real cache (structural, values masked in this file):** Tripartite now extracts the real account title and the `PK44…` IBAN-shaped account value (was header/row-index garbage); all 4 AMC copies extract the correct account_holder (incl. the full wrapped NBP title) and account_number, with no `/IBAN`, header, row-index or page-2 leaks. **7 new synthetic (fabricated, `PK99FAKE…`) regression tests** in `test_document_analysis_engine.py` cover: column-block positional mapping + noise header, dotted leaders, combined account/IBAN split, wrapped multi-line title + plain-account-number-vs-IBAN disambiguation, page-1-first-match semantics, row-index guard, and honest-miss (absent) fields; 6 of 7 verified (via `git stash`) to genuinely fail on the pre-fix code. A real `PK44…`/`30901040400584`-style value caught in an early docstring was scrubbed to structural placeholders before anything was left in the diff — diff is PII-clean. **Test runs** (all against the isolated `finance_verification_test` DB — note: the current `tests/conftest.py` already redirects `DATABASE_URL` to `*_test` before any app import, so the old CONTEXT.md pytest-wipes-dev-DB warning does NOT apply to the current conftest): `test_document_analysis_engine.py` **66 passed** (59 pre-existing + 7 new); `test_document_analysis_api.py` 76 passed + **13 failed — the same documented `TECH_BLANK_PAGE` baseline** (`fcc9fda`'s `is_blank_image()` flagging the shared image fixture, `assert 'FAILED' == 'ANALYZED'`, confirmed identical, extractor change cannot influence technical validation); downstream consumers `test_rule_engine_api`/`test_confidence_api`/`test_normalization_api`/`test_feedback_api`/`test_reports_api`/`test_completeness_api` **98 passed**. Remaining test files reference the two `DocumentType` enums only for routing/constants — zero direct `extract_fields` calls, so unaffected (verified by grep). Nothing committed (user to review).

- **2026-08-18, same-session follow-up: the Employee account now has genuine all-access ("Employee = everything") — a real backend role, not just a frontend view.** The seeded `Verification Officer` account was previously mapped to REVIEWER, so the Validation page showed it "view-only access to this queue" (`ValidationPage` gated `canOperate` on `isOperator`), System Logs 403'd it (backend `require_role(ROLE_IT)`), and the frontend treated *any* non-canonical stored role as a fake "Employee superuser" that the backend would actually 403. Now: `ROLE_EMPLOYEE = "EMPLOYEE"` is a canonical role in `app/auth/roles.py` (`ROLE_GROUPS["Verification Officer"] = ROLE_EMPLOYEE`), and `require_role` in `app/auth/dependencies.py` grants EMPLOYEE every guard by design (single-point superuser check) — so the Employee account is backend-authorized for operator (request documents/reject/submit), reviewer (human review, validation report) and IT (system logs) endpoints. Frontend mirrors it: `frontend/src/utils/roles.js` adds `ROLE_EMPLOYEE`, maps "Verification Officer" → EMPLOYEE, and redefines `isEmployee` from "any non-canonical stored role" to `effectiveRole(user?.role) === ROLE_EMPLOYEE` — an unknown role like "SUPERVISOR" now gets the honest ungated-only nav (dashboard/applications/processing/settings) instead of a fake full view; `ValidationPage.canOperate = isOperator(user) || isEmployee(user)` and `SystemLogsPage.allowed = isIt(user) || isEmployee(user)` open those pages to the Employee account. Deliberately **not** making `isOperator` return true for Employee globally, so `ValidationReportPage.canOpenHumanReview = !isOperator(user)` keeps showing the button to Employee. **Tests**: backend `test_roles.py` updated (four canonical roles, mapping, `require_role` allows Employee for every guard + still 403s a plain REVIEWER on the IT guard) + new `test_employee_account_can_run_operator_actions` in `test_operator_workflow_api.py` (the "Verification Officer" account actually executes a request-documents action end-to-end); frontend `roles.test.js`/`ValidationPage.test.jsx`/`SystemLogsPage.test.jsx`/`Phase1Routes.test.jsx` updated/added (Employee sees operator actions + no view-only note, Employee views system logs, SUPERVISOR restricted). Run results: backend `test_roles.py`+`test_operator_workflow_api.py` 24 passed; `test_system_logs_api`/`test_auth_api`/`test_auth_enforcement`/`test_validation_tasks_api`/`test_human_verification_api`/`test_validation_workflow` 80 passed; frontend full suite 70 passed (12 files); `npm run build` + `npm run lint` clean. The 403-role tests still use canonical `reviewer_client`/`operator_client` so they keep verifying role separation. Also `.gitignore` now covers `worker.heartbeat` (bulk-queue worker liveness file) alongside `ocr_cache`/`.ocr_cache`. Committed with the extractor fix.

- **2026-08-19: `GDA_Abbotabad`'s `ONE_LINK_LETTER` group -- real finding, not a copy-count issue.** Read all 4 cached `GDA_Abbotabad__ONE_LINK_LETTER_copy{1..4}.txt` files in full (1,602 lines total), not just counted. Only `copy1`'s first ~200 lines are genuine 1-Link Application Form content (Form A/1B/Form B). Everything else across all 4 files -- the remaining ~1,000 lines of `copy1`, and the entirety of `copy2`/`copy3`/`copy4` -- is a completely unrelated **Bilateral Agreement/SLA** between KPITB and GDA (17 numbered clauses, full signature blocks) followed by **three separate stamp-paper counterparts of a Participation Memorandum**, each triggering a new "copy" boundary at its own E-stamp/watermark page rather than being recognized as a continuation of the document already open. **This directly confirms, on a real file, the over-fragmentation risk the Bilateral/Tripartite scope-boundary entry above already predicted** ("the more specific stamp-paper boilerplate text... does leave *some* OCR trace... using it as a splitter boundary would risk the same over-fragmentation problem") -- not a new failure mode, the predicted one actually happening. The continuation-grouping fix (`8231d86`) is not misbehaving on its own designed case (an exact repeated strong-title phrase); these boundaries are triggered by something else entirely, which that fix was never meant to catch. `upload/services.py::_ensure_slot_available` would hard-reject a 4th copy against the cap of 3 on a real bulk upload of this file -- but the more important fact is none of the 4 slots correctly represent a real `ONE_LINK_LETTER` boundary at all. Not fixed here -- same "wrong mechanism, needs visual/structural detection, not more text phrases" conclusion as the entry above. **Department decision, 2026-08-19**: the hard-cap rejection this causes is being addressed separately (see the flexible-copy-limit entry below) -- that change stops the crash, it does not touch this absorption bug, which remains open.
- **2026-08-19: `GDC Alpurai Shangla .pdf`'s real split result -- 6 real document types collapsed into one undifferentiated blob, worse than TMA Lal Dir Upper's 5-in-1.** Page-by-page read of all 9 pages (confirmed count, not an earlier stale "15") against the master rules checklist vocabulary. The splitter produced **exactly 1 document** -- a single `OTHER_SUPPORTING_DOCUMENT copy 1` (554 lines) -- but that one label hides: the checklist cover page (page 1, correctly excluded by `68d47d6`), a genuine Account Maintenance Certificate (page 2), a genuine 1-Link Application Form across 3 pages (pages 3-5), a genuine Formal Request Letter (page 6, subject "ON-BOARDING AS A SUB-SELLER WITH KPITB" -- a third real subject-line variant alongside TMA Lal Dir Upper's), an unmapped "Business Certificate" (page 7, headed "ANN-07", doesn't cleanly match any named checklist item -- a new, unresolved ambiguity), and Participation Memorandum/Tripartite content across the final 2 pages (partially unfilled template placeholders mixed with real filled-in fields, a mixed-fill shape not seen elsewhere). **The Authority Letter's absence is confirmed genuine, not part of this absorption bug**: no Authority Letter content exists anywhere in the 9 pages, and the file's own page-1 checklist table corroborates this independently -- every other required item's row is marked "yes"; only the Authority Letter row lacks that mark, meaning the source document itself records it was never submitted. Not fixed -- same absorption mechanism as the entries above, just total rather than partial.

**2026-08-22: root cause of this file's Application Form boundary miss found and fixed (narrow scope, not the general absorption mechanism) -- confirmed by a real end-to-end re-run of `DocumentSplitter.split_bulk_pdf` against the actual source PDF with live PaddleOCR, not from cache.** The exact-string phrase that closed this same boundary on 4 other real files (`TMA_Lal_Dir_Upper`, `TMA_Khal_Dir_Lower`, `DG_Sports_KP_Onboarding_Documents`, `GDA_Abbotabad`) never matched here because this file's own OCR output reads `"Application Form (In-Direct Customex)"` -- a one-character misread of "Customer)". Fixed by truncating `_STRONG_TITLE_PHRASES`'s `ONE_LINK_LETTER` entry (and `_CONTINUATION_TITLE_PHRASES`, which must match it exactly) to the prefix `"APPLICATION FORM (IN-DIRECT"`, dropping only the final word -- absorbs any future misread of that one word instead of hardcoding a fix for this specific typo. Verified zero false-positive risk by grepping the shorter prefix against the entire real OCR cache: every occurrence, in every file, is this same genuine title. **Real-file result, live re-run, not cache-inferred**: before, 1 document (9 pages, the full blob described above); after, **2 documents** -- `OTHER_SUPPORTING_DOCUMENT` (3 pages) and `ONE_LINK_LETTER` (6 pages), zero absorption warnings. **Honest, not a full fix for this file**: the Application Form boundary itself now splits correctly, but `ONE_LINK_LETTER`'s group grew to 6 pages, not the 3 the genuine form actually is -- the Formal Request Letter, the unmapped "Ann-07" Business Certificate, and the Participation Memorandum content are still being absorbed as continuation pages after the form opens that group (consistent with the already-known, deliberately-deferred gaps on those specific boundaries for this file). **New finding, not previously known, left unfixed and out of scope for this fix**: the 3-page `OTHER_SUPPORTING_DOCUMENT` group shows the checklist cover page and the genuine Account Maintenance Certificate page still didn't independently split either -- this file's real OCR text for that AMC page apparently doesn't cleanly strong-match `"ACCOUNT MAINTENANCE CERTIFICATE"` in the header zone (the one line that does mention it, `"Account Maintenance Certificate from concerned..."`, reads as checklist-table body text referencing the requirement, not the certificate's own title). Not investigated further this session -- a different boundary than the one this fix targeted.
- **[RESOLVED 2026-08-21] 2026-08-19: Zarghuna's bank-account-block structural parser (`origin/feature/zarghuna-bulk-queue`, `16902bd`) -- Tripartite fix initially confirmed correct by direct execution (though a subsequent commit `2039bcd` introduced a `party_1link` regression, which is now fully resolved — see 2026-08-21 entry below); AMC fix initially confirmed NOT to close its own headline bug on a real sample.** Both evaluated with equal rigor: extracted via `git show` (no merge), run directly against real cached text, not just her own docstrings/synthetic fixtures. **Tripartite**: run against `TMA_Lal_Dir_Upper`'s real Participation Memorandum text (pages 8-9) -- all 5 critical fields (`party_1link`, `party_kpitb`, `party_subbiller`, `account_holder`, `account_number`) extract correctly, matching this file's own documented real values exactly (see the Tripartite entry above). Confirmed by execution, clean fix. **AMC**: run against `DG_Sports_KP_Onboarding_Documents__ACCOUNT_MAINTENANCE_CERTIFICATE_copy1.txt` -- the exact file her fix's docstring targets ("all three critical fields empty") -- `_extract_bank_account_block(text)` returns `{}`; the full extractor's critical fields stay empty. **Root cause**: her label matcher anchors same-line value extraction via `.match()`, but this file's real labels carry a parenthetical descriptor suffix her code doesn't strip (e.g. `"Account No. (T-24 System)"`) -- the same-line normalization fails against that suffix and the code never falls through to the next line, where the real value sits. This file also has a 5th real shape not present in any of her 7 new fixtures: three parallel account "generations" (Old/T-24/New-Islamic), each its own Account No./IBAN No. label pair, with `account_holder` stated only in prose ("Peshawar Sports Complex"), never as a labeled field at all. Her 7 new tests (`tests/test_document_analysis_engine.py`) remain formally unrun by pytest -- every attempt in an isolated worktree hits `sqlalchemy.exc.OperationalError: database "finance_verification_test" does not exist`, a missing test-database provisioning gap, not a code defect; not fixed here per the standing rule against touching DB state not understood. Not yet communicated to Zarghuna directly at the time this was found -- since raised with her separately; not this session's to touch (see her PR, still pending on her side). **UPDATE 2026-08-21: CONFIRMED FIXED.** The extractor now correctly handles this real data, extracting the T-24 System account block (`account_holder`/`account_number`/`iban`) successfully. This open decision/gap is fully closed.

- **2026-08-19: AMC 5th-layout gaps fixed (`backend/app/document_analysis/extractors.py`) — the reviewer-confirmed DG_Sports cases: parenthetical system-qualified labels ("Account No. (T-24 System)"), three parallel account "generations" (T-24/CBS/Core), and an account holder stated only in prose. Not committed (user to review).** The DG_Sports AMC OCR text is not on this machine (`docs/ocr_cache/` holds only GDA/TMA files; `Confidential Data/` absent), so the fix was built from the reviewer's structural description with synthetic fixtures (all fake values) and is flagged for real-cache re-verification on the reviewer's copy. Four changes: (1) `_match_label` now strips a parenthetical qualifier that is the *first token* after a field label (`_QUALIFIER_RE`, anchored `\s*\([^()]*\)` so `Account No.: (PK06…)` and `3005642921 (PK06…)` stay values) — the value on the same line after a separator or on the next line is then captured; (2) `_interleaved_scan` now falls through to the bare-label next-line path when an inline value normalizes to nothing, instead of silently dropping the field; (3) `_extract_holder_from_sentence` (new, used only when no labeled holder exists) recovers the holder from ownership-anchored prose ("account of …", "account titled …", "in the name of …", "maintained by …"), with the capture char class case-scoped via `(?-i:…)` so prose words can't leak in, plus N/A/placeholder and lone-parenthetical guards in `_normalize_account_holder`; (4) `_normalize_account_number` tightened — the lenient branch no longer accepts spaces (prose tails were getting captured), and a last-resort run-token scan extracts a structurally valid IBAN/digit token from a prose-laden remainder. Two real-data results confirmed against the local cache (values masked): the previously-dropped `TMA_Khal_Dir_Lower__ONE_LINK_LETTER_copy3` "account number (1BAN) PK… titled as Tehsil General Account" line now yields the clean IBAN token (pre-change: absent; interim bug caught in-session: whole prose sentence as account_number), and all 4 GDA AMC copies + the Tripartite sample extract identically to before. **Also caught and fixed in-session: a column-block misdetection** — a plain label/value layout whose value line looked like a caption header was being read as a stacked table (landing an all-digit value in the account_holder slot and the IBAN line in the account slot); `_extract_column_block` now rejects a block unless *every* recognized value slot normalizes to something real. **Tests**: 6 new synthetic cases in `test_document_analysis_engine.py` (parenthetical label value-on-next-line, parenthetical label inline-after-colon, parenthetical IBAN not eaten, first-of-parallel-generations, sentence-only holder + no false positive on "account is maintained with us", OCR-noise prose tail → clean token) — **71 passed**; `test_document_analysis_api.py` still exactly the documented 13 `TECH_BLANK_PAGE` / `assert 'FAILED' == 'ANALYZED'` baseline, zero new failures. **Also resolved: the "finance_verification_test DB provisioning" concern is a non-issue** — `tests/conftest.py` already auto-creates the `*_test` database if missing (`_create_test_database_if_missing`, requires CREATEDB on the role) and applies `alembic upgrade head` before the suite (CI's 815-test run proves it works in CI too); it only raises clear instructions if the role lacks CREATEDB. Nothing committed.

- **2026-08-20: System Logs UI replaced with two IT-only business pages — Application History (`/application-history`) + Performance (`/performance`) — backend + frontend + tests, not committed (user to review).** Scope per user decision: remove System Logs from the frontend UI only (sidebar, dashboard quick actions, routes, Settings); the backend `system_logs` module, the `audit_logs` table, every audit write site and `test_system_logs_api.py` are untouched (the audit infrastructure stays; only its one UI surface is gone). Backend work on branch `feature/zarghuna-bulk-queue`:
  - **Event-semantics verification first, before writing code**: `applications.submitted_at` = creation time; `DOCUMENTS_REQUESTED` is written by `operator_workflow.services.request_documents` (has created_at/reason/missing_document_types/actor); **`upload.services.py::_record_documents_received` only writes `DOCUMENTS_RECEIVED` when status is `NEEDS_DOCUMENTS` at call time** — the bulk path flips to `PROCESSING` first (lines 277-280) so bulk uploads on `NEEDS_DOCUMENTS` apps record no received event; the single-upload path records it (line 184) before any status change. Final decisions live on `human_reviews` (reviewer_name/reviewed_at/decision/comments/rejection_reason), NOT on validation_history (`REVIEW_*` ValidationEventType values exist in the enum but are never written). `APPLICATION_PIPELINE` job `completed_at` marks when `PENDING_REVIEW` was set.
  - **New `backend/app/application_history/`**: `GET /applications/history` (list: metadata + most-recent-workflow-event per app via a bulk 3-query latest-event pick across history/reviews/documents — no N+1; query search on id/name/creator; status filter; offset/limit) and `GET /applications/{application_id}/timeline` (full chronological lifecycle: creation, uploads, workflow events, pipeline completion, review decisions — business-facing labels/detail assembled server-side, never raw JSON/PII). IT-only (`require_role(ROLE_IT)`, Employee inherits), 404 for unknown application.
  - **New `backend/app/performance/`**: `GET /performance/overview` (totals, status counts, avg waiting/processing/review/turnaround — averages computed only over applications that actually have the metric, so all-in-flight sets report `null` not a misleading "0 days"; total resubmissions + missing-document cycles) and `GET /performance/applications` (per-app `waiting_seconds`/`processing_seconds`/`review_seconds`/`total_turnaround_seconds` + `resubmissions` + `missing_document_cycles` + **evidence `TimeSpan`s behind every number**: label/start/end/duration_seconds/open/detail). **Timing model (user-mandated)**: waiting = each `DOCUMENTS_REQUESTED` paired with the next `DOCUMENTS_RECEIVED`; an unmatched request is an **open span** (start → now, `open=True`, `end`/`duration_seconds` null) — shown as evidence but never counted; internal processing = each queue job's started_at→completed_at (→now while PROCESSING); review = pipeline completion → latest review decision (→now while PENDING_REVIEW); total turnaround = submitted_at → latest decision (→now in flight). IT-only.
  - **Repository additions**: `queue_job_repository.list_all_by_application()` (all job types incl. APPLICATION_PIPELINE); `application_repository.search()` (query on id/name/created_by + status filter + pagination) — return annotation is `tuple[Sequence[Application], int]`, because the class's existing `list` method shadows the builtin `list` in class-scope annotations (`tuple[list[...]]` raised `TypeError: 'function' object is not subscriptable`).
  - **Route ordering is load-bearing**: `application_history_router` is registered in `api/__init__.py` BEFORE `upload_router` so the literal `/applications/history` path wins over `/applications/{application_id}`. OpenAPI verified: 72 paths incl. all four new endpoints.
  - **Tests**: `tests/test_application_history_api.py` + `tests/test_performance_api.py` (role guards incl. Employee-inherits and non-IT 403, empty overview nulls, averages math, per-app evidence spans, open unmatched-request span, search/pagination, timeline merge+ordering, 404). **53 passed** for the two new files + `test_system_logs_api` + `test_operator_workflow_api` + `test_auth_enforcement` (30 regression + 23 new). Fixed mid-test: routes `application.application_id` → `application.id` (ORM object, no alias); `test_history_search_by_creator` sets `created_by` via repository because the API derives the actor from `current_user.name` and ignores any payload. The `PydanticJsonSchemaWarning "Default value Ellipsis is not JSON serializable"` is pre-existing (verified via `git stash` — present without the changes).
- **Frontend (same session)**: deleted `pages/SystemLogs/` (page + CSS), `hooks/useSystemLogs.js`, `services/systemLogs.js`, `SystemLogsPage.test.jsx`; removed the `settings/system-logs` route/filter and the System Logs admin link from Settings; replaced the dashboard `system-logs` quick action with `application-history` + `performance` (Employee and IT sets); added IT-only top-level sidebar items in the System section (`History`/`BarChart3` icons). Two new pages built entirely from the real backend data above, business-facing only, IT-gated in the UI with the same access-denied pattern System Logs used: **Application History** (search/status filter, per-app row with status badge + last-event chip, expandable chronological timeline panel with kind icons/actor/detail) and **Performance** (summary cards incl. avg durations via a new `formatDuration` helper in `utils/format.js` + "No completed spans yet" honesty for null averages, per-app table with waiting/processing/review/turnaround columns, expandable evidence spans per row with `Open` tags and "→ now" ranges for unmatched requests). Frontend suite **90 passed (17 files)**; `npm run build` clean; `npm run lint` 0 errors (same 4 pre-existing fast-refresh warnings). Nothing committed — user to review. Note: the `test_document_analysis_api.py` 13-failure `TECH_BLANK_PAGE` baseline is unchanged and unrelated to this work.

- **2026-08-20, Phase 2 checkpoint: Application History cycle grouping COMPLETE.** Frontend `ApplicationHistoryPage.jsx` now groups `DOCUMENTS_REQUESTED → DOCUMENTS_RECEIVED` pairs into visual resubmission cycles. Implementation: purely frontend, no backend/schema/role changes; uses only backend workflow events; no timestamp heuristics; preserves chronological ordering and underlying events; supports closed cycles, open/unmatched requests, and multiple cycles; preserves existing document-upload grouping and structured metadata. Files changed: `ApplicationHistoryPage.jsx`, `ApplicationHistoryPage.module.css`, `ApplicationHistoryPage.test.jsx`. **Verification**: Application History tests **12 passed**; full frontend suite **97 passed (17 files)**; ESLint **0 errors** (4 pre-existing fast-refresh warnings); production build **successful**. Access model unchanged: Employee=all, IT=allowed, Operator=denied, Reviewer=denied. UX is intentionally not final — current cycle wording is technical ("Resubmission Cycle 1"), future should be business-oriented ("Documents requested — Cycle 1 of 2"). Full checkpoint at `docs/PHASE2_CHECKPOINT.md`. **Next step: real-data UX validation** before Phase 3. Do NOT implement: UX wording changes, new cycle API, Performance→Application History drill-down, new database tables, new backend cycle calculations, or additional cycle heuristics — those belong after validation. No commit/push unless explicitly instructed; no backend changes during validation unless an actual API deficiency is discovered.

- **2026-08-21: `feature/zarghuna-bulk-queue` merged into `main` (23 commits).** Shipped: roles (Employee/Operator/Reviewer/IT with real role guards, not just UI), AMC/Tripartite structural bank-account-block parser, performance metrics API+page, application history timeline, resubmission workflow fix, remember-me auth sessions + ENVIRONMENT fail-closed.
  - **AMC extractor gap resolved**: previously "returns {} on DG_Sports real data" (see 2026-08-19 entry), now **CONFIRMED FIXED** by direct re-execution against the same real file — correctly extracts the T-24 System account block (`account_holder`/`account_number`/`iban`) from the 3-account-generation structure.
  - **Test accounts now real and merged**: operator/reviewer/it, password `12345678`, plus the existing all-access Employee account.
  - **Full suite**: 912 passed, 12 failed (same pre-existing `fcc9fda` `TECH_BLANK_PAGE` baseline, root cause unchanged), 1 unrelated environment collision (`test_health.py` vs live `worker.heartbeat`, confirmed passes clean with worker stopped, not a real bug).

- **2026-08-21: `AuthorityLetterExtractor` `organization_name` garbage-capture FIXED (commit `6759c21`).** Root cause: the primary `organization_name` regex terminated on `\n`, so a line ending `"...on behalf of the\n"` captured the bare word `"the"` instead of the real organization. **Two documented real cases fixed**: `TMA_Lal_Qilla_Dir_Lower` and `TMA_Samarbagh_Dir_Lower` (both OCR'd and verified directly; Samarbagh was OCR'd fresh this session). Fix: tightened the regex to require a non-whitespace character before the newline, and added `"the"` to `_GENERIC_ORG_REFERENCE` so any remaining bare-pronoun captures trigger the letterhead fallback. Full 9-sample `AUTHORITY_LETTER` sweep: all other files byte-for-byte unchanged. Full `test_document_analysis_engine.py` + `test_reports_api.py` suite: **89/89 passed**.

- **2026-08-21: `BLUR_THRESHOLD` false-positives FIXED (commit `5e20a81`).** The global `variance_of_laplacian` score was giving false positives (flagging as blurry) on sparse, mostly-whitespace pages (like synthetic tests) because the zero-edge background diluted the variance. A tiled `max()` approach was tested but found to create a severe false-negative risk: a completely blurry page with a single sharp artifact (like a stray staple mark) scored `1403.94` and incorrectly passed the `100.0` threshold. Fix: implemented a tiled **95th-percentile** variance calculation. Validated: a real blurry document scores `18.34` (correctly fails), the stray-mark stress case scores `26.88` (correctly fails), and a full sweep of all 9 real PDF types scores safely between `1549` and `8678` (correctly passing with no regression). 17/17 technical validation tests pass.

- **2026-08-21: `party_1link` regression FIXED in `TripartiteAgreementExtractor` (commit `6c26f8d`).** The structural block parser merge (commit `2039bcd`) changed extraction order such that regexes ran first and their results were kept, causing `party_1link` to eagerly capture the Authority Letter preamble text (`"1-Link and the Khyber Pakhtunkhwa Information Technology Board (KPTIB) on behalf of TMA Thall."`) at the top of compound files like `TMA_Thall_Agreement`. Fix: added a fixed-width negative lookbehind `(?<!related to )` to the `party_1link` pattern to exclude the Authority Letter boilerplate phrase `"matters related to 1-Link"`. Validated against real data: `TMA_Thall_Agreement` correctly skips the Authority Letter and extracts `"1LINK (Private) Limited..."` from the actual Participation Memorandum section. `party_kpitb` is still correct, and there is no regression on `TMA_Lal_Dir_Upper` ONE_LINK_LETTER copies. Note honestly: the extracted `party_1link` value still isn't fully clean (it trails on with `"...and Khyber Pakhtunkhwa..."` because it lacks a comma to stop it) — this is a pre-existing imperfection returning to its pre-regression baseline, not a newly introduced issue. Tests: 79+10 passed. This fully resolves the in-session finding that `party_1link` was producing "WORSE / different garbage" after the merge.

- **2026-08-21: Splitter absorption instance on Samarbagh diagnosed (not fixed).** While OCR'ing `TMA_Samarbagh_Dir_Lower.pdf`, page 8 was absorbed into the open `BUSINESS_REQUIREMENT_DOCUMENT`. Diagnosed the cached text: page 8 is a Formal Request Letter containing the phrase "APPLICATION FOR ONBOARDING ON 1LINK 1BILL SERVICES". This caused a weak match for `ONE_LINK_LETTER`, but without hitting the strong threshold, it defaulted to being absorbed as a continuation. This confirms it is the exact same root mechanism as the `GDA_Abbotabad` and `GDC Alpurai Shangla` cases, not a new failure mode. Left unfixed, as it requires a different detection mechanism entirely.

- **2026-08-21: Tripartite Agreement compound-splitting scope assessment.** With the `party_1link` regression fixed above, the broader problem remains: relying on text patterns to split complex compound documents (Authority Letter + Participation Memorandum + SLA) is fundamentally fragile. Four options were scoped for future work (none committed to now): (1) Structural NLP block parsing (fragile to OCR noise, fails Section 8 visual requirements), (2) Visual/CV image classification (solves Section 8 visually but requires training a new ML model), (3) Layout-aware object detection like YOLO (finds physical stamps/signatures, requires ML training), (4) Multi-modal LLMs (highly accurate without training, but introduces high latency, API cost, and privacy concerns). Logged as scoping notes for a future decision.

*(The entries below are from `audit/backend-verification-2026-08-20`, a parallel investigation branch that diverged from `main` at `d1668f0` and is being merged in on 2026-08-22 -- see the dated merge-resolution entry appended at the end of this section for how the two independent timelines were reconciled. Its own 2026-08-19 entry on Zarghuna's bank-account-block parser is dropped here as a pure duplicate: it is essentially the same finding as the [RESOLVED 2026-08-21] entry above, minus that entry's later "CONFIRMED FIXED" update.)*
- **2026-08-20: TripartiteAgreementExtractor confirmed structurally not-production-ready against a second real sample, `TMA Thall Agreement.docx` -- and this is not a regex-tunable problem.** Distinct from `TMA_Lal_Dir_Upper`'s Participation Memorandum sample above (3/6 correct, 2/6 garbage, 1/6 honest miss). This `.docx` was cached whole (`TMA_Thall_Agreement__TRIPARTITE_AGREEMENT_copy1.txt`, 301 lines) -- it was never run through `DocumentSplitter.split_bulk_pdf`, which only handles PDF bytes, so it bundles an Authority Letter, a Participation Memorandum, the actual Agreement, a Formal-Request-style letter, and a signature table all in one file. Isolating just the "AGREEMENT FOR DIGITAL PAYMENT COLLECTION VIA PAYMIR" section and re-running the extractor: `party_kpitb` extracts correctly (KPITB is named directly); `party_1link` and `party_subbiller` land on garbage/empty because this real document has no formal "hereinafter referred to as 1-Link" naming clause anywhere; `account_holder`/`account_number` also fail because the real account clause is narrative prose ("...deposited in PK82KHYB50130987142561901...titled as TEHSIL GENERAL ACCOUNT TMA THALL"), not a labeled `Account Title:`/`Account Number:` line the patterns require. No pattern changes attempted -- per the CrossPeriodRule/CrossBranchCodeRule caution already on record above, tuning regex to fit one atypical real sample without generalizing repeats a documented mistake. Also worth flagging: `f8edde7` ("tighten TripartiteAgreementExtractor party/account patterns") was committed before this sample existed, so its correctness against real Tripartite content remains unverified either way -- not confirmed right, not confirmed wrong.
- **2026-08-20: 2 new `organization_name` garbage-capture cases found in `AuthorityLetterExtractor`, distinct from the honorific bug already fixed in `ccaccff`.** Real samples `TMA_Lal_Qilla_Dir_Lower__AUTHORITY_LETTER_copy1.txt` and `TMA_Samarbagh_Dir_Lower__AUTHORITY_LETTER_copy1.txt` both extract `organization_name: 'the'`. Source text: "...to deal with and conduct correspondence and matters related to 1-Link and the\nKhyber Pakhtunkhwa Information Technology Board (KPITB) on behalf of the\nAuthority." -- the pattern's capture stops at the line break before reaching KPITB's actual name, landing on the dangling "the". Vague/awkwardly-wrapped source prose, not a regression from `ccaccff`'s fix (different field/case). Note for whoever picks this up: `feature/afsana-validation-logs` commit `89b4d60` (2026-08-19, unmerged) generalizes a *different* `AuthorityLetterExtractor` field (`focal_person_name`, for a third department, `TMA Lal Dir Upper`) -- it does not touch `organization_name` and would not fix this case if merged as-is. Separately, 1 of the 8 real samples checked (`TMA_Thall_Hangu__AUTHORITY_LETTER_copy1.txt`) is not an Authority Letter at all -- it's a "MASTER CHECKLIST" cover page listing "Authority Letter" as a checklist row item, misclassified by the splitter. Zero-field extraction there is the correct outcome for that page's actual content; if there's a bug, it's in splitter classification, not this extractor. Neither issue fixed here.
- **2026-08-20: `BusinessRequirementDocumentExtractor` -- header-echo capture pattern, fixed 2026-08-21.** Across real cached samples: some captures echo a section header verbatim as the field value (e.g. a field capturing the literal string `'SERVICES OFFERED'` instead of the services actually listed beneath it) -- the capture group doesn't skip past the header line into its content. (Resolved via 7-line window heuristic, see below).
- **2026-08-20: root cause confirmed for the 2 zero-field BRD samples (`TMA_Lal_Qilla_Dir_Lower__BUSINESS_REQUIREMENT_DOCUMENT_copy2.txt`, `TMA_Samarbagh_Dir_Lower__..._copy2.txt`) -- two independent real bugs stacked in the same sample.** Read both raw cached files in full, split at their `--- page break ---` marker, and re-ran the extractor against each half in isolation to separate the two mechanisms rather than inferring from the combined zero-field result alone. **(1) OPEN BUG - Splitter continuation-absorption, same mechanism already documented for `TMA_Lal_Dir_Upper`/`GDC_Alpurai_Shangla` above**: each `copy2` file is actually two unrelated real documents merged into one -- a genuine BRD continuation page followed by a completely separate onboarding-request letter to KPITB's Director (\"Subject: APPLICATION FOR ONBOARDING...\"). `app/preprocessing/splitter.py`'s `FORMAL_REQUEST_LETTER` entry has no phrase matching this real subject line, so the page is silently absorbed as a continuation of the still-open BRD group. **(2) FIXED - `BusinessRequirementDocumentExtractor` gap on real content**: The `digitization_intent_confirmed` pattern was broadened to match the full department name. Verified against the 5 real cached samples present at the time via venv interpreter (TMA_Lal_Qilla copy1/copy2, GDA_Abbotabad, DG_Sports, TMA_Khal). *Correction (2026-08-21): An earlier commit (bd83c22) and entry claimed this was verified against "all 7 real samples", but TMA_Samarbagh copy1/copy2 had actually been deleted from the cache for testing and were missing during that sweep. The fix itself was safe because Samarbagh shares the exact Lal Qilla template vocabulary, but the physical files were not present.* **(3) FIXED (2026-08-21) - `revenue_services_listed` header-echo**: The regex was updated from a blind `.extract()` on the anchor to a line-aware boundary pattern `[^\n]*(?:\n[^\n]+){0,7}` that captures the header plus up to 7 subsequent complete lines. **Warning: This is a fitted heuristic tuned to the observed samples, not a general semantic boundary.** It will pull in adjacent, unrelated content if the real list is shorter than 7 lines (e.g., bleeding into digitization-intent text in synthetic fixtures), and will truncate real lists longer than 7 lines. Acceptable risk because this is a non-critical human-review context field, not a gating field, but must be understood as a heuristic, not a clean general fix.
- **2026-08-20: DB-guard investigation -- inconclusive, no live reproduction achieved.** The 68 failed / 122 errors IntegrityError-heavy test run earlier today prompted an extended investigation into whether `tests/conftest.py`'s DATABASE_URL-rewrite-to-`_test` guard was misfiring. No live reproduction of the guard firing was achieved during this investigation -- every reproduction attempt instead hit either an invocation-method quirk (`cmd /c` vs direct PowerShell interpreter invocation producing inconsistent results) or an unrelated, since-resolved false alarm (a `ModuleNotFoundError` for psycopg that could not be reproduced when the interpreter was invoked directly and consistently). The rewrite logic (lines 33--39 of `backend/tests/conftest.py`) and the guard in `_wipe_database()` were both read and confirmed present and unchanged since commit `d1668f0`. Left genuinely unresolved: whether the original 68-failed/122-error run was a real guard misfire or was actually hitting substantive test failures whose error signature was misread as IntegrityError-driven DB contamination. **If this resurfaces**: before investigating `conftest.py` again, first verify `$env:DATABASE_URL` is unset in the invoking shell (`echo $env:DATABASE_URL` in PowerShell -- must be empty), and always invoke pytest via `& '<path>\python.exe' -m pytest ...` directly rather than through `cmd /c ... && python.exe -m pytest ...`, since mixed invocation methods produced several misleading one-off errors this session.

- **2026-08-22: `audit/backend-verification-2026-08-20` merged into `main` (commit `904ab84`), closing the timeline reconciliation promised in the bridging note above.** The two branches had independently fixed the same `AuthorityLetterExtractor.organization_name` garbage-capture bug (main's `6759c21` vs the audit branch's `0be3ff6`) -- both variants were run against all 9 real cached Authority Letter samples before choosing, not picked by "ours wins": byte-identical results on every sample, so the choice came down to which was structurally safer, and the audit branch's bounded one-newline-tolerance variant was adopted, matching the surviving class docstring's own documented convention. `.gitignore` additions from both branches were combined (no conflict, pure union). The `CONTEXT.md` timelines were combined by hand, dropping the audit branch's own 2026-08-19 entry on Zarghuna's bank-account-block parser as a pure duplicate of the `[RESOLVED 2026-08-21]` entry already on `main` (same finding, minus that entry's later "CONFIRMED FIXED" update). The merge also brought in the audit branch's `SESSION_HANDOFF_2026-08-20.md`, its BRD fixes, its DB-guard investigation notes (both entries above), and additional test coverage. **Post-merge verification, not just an assertion**: `test_document_analysis_engine.py` went from 60 to 79 tests (confirmed via actual diff of the merged file, not inferred from the audit branch's own claim) -- audit branch contributed 0 new `def test_` functions of its own (its 3 changes were assertion updates to pre-existing tests), main's own prior session work independently added the other 19; `test_reports_api.py` 10 passed; full suite 912 passed / 13 failed (known baseline, unchanged). A separate stale-assertion regression surfaced immediately after (`revenue_services_listed` in `test_document_analysis_api.py`, one of the merge's pre-existing tests never updated when an unrelated BRD header-echo fix landed) -- fixed same day as its own commit (`4f8c390`), suite confirmed back to the exact 13/912 baseline. `audit/backend-verification-2026-08-20` was confirmed fully merged (`git branch --merged main`) and deleted, both local and remote, after this verification.
- **2026-08-22: `BilateralAgreementExtractor` rewritten against real content, first real-sample validation for this type (commit `fc27a24`) -- and it surfaced a downstream rule-engine bug, fixed the same day (commit `19a2e29`).** The original patterns were written blind against `docs/Master_Rules_Combined.md` and matched none of the real content on either of the two real samples now on file (Conservator Wildlife Peshawar Zoo, a second KP department) -- wrong platform terminology (spec's "Digital Muhasil/PayMin/Paymere BCX" vs the real documents' own defined "PAYMIR"), wrong section anchor ("Section 5.2" vs the real bare "5.2."), and the same table row-index garbage-capture bug (`account_number: '01'`) already fixed elsewhere for AMC/Tripartite. Rewritten field-by-field against both real samples, reusing the existing `_extract_bank_account_block` structural parser for the account fields (see `extractors.py`'s `BilateralAgreementExtractor` docstring for the full before/after). One real n=2 generalization bug caught during validation: the `transaction_charges` stop boundary matched a false numbered list inside an unrelated e-stamp verification page physically inserted in one real sample between the charges table and the next section -- fixed with two stop conditions (an all-caps section header, or the OCR cache's own page-break marker). **Result, confirmed on both real samples**: 6 of 7 fields correct; `account_holder` is a genuine structural honest-miss on both -- neither real sample's bank-account table has an "Account Title"/"Account Holder" column at all, only Bank Name + Account No.
  That honest-miss finding then broke `CrossAccountHolderRule` (`rule_engine/rules/cross_document_rules.py`), which required `BILATERAL_AGREEMENT` as a participant on `account_holder` -- the same shape of problem as the already-documented `CrossBranchCodeRule`/`CrossPeriodRule` cautionary tales, just discovered for a rule that was actually registered and passing (on stale, unrealistic test fixtures) rather than one already known to be broken. Fixed by narrowing `CrossAccountHolderRule.participants` to `ACCOUNT_MAINTENANCE_CERTIFICATE`+`TRIPARTITE_AGREEMENT` only (both genuinely extract `account_holder` for real) -- `BILATERAL_AGREEMENT` removed, not the whole rule unregistered, since the AMC↔Tripartite comparison remains real and meaningful. Propagating this through the test suite (`test_rule_engine_api.py`'s `BILATERAL_STATEMENT_TEXT`, `test_reports_api.py`'s two hardcoded fleet-wide `overall_confidence` assertions, `test_rule_engine_rules.py`'s generic cross-document unit test) took one more commit (`c56efee`) to fully settle; full suite confirmed back to the documented 13-failed baseline (913 passed, +1 over the prior 912 from a new test this work added) after all four commits landed.
- **2026-08-22: `TripartiteAgreementExtractor.branch_code` real-sample validation attempted and blocked -- accepted, not blocking, needs future work.** Proposed fix: port `OneLinkLetterExtractor`'s old parenthetical branch-code pattern (`r"branch\s*\([^()]*\((\d{3,6})\)+"`, matches a "Branch (0312)"-shaped clause) into `TripartiteAgreementExtractor`, whose current pattern (`r"(?:Branch Code|Branch)\s*:\s*(.+)"`) requires a literal colon and never matches that shape at all. That pattern was itself removed from `OneLinkLetterExtractor` in `26d57eb` (2026-08-19, department decision) for the same reason `CrossBranchCodeRule`/`CrossPeriodRule` were unregistered: one real clause shape is an unambiguous single-account sentence, but the other real shape is a multi-bank reference table with no textual indication of which entry is operative, and the pattern can't tell the two apart.
  Why it's not fixable today: the only real Tripartite Agreement sample on file (`TMA_Thall_Agreement__TRIPARTITE_AGREEMENT_copy1.txt`, already flagged 2026-08-20 above as structurally not production-ready) has no branch code anywhere to validate against, filled or otherwise. Its prose clause reads `"...maintained with (BANK OF KHYBER THALL) in its branch (Branch name/address);"` -- a literal unfilled template placeholder, not real data. Its account table (`"01 | Bank of Khyber Thall Branch | PK82KHYB..."`) has no separate branch-code column either -- "Branch" is just part of the bank's name. The real `"Branch (0312)"` text that motivated this fix exists only in two other real files (`TMA_Khal_Dir_Lower`, `TMA_Lal_Dir_Upper`), and in both cases it was cached and classified as **ONE_LINK_LETTER**, not Tripartite -- neither source PDF has any TRIPARTITE_AGREEMENT-classified copy at all. There is currently no way to real-sample-validate a Tripartite-specific branch_code pattern; porting a pattern that only worked on a different document type would repeat the exact mistake `CrossBranchCodeRule`/`CrossPeriodRule` already document.
  What a real fix requires: a genuine real Tripartite Agreement sample that actually states a branch code (filled, not a placeholder) to validate against. Until one exists, `branch_code` stays as-is (a colon-anchored pattern that honestly misses on the one real sample checked, not garbage) -- left alone rather than swapped for an equally-unvalidated alternative.

- **2026-08-22: `feature/afsana-validation-logs`' remaining 3 extraction commits reconciled -- 2 small pieces landed, the rest dropped, one drop caught by real-sample re-validation, not by inspection alone.** `09e099a`/`685ec5d`'s `BilateralAgreementExtractor` prose-pattern changes were validated (per their own commit messages) against `GDC_Madyan_Swat`'s real "Participation Memorandum" text -- **that text is real, but the splitter classifies it as `ONE_LINK_LETTER`, not `BILATERAL_AGREEMENT`**, confirmed by actually OCR'ing and splitting the source PDF today (it was never in the cache before): 5 documents produced (1 `ACCOUNT_MAINTENANCE_CERTIFICATE`, 3 `ONE_LINK_LETTER` copies, 1 `OTHER_SUPPORTING_DOCUMENT`), zero `BILATERAL_AGREEMENT`. `BilateralAgreementExtractor` never runs on this content in the real pipeline -- the branch's validation fed the text directly into `extract_fields(text, BILATERAL_AGREEMENT)` in a test, bypassing real document-type routing entirely. Dropped, not merged: today's from-scratch rewrite (`fc27a24`, see above) is validated against the two real samples that actually do route to this extractor. `ac5914e`'s `AccountMaintenanceCertificateExtractor` next-line/parenthetical/combined-label regex changes are superseded outright -- confirmed by running her own real-sample fixture (`GDC_Madyan_Swat`'s genuine AMC page, now cached) directly against current main: `_extract_bank_account_block` (the shared structural parser that replaced AMC's regex-based account fields entirely after she diverged) already gets `account_holder`/`account_number`/`iban`/`branch_name` right, without her patch. Her one still-relevant piece -- adding a bare `"Date"` label alternative to `issue_date`, since Madyan Swat's real AMC only has an unlabeled `"Date: 17/02/2026"` line -- is a real, still-open gap, but the fix itself is unsafe: tested against all 6 other real AMC samples on file, it fixes 2 genuine honest-misses and introduces 2 new garbage captures, one a clear regression (`GDA_Abbotabad copy4`: `"specific"` -> `"d"`, a single meaningless character). Not landed -- fixing 2 samples by breaking 2 others is not a net improvement. **Landed instead, both real and independent of the above**: `frontend/src/data/documents.js` gained `OTHER_SUPPORTING_DOCUMENT`/`BULK_UPLOAD` catalogue entries (`6c794c9`) -- real backend `DocumentType` enum values the frontend catalogue was missing, previously silently mislabeled as "Authority Letter"; `_parse_date` gained hyphenated date format support (`%d-%m-%Y`, `%d-%b-%Y`, `%d-%B-%Y`, `e2ce416`) -- purely additive, though its cited real-sample motivation lives in the same misrouted `ONE_LINK_LETTER` content and no currently-parsed real field was found anywhere in the cache that actually needs it yet; kept anyway since it can't regress anything already working. `25c8508` (the focus-trap fix) was already cherry-picked earlier today. With this, every commit unique to `feature/afsana-validation-logs` has been accounted for -- see the branch-reconciliation-status entry immediately following this one.

- **2026-08-22: `feature/afsana-validation-logs` fully reconciled -- nothing left un-accounted-for, branch deletion still not done here.** Confirmed by diffing the full tree at the branch's merge-base against its tip (not just re-reading `git log main..branch`, which will always show these commits as "unmerged" by SHA -- cherry-picking and manual patches create new commits, they don't make the old ones ancestors of `main`): every file the branch touches beyond that merge-base -- `backend/app/core/config.py`, `SessionTimeoutModal.jsx`, `ConfirmDialog.jsx` (all from `25c8508`, cherry-picked earlier today as `9c28012`), `backend/app/document_analysis/extractors.py` + `backend/tests/test_document_analysis_engine.py` (the 3 extraction commits, reconciled above), `frontend/src/data/documents.js` (`09e099a`'s catalogue fix, landed) -- is accounted for. The branch's own `CONTEXT.md` edits were not merged verbatim; main's independent, more current narrative already covers the same ground. Local branch ref (`feature/afsana-validation-logs`) was stale throughout this reconciliation -- it pointed at an old commit already an ancestor of `main`; all comparisons here used `origin/feature/afsana-validation-logs`. Deleting the branch (local and remote) is a deliberate action, not something to do as a side effect of a docs update -- left for explicit confirmation.

- **2026-08-22: `SCHEDULE_OF_CHARGES` removed from the required checklist -- was silently making `APPROVED` unreachable for every real application, not just an extraction gap (commit `6cd94ee`).** Abdul's decision: it is not a real standalone document in this checklist -- its content (the Payment Methods & Charges clause) lives inside the Bilateral Agreement, confirmed today when both candidate real Schedule of Charges files turned out to actually be Bilateral Agreements. Investigated before any code change: `DocumentScheduleRule` (`DOC_SCHEDULE_OF_CHARGES_PRESENT`) was registered and hard-FAILed (severity ERROR) whenever the type was absent, and per `OVERALL_STATUS_PRECEDENCE` any FAIL forces the whole application's `overall_status` to FAIL. Since **zero real samples across every real source file processed this entire session (~21 files) have ever been splitter-classified as `SCHEDULE_OF_CHARGES`** -- confirmed by re-checking the full OCR cache, not assumed from the 2026-08-17 blocker entry alone -- this rule was an unconditional, permanently-unsatisfiable blocker: no application, however complete otherwise, could ever have reached `APPROVED` while it stayed registered. This is a materially bigger finding than a missing extractor; it's the same failure shape as `CrossBranchCodeRule`/`CrossPeriodRule` but for a top-level required-document-presence rule rather than a secondary cross-document check, and it had gone unnoticed as "the real, universal blocker" until traced through explicitly today (the existing application-`11189` live-demo entry already showed the empirical `FAILED` result, but hadn't been connected to "this specific rule structurally cannot ever pass").
  Cross-checked, not assumed: Master Rules §9's content requirements (charge/transaction information, PKR currency, completeness) are near-verbatim the same substance already required by §7's own "Section 5 — Transaction Charges" clause, and `BilateralAgreementExtractor.transaction_charges` (rewritten today, see above) already captures that full PKR fee table on both real Bilateral samples. `BILATERAL_AGREEMENT` is already in both `SIGNATURE_DOCUMENT_TYPES` and `STAMP_DOCUMENT_TYPES` (`rule_engine/constants.py`), covering §9's signature/stamp requirement too. No real extraction gap results from treating the two as equivalent.
  Removed from both `completeness/constants.py` and `rule_engine/constants.py`'s `REQUIRED_DOCUMENT_TYPES`. `DocumentScheduleRule` unregistered, not deleted (matches the `CrossBranchCodeRule`/`CrossPeriodRule` precedent — the class stays defined in `rule_engine/rules/document_rules.py` for if a genuine sample ever surfaces). `upload/constants.py`'s 6-copy `MAX_COPIES_BY_DOCUMENT_TYPE` entry dropped (falls back to the default 1-copy limit); its `DOCUMENT_TYPE_SLUGS` entry deliberately kept (a plain dict lookup with no default, used for storage regardless of required-checklist status — removing it would crash storage if the splitter's still-in-place title-match phrases ever did fire on a real page). `preprocessing/splitter.py`'s phrase table left untouched — harmless if it never matches, removing it would be unrelated churn.
  **Important, explicitly checked before claiming success**: this does not by itself unblock any specific already-known-incomplete application. Application `11189` (`TMA Khal Dir Lower.pdf`) was missing 5 of 8 required types; removing this one still leaves it missing `FORMAL_REQUEST_LETTER`/`ACCOUNT_MAINTENANCE_CERTIFICATE`/`BILATERAL_AGREEMENT`/`TRIPARTITE_AGREEMENT`, so it still fails `overall_status`. What changed is that `APPROVED` is now *reachable in principle* for an application that genuinely has all 7 of the remaining required types — before today, it was not reachable for any application at all.
  6 test files had hardcoded assumptions on the old 8-required-type/53-rule counts, fixed: `test_rule_engine_rules.py`, `test_reports_api.py`, `test_rule_engine_api.py`, `test_completeness_api.py`, `test_human_verification_api.py`, `test_operator_workflow_api.py` — plus two fleet-wide `overall_confidence` constants recalibrated (`0.9701` → `0.9587`, same established recalibration convention as earlier today). Full backend suite: 52 rules now (was 53), 912 passed / 13 failed (was 913/13) — same 13 pre-existing named failures, the 1-test drop is the removed `SCHEDULE_OF_CHARGES` parametrize case, not a regression. Frontend suite: 97 passed, 0 failed.

- **2026-08-22: `docs/TEAMMATE_BUG_TRIAGE.md`'s 2026-08-16 snapshot re-verified against current code — 14 of its 28 CONFIRMED items were already fixed by unrelated work landed since then, 14 are still real, both real-sample-validation findings were fixed as a side effect of the AMC structural-parser rewrite.** Investigation only, no fixes applied as part of this pass — see that file's own "Re-verification — 2026-08-22" section for the full corrected table (item-by-item status, commit citations, evidence) and a proposed fix order for the 14 still-open items, grouped by effort and by whether a fix can mirror a pattern already proven elsewhere in the codebase (e.g. two hooks are missing a request-ID/`hasWork` guard their own sibling hook already has). Nothing here changes any of this file's own still-open findings above (splitter absorption, AMC boundary, `ID_DOCUMENT` collision, etc.) — that's a separate, still-current list.

- **2026-08-23: New, confirmed-real full-suite-only test flake found while verifying the teammate-triage batch-1/2 fixes -- `test_split_absorption_disagreement_is_logged_but_does_not_split` and `test_evaluate_with_zero_extracted_fields_does_not_500` both ERROR only when the full backend suite runs start-to-finish, never in isolation.** Not caused by any of that batch's 6 changes -- neither touched file (`preprocessing/splitter.py`'s test, `confidence`'s test) overlaps with what was fixed (`normalization/services.py`, `ReportIssues.jsx`, `ReviewDecision.jsx`, `Navbar.jsx`/`SidebarProfile.jsx`, `useValidationReport.js`, `useProcessingProgress.js`). Confirmed genuinely reproducible, not a self-inflicted concurrent-pytest-invocation artifact (the first suspicion, given this project's own prior "DB-guard investigation" caution above): two independently clean, fully-idle-verified, non-overlapping single invocations both reproduced the identical 2 errors -- a full suite run (842 passed/12 failed-baseline/2 errors) and a narrower bisection covering just the first 10 collection-order files ending at `test_confidence_api.py` (183 passed/2 errors). Both flaky tests use `caplog` to assert on specific logger output (`app.preprocessing.splitter` / the confidence module), and `tests/integration/test_paddleocr_integration.py` -- which imports and initializes the real `PaddleOCREngine` -- runs first in collection order every time, ahead of both. **Root mechanism not fully pinned down**: running just the integration file's tests directly followed by the two flaky tests (7 tests, no intervening files) passed cleanly with zero errors, so it is not as simple as "importing real PaddleOCR permanently breaks caplog for the rest of the process" -- something about the cumulative state of the ~180 intervening tests between the integration file and these two is also required to trigger it, not reproduced from the two endpoints alone. Left open, not investigated further today (out of scope for the triage-fix batch); if picked up, the caplog/logging-handler-reconfiguration angle is the strongest lead, not a DB-contention one (both failure modes are ERRORs, not assertion FAILUREs, consistent with a fixture/setup problem rather than a real logic bug in either test's subject code).

- **2026-08-23: `FieldBilateralPlatformNamePresenceRule` added (commit `a4159c2`) -- Master Rules §7 platform-terminology requirement, closed.** Presence-only rule on `platform_name`, same `_FieldPresenceRule` convention as the 9 sibling rules already in `field_rules.py`. `BilateralAgreementExtractor`'s `platform_name` field is real-sample validated (2026-08-22, see above) and both real Bilateral samples on file extract `'Paymir'` cleanly, confirmed directly against the extractor before writing the rule, not assumed. Rule count 52 -> 53, `field_presence` category 9 -> 10; hardcoded count assertions updated across `test_rule_engine_rules.py`/`test_rule_engine_api.py`/`test_reports_api.py`. 98+50 tests passed across affected/adjacent suites, zero new ruff/mypy issues vs baseline.

- **2026-08-23: `ESTAMP_PATTERN` double-escaped regex bug fixed (commit `757b57a`) -- `FormatEStampRule` is now correct, but still practically inert.** `r"^STAMP-\\d{6}$"` (double backslash) required a literal `\` before six literal `d` characters, so it could never match a real `STAMP-######` value -- fixed to `r"^STAMP-\d{6}$"`. Confirmed dead/unreachable before fixing: `e_stamp_id` has zero producers anywhere in `extractors.py`, and `_FormatRule.evaluate()` short-circuits to WARNING before the predicate ever runs when the field has no values -- which it always does here, so the broken regex has never actually executed against a real value. Nothing in tests or real usage depended on the broken behavior (grepped, zero references). **This fix does not close the gap**: `FormatEStampRule` remains practically inert until some extractor actually produces `e_stamp_id` -- not attempted here, deliberately out of scope for this fix.

- **2026-08-23: Bilateral Agreement `effective_date` rule -- scoped, genuinely ambiguous, NOT built.** Asked to build a presence rule (the same shape as the platform-name rule above); before writing it, went back to `docs/Master_Rules_Combined.md` in full rather than trust an earlier fragment-only read. §6 (Tripartite, line 106) is unambiguous: the preamble date "should not contain an entered date... An incorrectly populated date shall be flagged" -- filled is the violation. §7 (Bilateral, lines 141-142)'s wording is different and does not resolve cleanly either way: "dates that are required to remain blank must remain blank," which could mean `effective_date` specifically is one of those must-stay-blank dates (same direction as Tripartite), or could be read as only *other* dates being subject to that clause while `effective_date` itself is required. `BilateralAgreementExtractor.effective_date`'s own regex is anchored on `"made and entered into on this <DD> day of <MM>, <YYYY>"` -- structurally the same kind of preamble clause §6 describes for Tripartite, which leans toward the must-stay-blank reading. The two real samples on file split exactly on this: `Conservator_Wildlife_Peshawar_Zoo` has `effective_date` FILLED (`'2026-05-11'`); `schdule_of_charges_or_bilateral` has it BLANK (`None`). Building the rule in either direction risks an unconditional FAIL on one of only two real samples this extractor has ever been validated against -- the same failure shape already documented for `CrossBranchCodeRule`/`CrossPeriodRule`/`DocumentScheduleRule` above. **Needs an explicit scope decision on what §7 actually requires for this specific date before any rule is written**; not attempted today.

- **2026-08-23: Visual-detection pipeline investigated and CLOSED -- decided not to build, not merely deferred.** Distinct from the still-open splitter structural heuristic below: this one was scoped end-to-end and a deliberate decision made not to act on it, not left pending for lack of time.
  **Confirmed, precisely, not just re-asserted**: `VisualDetectionRepository.upsert()` exists and works (`visual_detection_repository.py`), but a fresh grep specifically for `VisualDetectionRepository` (not the generic `.upsert(` pattern, which false-positives against unrelated `OCRRepository.upsert()` calls in `document_processing/services.py`/`confidence/services.py`) confirms the only three real callers -- `human_verification/services.py`, `rule_engine/services.py`, `reports/services.py` -- only ever call `.get_by_application()`/`.get_by_document()`. Zero real write-side callers anywhere, including `bulk_queue`, `document_processing`, `document_analysis`.
  **Live-DB trace, not just code-reading**: `visual_detection_results` has 0 rows in the real dev database. Of the 3 real applications that have ever run the rule engine (`20292`, `20293`, `20294`), every one of the 11 `VIS_*` rules resolves to only two outcomes across all of them -- `FAIL` ("Document X is missing; cannot verify signature/stamp") when the document type wasn't uploaded, `PENDING_MANUAL_REVIEW` ("No signature/stamp detection outcome is available...") whenever it was uploaded. Zero PASS ever recorded, zero "detected but absent" FAIL ever recorded (that branch requires a detection row to exist at all, which never happens). `PENDING_MANUAL_REVIEW` is not silently cleared or bypassed anywhere -- `human_verification/services.py` reads no gating logic on `overall_status` at all.
  **Not "never built" -- built once, wired in once, then lost in the wholesale pipeline replacement.** `git log --all --diff-filter=D` found `app/pipeline/stamp_signature_detector.py` (and its test) deleted in the same commit (`726a607`) that replaced Samad's original synchronous pipeline with Zarghuna's async redesign -- same fate as the already-documented `ImageEnhancer` deletion from that commit (see the 2026-08-22 entry above). It was a real `StampSignatureDetector` class (OpenCV color-range + circularity heuristics for stamps, aspect-ratio + density contour heuristics for signatures), genuinely wired into the old `PipelineOrchestrator` and called on every document. **It is a cautionary tale, not reusable code**: both of its tests validated exclusively against synthetic OpenCV-drawn fixtures (a hand-drawn blue circle for "stamp," a hand-drawn squiggly polyline for "signature") -- never once against a real scanned document. Reviving it as-is would repeat the exact `CrossBranchCodeRule`/`CrossPeriodRule` mistake this file already warns against, restated for computer vision instead of regex.
  **The business itself never intended full automation here.** `docs/IMPLEMENTATION_ROADMAP.md`'s Phase 2 (written 2026-08-15, never executed) quotes Master Rules §15 directly: *"The system checks signature presence and required location, not signature authenticity... The authorized employee remains the final decision-maker."* And: *"Full automation of this category was never the intent."*
  **Why this is a non-issue for correctness, not just a lucky accident**: `human_verification/constants.py`'s `CHECKLIST_ITEMS` already lists 9 signature/stamp confirmation items covering every document type the 11 rules target (e.g. "Authority Letter signature confirmed," "Account Maintenance Certificate stamp confirmed"), and `validators.py`'s `_validate_approve()` hard-enforces server-side that every checklist item must be checked before an `APPROVE` decision is accepted (`ChecklistIncomplete` otherwise) -- not a UI nicety. A reviewer cannot approve an application today without personally confirming every required signature/stamp, structurally independent of the always-`PENDING_MANUAL_REVIEW` rule-engine rules. The one real gap was cosmetic: `ReviewDetections.jsx` (meant to *show* stored detection outcomes on the review screen) always rendered "No signature or stamp detections are stored for this application," reading as broken rather than as intentional -- fixed same day (`1b40409`) to state plainly that automated detection isn't wired up and the checklist below is the real verification step.
  **Decision (2026-08-23): do not build a detector.** The manual gate is real, already enforced, and matches the documented business intent. Building one now would mean validating against only 3 real applications' worth of evidence -- the same overfit risk already burned this project before. The only remaining value a detector would add is reviewer efficiency (skip re-verifying an already-confirmed signature), not correctness -- a UX investment, not a compliance one, and not undertaken today.

- **2026-08-23: Formal Request Letter splitter absorption bug CLOSED (commit `91f6600`)** -- the general case, not just the single narrow instance already fixed 2026-08-22. Originally scoped against 3 real samples (TMA Lal Dir Upper -- already correctly split, TMA Samarbagh, GDC Alpurai Shangla); full-corpus validation before implementing found **3 additional real instances of the identical bug** (TMA Khal Dir Lower, TMA Lal Qilla Dir Lower, TMA Thall Hangu), 6 total across 6 independent real source files. Root cause confirmed: real subject lines vary in exact wording per department, so the existing single-phrase match (`"SUBJECT: REQUEST FOR DIGITAL ACCOUNT"`) only ever covered one of them. Fix: a structural fallback in `_classify_page` -- an `"OFFICE OF THE..."` letterhead line together with a `"SUBJECT:"` line (colon required, to avoid false-matching mid-sentence legal boilerplate like `"Subject to arbitration..."`), both within the header zone on the same page, evaluated only after every existing phrase-based check has had its chance so it never overrides a more specific match.
  Validating this before writing any code surfaced a real false-positive risk and its fix, not just the 6 true positives: `TMA_Thall_Hangu__AUTHORITY_LETTER_copy1.txt`'s checklist cover page spells it `"MASTER CHECK LIST"` (two words) rather than `"CHECKLIST"`/`"MASTER CHECKLIST"` (no space), which the pre-existing `_CHECKLIST_PAGE_MARKER` guard's single-string substring check silently missed -- this page would otherwise have satisfied the new structural signal too. Widened the guard to a 2-marker tuple (`"CHECKLIST"`, `"CHECK LIST"`); grepped the full real corpus for the two-word spelling first (exactly 2 files, both genuine checklist covers, zero non-checklist content) before adding it, same discipline as every other change today. **Side effect, confirmed contained to the one page**: this also fixes the mislabeling already flagged in this file 2026-08-20 (this same checklist page's own "Authority Letter" table row was strong-matching `AUTHORITY_LETTER`) -- verified via a dedicated regression test that the genuine Authority Letter elsewhere in that same source file is unaffected.
  Real end-to-end validation against Samarbagh's actual source PDF, live PaddleOCR (not cache): before, 6 documents, the Formal Request Letter silently merged into an 788KB `BUSINESS_REQUIREMENT_DOCUMENT` blob (the splitter's own weak-match logging caught it live: `"Page 8 weakly matches ONE_LINK_LETTER but was absorbed..."`); after, 7 documents, 0 absorption warnings -- a clean 401KB `FORMAL_REQUEST_LETTER` splits out, the BRD shrinks to its own real 387KB content, every other document in the file byte-for-byte unchanged. 8 new regression tests (one per real true-positive shape, one for the widened-guard exclusion, one for the legal-boilerplate non-match). Full `tests/preprocessing/` suite: 39 passed, 0 failed. Lint/mypy: same pre-existing findings as baseline (`_classify_page`'s pre-existing `PLR0912` branch-count complexity warning got numerically worse, 16→19 over its already-violated 12 threshold, but not a new violation category).
  Deliberately not touched today, per explicit scope: everything visual (page-image statistics, watermark matching) and the visual-detection-pipeline discovery (see the CLOSED entry above) -- this fix stayed text-only throughout, exactly as scoped.

- **2026-08-23: Issue 2 closed -- `CNIC_FRONT` added to the required-document set (commits `e7f049e` Pass 1, `2925d2f`/`a476848`/`9facc50`/`5d04c59` Pass 2).** An omission fixed, not a new policy: the frontend's own upload checklist (`frontend/src/data/documents.js`) already treated CNIC as required, but `completeness/constants.py::REQUIRED_DOCUMENT_TYPES` and its mirrored copy in `rule_engine/constants.py` had never been reconciled against it -- a fully-compliant applicant who uploaded everything the Upload page asked for, including CNIC, could never reach `COMPLETE` (`CNIC_FRONT` fell through to the "unexpected document" path instead). Fixed by adding `DocumentType.CNIC_FRONT` to both sets, with a dated comment in each explaining the CNIC_FRONT-only reasoning below.
  **CNIC_FRONT only, not CNIC_BACK, same standing reasoning already on record above (2026-08-17 CNIC scope decision)**: CNIC_FRONT has a real, sample-validated extractor (`CnicFrontExtractor`) -- positive evidence real applications include it. CNIC_BACK still has the standing, deliberate "zero real samples exist" gap that already excludes it from extraction scope; making it a hard completeness requirement without any real evidence would have repeated the `SCHEDULE_OF_CHARGES` "unconditional blocker" mistake documented earlier in this file -- an unconditional blocker for a type that may not structurally arrive from a real bulk split.
  **Pass 2 (fixture reconciliation), 4 files, one commit each, in order**: `test_completeness_api.py` (`2925d2f`) -- required-document counts 7->8, plus swapped the two "unexpected document type" fixtures from `CNIC_FRONT` to `CNIC_BACK` (confirmed still genuinely outside the configured set, not assumed); `test_operator_workflow_api.py` (`a476848`) -- one empty-application queue-item count bump; `test_human_verification_api.py` (`9facc50`) -- two review-screen document counts; `test_reports_api.py` (`5d04c59`) -- two report document counts plus `build_full_application`'s docstring.
  **Two findings worth preserving, so nobody re-derives them from scratch later**: (a) `rule_engine/constants.py::REQUIRED_DOCUMENT_TYPES` is not consumed by any registered rule -- confirmed by grep, only by this test fixture -- so the originally-predicted `test_rule_engine_api.py`/`test_rule_engine_rules.py` blast radius never materialized; each `DocumentXRule` presence check (`rule_engine/rules/document_rules.py`) is a separate hardcoded class, not derived from this constant. (b) The `overall_confidence == 0.9587` constants in `test_reports_api.py` did **not** need recalculation -- confirmed by actually running the suite, not assumed -- because `CNIC_FRONT` has no dedicated text branch in `build_full_application` and falls to the same fully-covered `BANK_STATEMENT_TEXT` fallback the removed `SCHEDULE_OF_CHARGES` type used to use, landing on the identical rounded mean by genuine coincidence.
  **Final state, confirmed via full suite runs, not assumed**: full backend suite 853 passed / 12 failed (the pre-existing `test_document_analysis_api.py` baseline documented elsewhere in this file, confirmed unrelated); full frontend suite 127/127 passed.

- **2026-08-23: Bilateral `effective_date` presence rule CLOSED (commit `19e6cf1`) -- the scope decision flagged open in this file's own 2026-08-23 entry above.** Master Rules §7's Date Requirement ("The effective date and authorized person details should be checked, but dates that are required to remain blank must remain blank") doesn't resolve cleanly either way on its own; decision (Abdul, 2026-08-23): require presence, not blankness. Accepted, intended consequence: the real template's own unfilled placeholder (`schdule_of_charges_or_bilateral`'s "on this day of , 20 ,") now FAILs this rule rather than passing it. `Conservator_Wildlife_Peshawar_Zoo` (filled preamble) PASSes. No other cached Bilateral samples exist to broaden n=2.
  **Reusable lesson for any future date-field presence rule**: chose `_FieldPresenceRule` (`field_rules.py`) over the seemingly-more-obvious `_DateRule` (`date_rules.py`, the base every other date-presence rule -- `DateIssuePresenceRule`/`DateExpiryPresenceRule` -- already uses), and confirmed the choice empirically before writing any rule code, not by inspection alone: `_DateRule._evaluate_presence()` downgrades to WARNING ("nothing to validate") whenever no `FieldValue` exists at all for the field on that document, and `RegexExtractor` never emits one when its pattern doesn't match -- running `extract_fields()` directly against both real cached samples showed the blank-template sample's result has no `effective_date` key at all, not an empty one. Using `_DateRule` would have silently turned the intended FAIL into a WARNING for the one case this rule exists to catch. `_FieldPresenceRule.evaluate()` treats "no value for this field on this document type" as FAIL directly, which is what was actually wanted. **Check which base class a document-type-scoped field actually reaches "not extracted at all" vs "extracted but empty" through before assuming the same-named base class is the right fit** -- the two behave differently on exactly that boundary.
  Rule count 53->54, `field_presence` category 10->11 (`FieldBilateralEffectiveDatePresenceRule`, `FLD_BILATERAL_EFFECTIVE_DATE_PRESENT`); mechanical count bumps in `test_rule_engine_rules.py`/`test_rule_engine_api.py`/`test_reports_api.py`, 3 new dedicated regression tests. Full backend suite: 856 passed / 12 failed (same pre-existing baseline, +3 net over the last-recorded 853/12 from the 3 new tests plus the batch-3 items below). Zero new ruff/mypy findings vs baseline.

- **2026-08-23: Teammate triage batch 3, partial -- 2 of 6 remaining items closed (commits `88ecb7c` #21, `10aafa8` #23), 4 still open.** Re-verified all 8 items still listed as open in the doc against current code before touching anything (per this session's own repeated lesson about not trusting a doc's word alone): found 2 of the 8 -- **#8** (`useProcessingOverview.js` loading flash) and **#24** (`document.status.toLowerCase()` null guard) -- were already fixed earlier the same day by two commits (`b770551`, `74cf5b5`) that never got reconciled into `docs/TEAMMATE_BUG_TRIAGE.md`'s status table; corrected there now, no code change needed for either.
  **#21 closed (`88ecb7c`)**: the document download route (`/documents/{document_id}/download`) had no application-scoping check, unlike every sibling endpoint -- any authenticated user could download any document from any application by id. Nested under `/applications/{application_id}/documents/{document_id}/download`, `UploadService.download()` now uses the same `_get_application()`/`_get_document_for_application()` pair `delete()` already uses. New regression test confirms a document can't be downloaded via a different application's id. `get_document()` metadata (`/documents/{document_id}`, no download) has the identical unscoped shape but wasn't the triaged item -- left as a related, out-of-scope finding, not fixed here.
  **#23 closed (`10aafa8`), direction reversed from the original "add to backend enum" framing**: investigated adding `REJECTED` to `ValidationStatus` first, per this session's real-evidence discipline, rather than assuming the frontend entry was simply wrong. Grepped every `ValidationStatus.*` assignment across all 8 rule categories -- every `evaluate()` method hardcodes one of `PASS`/`FAIL`/`WARNING`/`PENDING_MANUAL_REVIEW`, zero code paths that could ever produce a fifth outcome. Adding the enum value would have shipped a piece of the data model nothing exercises -- the mirror image of the `SCHEDULE_OF_CHARGES` mistake (a value with no evidence anything sets it, instead of a requirement with no evidence anything satisfies it). Removed the dead frontend `RULE_RESULT_STATUSES` entry instead, same class as the already-fixed #19 `user?.initials` case. `ApplicationStatus.REJECTED` and the `REVIEW_REJECTED` validation-history event are real, separate, application-level concepts and were untouched -- only the misplaced per-rule-result copy is gone.
  **4 genuinely remain open, none started**: #12 (rate limiting on login -- needs a threshold policy decision), #13 (read-then-write race on `application.status`, 3 call sites -- needs a locking-strategy decision), #16 (`_process_bulk_upload` returns the wrong `ProcessingMethod` -- needs a decision on what to honestly return for "split, not yet OCR'd"), and #22 (base `AuthenticationError.status_code` is 500 not 401 -- investigated and confirmed trivial/safe, ready to implement, just not yet committed this batch).

- **2026-08-23: Teammate triage CLOSED -- #16, #13, #12 fixed after the pre-demo audit, bringing the doc to 28/28.** (#22 had already landed separately the same night, `3a45132`, per standing instruction not to give it its own entry.) All three were decided in advance (not discovered mid-fix) and implemented one at a time, diff/tests/ruff/mypy/approval/commit each, same discipline as every other fix tonight.
  **#16 closed (`c6b4a12`)**: `_process_bulk_upload()`'s `DocumentProcessingResult` claimed `processing_method=PADDLE_OCR, raw_text=""` for the bulk-wrapper document, even though this method only splits the PDF into per-type sub-documents -- it never runs PaddleOCR on the wrapper itself (each split document is OCR'd later via its own queued job). Confirmed no code path persists or reads this specific in-memory field before changing it. Now returns honest `None`/`None`; both fields were already `Optional`.
  **#13 closed (`a182fde`)**: `application.status` was advanced to `PROCESSING` via read-then-write at 3 call sites (`upload/services.py`, `bulk_queue/services.py`'s `enqueue_application` and `start_processing`) -- read the status into Python, decide the transition, write it back unconditionally. A concurrent status change (e.g. an operator rejecting the application) committed in that window could be silently clobbered back to `PROCESSING`. New `ApplicationRepository.update_status_if()` does a single conditional `UPDATE ... WHERE id=:id AND status IN (...)` instead; regression test injects a concurrent reject into the exact window between read and write and confirms it fails against the pre-fix code, passes against the atomic update.
  **#12 closed (`76d7209`)**: no protection against unlimited login-guessing against a known account. Added a DB-backed, Postgres-only lockout (no new dependency): 5 consecutive failed attempts locks the account 15 minutes (`MAX_FAILED_LOGIN_ATTEMPTS`/`LOCKOUT_DURATION_MINUTES` in `app/auth/constants.py`), checked/updated inside `AuthenticationService.login()`'s existing transaction, reset on success. A locked account rejects even a *correct* password until the lock expires (new `AccountLocked` exception, 429, with a "try again in N minute(s)" message). New `users.failed_login_attempts`/`locked_until` columns, migration `ae8554d98f13`. **This is now live on the exact login path the demo will use** -- 5 wrong password attempts against any real account (including the seeded `employee`/`operator`/`reviewer`/`it` accounts) locks it for 15 minutes.
  Full backend suite re-run after each of the 3 (and once more after all three): 857 passed, same pre-existing 12-test baseline (`test_document_analysis_api.py`) plus one self-inflicted `test_health.py` artifact from a live worker process running concurrently during the audit, not a regression. Zero net new ruff findings on any changed source file; one net new mypy finding (`application_repository.py`, a `Result.rowcount` typing gap) left consistent with an identical pre-existing unfixed occurrence elsewhere in the codebase (`validation_repository.py:216`) rather than inventing a new pattern -- both are candidates for the next mypy triage pass.

- **2026-08-23: Real, reproducible PaddleOCR native crash found during the pre-demo full-suite audit -- not a regression, not caused by any fix tonight, not investigated further.** Running the full backend suite serially (`pytest -q`) hit a genuine Windows access-violation crash inside PaddleOCR's native inference engine (`paddlex/inference/models/runners/paddle_static/runner.py`), reproducible in complete isolation and non-deterministic about which real-PaddleOCR test it lands on (`tests/integration/test_paddleocr_integration.py`'s several tests, `test_performance_safety.py::test_paddleocr_engine_singleton_is_reused`). This is the same class of bug CLAUDE.md already documents for `PaddleOCREngine`'s lack of internal locking, just triggered here by serial pytest execution on this machine rather than concurrent bulk-queue workers. **Practical takeaway: don't run a live full `pytest -q` during the demo** -- stick to targeted suites (all of which pass cleanly and were used throughout tonight's verification). Not fixed; out of scope for a fix the night before a presentation.

- **2026-08-23: Real-sample audit (`Individual PDFs/`, 9 real single-type onboarding documents, run individually through the live upload/split/analyze/rule-engine pipeline, not from cache) found a genuine Bilateral Agreement the splitter can never recognize -- the literal word "BILATERAL" never appears anywhere in its text.** `schdule of charges or bilateral.pdf` is a real, signed agreement between KPITB and the Khyber Pakhtunkhwa Private Schools Regulatory Authority (KP-PSRA) over the "Paymir" digital payment platform -- confirmed directly against the full 20,440-character OCR'd text: 0 matches for "BILATERAL" anywhere, but 38 matches for "AGREEMENT", 5 for "TRANSACTION CHARGES", 14 for "PAYMIR", 3 for "EFFECTIVE" (the effective-date clause). The document is simply titled "AGREEMENT" on the page, not "BILATERAL AGREEMENT". Since the splitter's `_STRONG_TITLE_PHRASES` match requires that exact phrase, the whole document falls through to `OTHER_SUPPORTING_DOCUMENT` -- and then a second bug compounds it: the `detect_document_type` fallback (the same known `ID_DOCUMENT` collision risk already documented earlier in this file) guesses `ID_DOCUMENT`, extracting nonsense fields (`full_name: 'Account No'`). Not fixed; this is now the **third** known real Bilateral sample (after the two already on file, see the 2026-08-23 `effective_date` entry above) and the first one with zero title-phrase overlap at all, which argues against a narrow phrase-list fix and needs a real product/scope decision on how title-matching should work when a real document just doesn't spell out its own checklist category name.

- **2026-08-23: Same audit found the Formal Request Letter structural fallback shipped earlier today (`91f6600`) still misses at least one real sample -- a single stray OCR character defeats the exact-prefix match.** `on boarding sub biller.pdf` (Individual PDFs/) is a genuine Formal Request Letter -- Govt. Postgraduate College No.1 Abbottabad's Principal writing to KPITB's Managing Director, "Subject: ON BOARDING AS A SUB-BILLER WITH KPITB" -- that should satisfy the `_FORMAL_REQUEST_LETTERHEAD_PREFIX` ("OFFICE OF THE") + `_FORMAL_REQUEST_SUBJECT_PREFIX` ("SUBJECT:") structural fallback added earlier today. It doesn't: PaddleOCR read the subject line as `.Subject:` (a stray leading period from the scan), and `_classify_page`'s check is `text.startswith(_FORMAL_REQUEST_SUBJECT_PREFIX)` -- an exact prefix match, so the leading period silently defeats it and the whole document falls through to `OTHER_SUPPORTING_DOCUMENT`/`UNKNOWN`. Confirmed live through the real pipeline (uploaded, split, OCR'd today; not from cache). **FIXED same night**: `_classify_page` now strips leading punctuation (`string.punctuation` + spaces) off the line before both the letterhead and subject prefix checks, so `.Subject:` matches the same as `Subject:`. New regression test reproduces this exact real sample. `_CHECKLIST_PAGE_MARKERS`'s existing substring-not-prefix choice was deliberately left alone (different, still-valid tradeoff for a different marker) -- this fix is narrowly scoped to leading noise, not a switch to substring matching.

- **2026-08-23/24: Real KP-PPRA bulk upload (reported firsthand by Abdul, reproduced live as application 20307, an 8-file/20-page combined PDF built from 8 of the 9 `Individual PDFs/` files) confirms the splitter title-matching gap is worse in combined uploads than in isolated single-file testing.** The application split into 7 documents (`AUTHORITY_LETTER`, `ACCOUNT_MAINTENANCE_CERTIFICATE`, `CNIC_FRONT`, `BUSINESS_REQUIREMENT_DOCUMENT`, `ONE_LINK_LETTER` ×3) -- Tripartite Agreement, Bilateral Agreement and Formal Request Letter never appeared as their own documents at all. Unlike the individual-file audit (where the same 3 types at least fell through to a visible, unclassified `OTHER_SUPPORTING_DOCUMENT`), here their content was silently absorbed as continuation pages into the neighboring `ONE_LINK_LETTER` groups -- worse, because the data is now mixed into the wrong document rather than merely missing. This is the exact real-world symptom Abdul reported (Validation page showing Tripartite/Bilateral/Formal Request Letter as "Missing/Requested" despite being in the uploaded file) and root-causes to the same known title-matching/continuation-absorption gap already on file above (Bilateral: no literal "BILATERAL" text; Formal Request Letter: needed more real samples before tonight's fix). Not fixed further tonight -- the Bilateral gap specifically still needs a product/scope decision, not a blind code change, per the project's own documented history of overfitting on thin evidence (`CrossBranchCodeRule`/`CrossPeriodRule`). Test application 20307 and its storage files were left in place (not cleaned up) since Abdul was actively reviewing the result at session end.

- **2026-08-23/24: System Logs "dummy data" complaint (IT role, reported by teammate Zarghuna) root-caused and fixed -- 99.97% of audit_log rows in the dev DB are orphaned (`application_id IS NULL`), not fabricated.** `audit_logs.application_id` has `ON DELETE SET NULL` (deliberate: preserves the audit trail when an application is deleted rather than cascading the deletion), and this session's own heavy test-application churn accumulated a large number of orphaned rows that flooded the IT System Logs view with entries no IT reviewer can act on. **Fixed**: `AuditLogRepository.search()`/`_apply_filters()` gained an `include_orphaned` parameter (default `True`, so the repository itself stays behavior-preserving for any other caller); `SystemLogService.search()` flips the default to `False`; the `/system-logs` route exposes `include_orphaned` as an opt-in query param, defaulting to hiding orphaned rows. Frontend: a "Show entries with no linked application" checkbox on the System Logs page, unchecked by default. New backend tests (hides by default / `include_orphaned=true` shows everything) and a frontend test for the toggle. No destructive DB cleanup performed -- the orphaned rows themselves were left alone per Abdul's explicit instruction, this is a display filter only.

- **2026-08-23/24: Bulk queue worker crash mitigation added -- `backend/scripts/run_worker_with_watchdog.py`, a crash-restarting supervisor.** Does not fix the underlying PaddleOCR native crash documented above (compiled third-party internals, judged unsafe to patch blind the night before delivery) -- it wraps `python -m app.bulk_queue` in a restart loop so a crash no longer leaves bulk uploads silently stuck forever with no worker running. Restarts only on nonzero exit (a clean shutdown, exit 0, stops the supervisor too); a safety valve stops restarting after 5 crashes within 2 minutes, on the theory that a worker crashing that fast is a structurally broken environment, not the known intermittent OCR crash this exists for.

- **2026-08-23/24: Two more bug reports from teammate Zarghuna received, deliberately NOT investigated yet -- on hold per Abdul's explicit instruction pending more detail from her.** (1) Reviewer/Operator account cross-visibility ("content of one account is visible in another") -- Abdul is asking her directly whether she was testing in two browser tabs of the same session before any code investigation starts. (2) IT Performance board showing "wrong stats" -- Abdul is asking her for a specific application/number/expected-value example first. Neither has a root cause yet. Recorded here so they aren't lost, not because either is understood.

- **2026-08-24: Pre-demo checklist item -- `python -m app.auth.seed` must be run on whichever machine actually hosts the live demo, before anyone tries to log in as Operator/Reviewer/IT there.** A full live rehearsal tonight (see the three entries below) found this dev DB had only the `employee` account seeded -- `operator`/`reviewer`/`it` logins returned a real `401 Invalid employee credentials` until the seed script was run mid-rehearsal (idempotent, safe, no other accounts touched). Not a code bug, a real environment/setup gap: skipped on the actual demo machine, this would be a flat, unrecoverable login failure live.

- **2026-08-24: Full live demo rehearsal (application 20315, the same combined KP-PPRA file as 20307, run end-to-end through the real API and the real watchdog-supervised worker -- no direct DB/script bypasses) found the Human Review recommendations panel contradicts the operator queue on document completeness.** `GET /applications/20315/human-review`'s `report.recommendations` includes `{"code": "MISSING_REQUIRED_DOCUMENT", "message": "Ensure all required documents are uploaded: ONE_LINK_LETTER, TRIPARTITE_AGREEMENT, BILATERAL_AGREEMENT."}` -- but `GET /validation/applications`'s entry for the same application shows `ONE_LINK_LETTER` with `is_present: true, copy_count: 3`, and the document list has 3 real `ONE_LINK_LETTER` documents (ids 42949-42951, all `COMPLETED`). A reviewer would see two authoritative-looking screens disagree about whether a real, present document is missing. Tripartite/Bilateral belong in that message (genuinely absent, the known absorption bug); only the `ONE_LINK_LETTER` inclusion is the anomaly. Not root-caused tonight -- worth checking first whether the rule generating `MISSING_REQUIRED_DOCUMENT` is actually keying off a required-field extraction gap (2 of the 3 `ONE_LINK_LETTER` copies extracted zero fields, `NEEDS_REVIEW`) rather than document presence, and mislabeling the code/message. Not fixed.

- **2026-08-24: Same rehearsal found the Human Review checklist still asks a reviewer to confirm a "Schedule of Charges signature," even though `SCHEDULE_OF_CHARGES` was removed from the required document checklist on 2026-08-22.** `GET /applications/20315/human-review`'s `checklist` array includes `{"item_name": "Schedule of Charges signature confirmed", "is_checked": false, ...}` alongside the other 14 real items. The 2026-08-22 decision (`6cd94ee`/`433ab2c`, see above) removed `SCHEDULE_OF_CHARGES` as a standalone document type -- it was an unconditional, unsatisfiable rule-engine blocker with zero real samples -- but the fixed 15-item `human_verification` checklist constant was never updated to match, so it still asks about a document type the system no longer collects. **FIXED 2026-08-25 (commit `1f9a8e9`)**: removed both `"Schedule of Charges signature confirmed"` and `"Schedule of Charges stamp confirmed"` from `CHECKLIST_ITEMS` (the stamp line had the identical staleness and was left in by the same oversight), updated the mirrored list in `backend/docs/human_verification.md`, and replaced three hardcoded `== 15` test assertions with `len(CHECKLIST_ITEMS)` so the test suite can't silently drift out of sync with the constant again. 34/34 `test_human_verification_api.py`/`test_human_verification_engine.py` tests pass.

- **2026-08-24: Same rehearsal found the Performance page's `processing_seconds` figure significantly undercounts real wall-clock bulk-upload processing time.** Application 20315: created 20:11:48, reached `PENDING_REVIEW` 21:23:43 -- 72 real minutes through the live watchdog-supervised worker, zero retries, zero crashes. `GET /performance/applications?query=20315` reports `processing_seconds: 869` (14.5 minutes) for the same application -- roughly a fifth of the real figure. Root cause, read directly from the returned `processing_spans`: every span present is a `Processing: DOCUMENT_OCR` span for one of the *split* documents (ids 42946-42953); the wrapper `BULK_UPLOAD` document's own splitting/OCR pass (job id 23170, the ~46-minute stretch before any split document existed at all -- confirmed live via the watchdog log showing repeated PaddleOCR calls against the still-unsplit combined PDF) produces no span of its own. Not fixed; as it stands this number would materially understate real bulk-upload processing time to anyone -- IT, leadership -- using it to judge system performance.

- **2026-08-25: Root cause found (read-only investigation, not fixed) for the ONE_LINK_LETTER-flagged-as-missing anomaly logged above (2026-08-24 entry) -- it is a rule-engine message-mapping bug, not an extraction gap, and fixing it raises a real product question that shouldn't be decided under time pressure.** Direct read-only query of `validation_results` for application 20315 confirms: `DOC_ONE_LINK_PRESENT` is `FAIL` with message `"Document ONE_LINK_LETTER is present more than once (3 documents)"` (`related_document_ids: [42949, 42950, 42951]`) -- the *duplicate* case, not the missing case -- while `DOC_TRIPARTITE_PRESENT`/`DOC_BILATERAL_PRESENT` are `FAIL` with `"Required document ... is missing"` -- the genuine missing case (the known absorption bug). `_DocumentPresenceRule.evaluate` (`app/rule_engine/rules/document_rules.py:27-47`) fails a document-presence rule identically for "zero present" and "more than one present," and `DOCUMENT_TYPE_BY_RULE`/`_findings()` (`app/reports/constants.py:97-105`, `app/reports/services.py:486-490`) bucket any failure of these rule ids into the same `missing_document_types` list and one fixed template ("Ensure all required documents are uploaded") regardless of which of the two reasons fired -- so a reviewer sees "go upload this" for a document that is present three times over. Open product question, not yet decided: `backend/tests/preprocessing/test_document_splitter.py` already has a fixture built around "multiple copies of 1-Link," meaning the splitter itself expects and tracks (`copy_count`) multiple ONE_LINK_LETTER copies as a normal real-world case -- but `_DocumentPresenceRule` still hard-fails `len(documents) > 1` uniformly for every required document type, ONE_LINK_LETTER included. Whether "exactly one" is even the correct rule for this document type is a scope decision, not a bug fix. Not fixed -- deliberately deferred for the same reason as Tripartite/Bilateral absorption: this project has already been burned twice (`CrossBranchCodeRule`/`CrossPeriodRule`) rushing rule changes on real-sample evidence gathered under deadline pressure.

- **2026-08-25: Real RBAC gap found and fixed -- `create_application`, `upload_document`, `replace_document`, `delete_document` and `bulk-upload` had no role restriction at all; any authenticated role (including Reviewer and IT) could create and upload applications.** Confirmed in practice: application 20316 was created by a `Reviewer`-role account. Per department decision, only `EMPLOYEE` (all-access by design) and `OPERATOR` should be able to create/upload/replace/delete -- Reviewer and IT should get `403`. Fixed in `backend/app/upload/routes.py`: all five endpoints now depend on `require_role(ROLE_OPERATOR)` instead of the bare `get_current_user`, matching the pattern already used in `app/operator_workflow/routes.py`. Live-tested against all four roles post-fix: Employee/Operator `201`, Reviewer/IT `403`. Backend suite fallout: 23 tests across `test_system_logs_api.py`, `test_performance_api.py`, `test_application_history_api.py` and `test_operator_workflow_api.py` were using an IT/Reviewer client purely to create a throwaway application for setup (now correctly rejected) -- fixed by adding `create_application_direct()` to `tests/test_technical_validation_api.py` (creates the row straight through `ApplicationRepository`, bypassing HTTP/role entirely) rather than adding a second role-client fixture, because every role-client fixture (`it_client`/`reviewer_client`/`employee_client`) wraps the *same* underlying `TestClient`/cookie jar -- requesting two of them in one test makes the second login silently overwrite the first, which is itself worth knowing if this pattern is reused elsewhere. Full suite: 843 passed (867 with the 12 pre-existing `test_document_analysis_api.py` failures and the 1 known `test_health` environmental artifact still present, both unrelated to this change).

- **2026-08-25: AMC/Tripartite absorption confirmed directly on a live application (20316, "TMA Khal Dir Lower") and root-caused as two distinct bugs, not one.** The operator queue reported `ACCOUNT_MAINTENANCE_CERTIFICATE`, `BILATERAL_AGREEMENT`, `CNIC_FRONT` and `TRIPARTITE_AGREEMENT` all missing. Direct inspection of the real split documents and their OCR text: **AMC is genuinely present**, misclassified as `ONE_LINK_LETTER copy1` (document 42957) -- its content is unmistakably an Account Maintenance Certificate form (`Organization Name`, `NTN/Registration/Incorporation No.`, `Bank Account Number`, `Title of Account`, `Name of the Bank and branch`, `Number of Directors`), but its own title line OCR'd too badly (`"AEm (UD m along prth pompamg stamp Danutm"`) to strong-match `_STRONG_TITLE_PHRASES`' AMC entry -- a one-off severe OCR-noise problem on this specific scan, not the systemic bug below, and not fixed (no generalizable pattern found yet across other real AMC samples, consistent with the project's standing rule against single-sample-driven extractor changes). **Tripartite is also genuinely present**, misclassified as `ONE_LINK_LETTER copy2`/`copy3` (documents 42960-42961) -- this one *is* systemic, see the dated entry directly below. CNIC's absence was not investigated further tonight (out of scope for this pass).

- **2026-08-25: Systemic splitter misclassification found and fixed (department decision) -- Tripartite Agreement's real-world instrument is a stamp paper identified by its signing parties, not by any literal title, and was being routed to the wrong document type on nearly the entire real corpus.** `_FULL_PAGE_STRONG_PHRASES`' `"PARTICIPATION MEMORANDUM"` entry mapped to `DocumentType.ONE_LINK_LETTER`, a 2026-08-19 department decision made at the time this was still an open question (see the "1-Link Letter checklist-meaning mismatch" line in this file's Known Gaps section, and `OneLinkLetterExtractor`'s own docstring). Tonight's correction: a "PARTICIPATION MEMORANDUM FOR BILLER/SUB-BILLER/BILL AGGREGATOR MEMBERS" stamp-paper instrument **is** the Tripartite Agreement (1LINK, the department and KPITB as the three signing parties -- confirmed by its own "1- For & Behalf of / 2- / 3-" signature block on every real sample checked), not a 1-Link Application Form. Grepped every real cached OCR sample in `Confidential Data/.ocr_cache/` carrying this phrase -- GDA Abbotabad, GDC Madyan Swat, TMA Khal Dir Lower, TMA Lal Dir Upper, TMA Lal Qilla Dir Lower, TMA Samarbagh, TMA Thall Hangu, and one file literally named `tripartite` -- **zero counterexamples**; every one shows the same three-party marker. Fixed: `_FULL_PAGE_STRONG_PHRASES` now maps `"PARTICIPATION MEMORANDUM"` to `TRIPARTITE_AGREEMENT`. Separately, per the same department decision: **`SCHEDULE_OF_CHARGES` is not a distinct real-world document -- it is the same document as `BILATERAL_AGREEMENT`** (consistent with the 2026-08-22 finding that its content overlaps Bilateral's own Transaction Charges section, and directly evidenced by a real source file in `Confidential Data/` literally named `schdule_of_charges_or_bilateral`). Fixed: `_STRONG_TITLE_PHRASES`' `SCHEDULE_OF_CHARGES` entry (`"SUB-BILLER AGREEMENT"`, `"SUBBILER AGREEMENT"`, `"SUB BILLER AGREEMENT"`, `"SCHEDULE OF CHARGES"`) merged into the `BILATERAL_AGREEMENT` entry; the standalone entry removed. Both `TripartiteAgreementExtractor` and `BilateralAgreementExtractor` already exist and are already real-sample validated against exactly this content, so extraction should mostly already work once routing is correct -- this was a splitter bug, not an extraction gap. Tests: 3 splitter tests updated to the new (correct) expected types, `test_document_splitter.py` 40/40 passing; corpus-wide dry-run classification of all 27 real cached files containing either phrase confirms zero still land on the old, wrong types.

- **2026-08-25: `ONE_LINK_LETTER`-flagged-as-missing anomaly (2026-08-24/25 entries above) fixed at the message-mapping level, without touching the "should a duplicate even fail" policy question.** That policy question (whether `_DocumentPresenceRule` should hard-fail `len(documents) > 1` for every required type, ONE_LINK_LETTER included) is still an open, deliberately-deferred scope decision -- not decided today, same reasoning as the Tripartite/Bilateral discriminator below. What *was* a genuine, uncontroversial bug: a document present 3 times over was reported to a reviewer with the identical message and bucket as a document present 0 times ("Ensure all required documents are uploaded"), which is misleading regardless of how the policy question is eventually settled.
  Fixed in `app/reports/constants.py` (`DUPLICATE_MESSAGE_MARKER = "is present more than once"`, matching `_DocumentPresenceRule`'s own message text; new `DUPLICATE_REQUIRED_DOCUMENT` recommendation code/template, ordered right after `MISSING_REQUIRED_DOCUMENT`), `app/reports/services.py::_findings()` (splits `DOC_*_PRESENT` FAILs into `missing_document_types` vs a new `duplicate_document_types` by checking each row's own message for the marker, rather than lumping every FAIL of these rule ids together), and `app/reports/validators.py::build_recommendations()` (new code wired into `_FLAGS`). The underlying rule's PASS/FAIL/message logic (`_DocumentPresenceRule.evaluate`) is untouched -- this is a report-presentation fix, not a rule-engine change. `findings` isn't part of any Pydantic response schema (only `recommendations` is), so this was purely additive -- no schema/migration/frontend change needed; `ReportIssues.jsx` already renders recommendations generically by `code`/`message`.
  2 new tests: a pure unit test (`test_reports_engine.py::test_recommendations_duplicate_document_is_distinct_from_missing`) and a real end-to-end integration test (`test_reports_api.py::test_report_duplicate_document_recommends_duplicate_not_missing`, builds a full application then adds a second real `ONE_LINK_LETTER` document and confirms the report recommends `DUPLICATE_REQUIRED_DOCUMENT` for it, not `MISSING_REQUIRED_DOCUMENT`). Ruff/mypy: zero new findings on every changed file, confirmed via stash-diff against the pre-existing baseline (32 ruff findings, 0 mypy findings in `app/reports`, both files unchanged before/after).

- **2026-08-25: Deliberately not attempted -- automatically telling Tripartite and Bilateral apart by signing-party count.** The obvious next step (2 parties = Bilateral, 3 = Tripartite, decided from the stamp paper itself) turned out not to be safe: the stamp paper's own "First Party"/"Second Party" fields are a fixed template present on *every* real sample checked, Tripartite and Bilateral alike (confirmed on both real Bilateral samples -- Conservator Wildlife Peshawar Zoo and a second department -- and 4 independent real Tripartite samples) -- they cannot discriminate the two, disproving the discriminator before it was written. Both real Bilateral samples are real, working, and already extractor-validated (`BilateralAgreementExtractor`, 2026-08-22), but are currently reached only via `OTHER_SUPPORTING_DOCUMENT`/absorption, not any automatic classification. A genuine structural signal that reliably distinguishes a standalone Bilateral document from a Tripartite document's own embedded PAYMIR-agreement section (confirmed present as a *sub-section* inside at least one real Tripartite sample, `TMA_Thall_Agreement.docx`) has not been found yet. Needs more real samples and real page-boundary analysis, not a guess under deadline pressure -- logged here rather than rushed, same standing reasoning as the ONE_LINK_LETTER/CrossBranchCodeRule entries above. **Superseded 2026-08-26, see the dated entry below**: the party-count discriminator this entry rejected is still correctly rejected, but a different, safer mechanism (title matching, not party counting) closed the actual routing gap, and the "embedded sub-section" reading of `TMA_Thall_Agreement.docx` above turned out to be a misreading -- it's a second, complete, independent instrument, not a sub-section.

- **2026-08-25: Performance page `processing_seconds` undercount (2026-08-24 entry above) root-caused and fixed -- `started_at` was never a stable measure of when a job actually began.** `QueueJobRepository.heartbeat()` periodically overwrites a `PROCESSING` job's `started_at` as a liveness lease (so `recover_stale_processing` never falsely declares a live-but-slow worker crashed) -- entirely correct for that purpose, but `app/performance/services.py::_processing_spans()` was also using `started_at` as the span's start time for computing `duration_seconds`. On a long-running job that receives at least one heartbeat, this meant the reported duration only covered the time since the *last* heartbeat, not the true elapsed time since the job was claimed -- exactly matching the 869s-vs-72-real-minutes gap found on application 20315's bulk-split job.
  Fixed with a new immutable `claimed_at` column (migration `c7d8e9f0a1b2`, `queue_jobs.claimed_at`, nullable): `QueueJobRepository.claim_next()` now sets it once alongside `started_at` at claim time, but nothing else ever touches it -- `heartbeat()` still only updates `started_at`, so stale-job recovery is byte-for-byte unchanged. `_processing_spans()` now uses `job.claimed_at or job.started_at` as the span start -- the `or` fallback means rows written before this migration (where `claimed_at` is permanently `NULL`) keep exactly their pre-fix behavior rather than silently dropping out of the report. Real dev DB migrated (`alembic upgrade head`, confirmed applied).
  2 new regression tests in `test_performance_api.py`: one simulates a job whose `started_at` drifted to 90 seconds before completion (heartbeat-refreshed) while `claimed_at` correctly records the job started 46 minutes earlier, and asserts `processing_seconds == 46*60` (would have been 90 before the fix); the other confirms a job with no `claimed_at` set (the pre-migration case) still reports its `started_at`-based duration unchanged. `_add_queue_job()` test helper (`test_application_history_api.py`) gained an optional `claimed_at` parameter, default `None`, so every existing caller is unaffected. Full suite, ruff and mypy all confirmed at the identical pre-existing baseline (zero new findings, same 13-failure baseline) before this was committed.

- **2026-08-25: All 12 pre-existing `test_document_analysis_api.py` failures root-caused and fixed -- a too-sparse test fixture, not a product bug.** These had been standing since before this session and were repeatedly logged as "known, pre-existing, unrelated to real usage" without being root-caused. They were: `PLAIN_TEXT` (a single ~140-character line rendered onto an otherwise-blank A4 page by `make_text_pdf_bytes`) covers well under `BLANK_INK_RATIO` (0.5%) of the rendered page in ink -- confirmed directly (0.328% measured, `is_blank_image` returns `True`) -- so technical validation's blank-page check (`app/technical_validation/utils.py::is_blank_image`) failed the document before OCR ever ran, and every test using `PLAIN_TEXT` got `"No OCR result found for the document; run document processing first"` instead of the outcome it was actually asserting on. Not a splitter/extractor/rule-engine bug and not something any real onboarding scan would ever trigger -- real documents have far more content per page. `test_confidence_api.py` had already hit and worked around the identical issue earlier (`UNRELATED_DENSE_TEXT`, explicitly commented "to avoid tripping the unrelated pre-existing blank-page technical-validation regression"), which is what led to finding the same root cause here.
  Fixed by rewriting `PLAIN_TEXT` into a denser, realistic multi-line letter body (0.846% ink, comfortably clear of the threshold) -- wording/intent unchanged, still matches none of `detect_document_type`'s 4 keyword categories. 11 of the 12 tests were fixed by this alone.
  The 12th (`test_analyze_recognized_checklist_type_stores_real_type_not_unknown`) uncovered a second, independent, genuine staleness once the blank-page failure stopped masking it: it used `DocumentType.ONE_LINK_LETTER` as its example of "a recognized checklist type with no extractor yet," but `OneLinkLetterExtractor` was added to `_EXTRACTORS`/`_CHECKLIST_TYPE_MAP` since this test was written -- so the type it exercises now takes the real-extraction path (`confidence_score=0.2`, not `None`) instead of the `_persist_recognized_unsupported_type` stub path the test was actually testing. Checked whether the stub path is still reachable at all before picking a fix: every one of the 8 types in `_CHECKLIST_TYPE_MAP` now has a matching `_EXTRACTORS` entry, but `DocumentType.CNIC_BACK` (excluded from both maps by the standing zero-real-samples decision, see the 2026-08-17 CNIC scope entry above) still genuinely exercises the stub path -- swapped the test to use it instead of inventing a fix around a scenario that no longer exists for any real checklist type.
  Full suite re-run after both fixes: 882 passed / 1 failed (`test_health`, the one remaining known environmental artifact) -- exactly the prior 870 plus these 12, confirmed not assumed. Ruff: identical pre-existing 15-finding baseline on the changed file (stash-diff confirmed). No production code touched -- this was entirely a test-fixture fix.

- **2026-08-26: Bilateral Agreement given a real title match, closing the routing gap the 2026-08-25 "deliberately not attempted" entry above left open -- via a different, safer mechanism than the one that entry correctly rejected.** That entry was about discriminating Tripartite from Bilateral by signing-party count, and was right to reject it (generic "First Party"/"Second Party" fields on every stamp paper, both types alike). This is not that: it's matching the Bilateral document's own real title text directly, the exact mechanism already proven safe for Tripartite's `PARTICIPATION MEMORANDUM` fix two days ago -- not a discriminator, since the two types' real titles share no text at all.
  Re-checked the full real corpus (`Confidential Data/.ocr_cache/`, 65 files) before writing any code, per this project's standing norm: the literal word "BILATERAL" still never appears on either real Bilateral sample (confirmed 2026-08-23), but both open with the identical literal heading "AGREEMENT" / "FOR DIGITAL PAYMENT COLLECTION VIA PAYMIR" / "BETWEEN" / "Khyber Pakhtunkhwa Information Technology Board" / "AND" / <counterparty>. Grepped "FOR DIGITAL PAYMENT COLLECTION VIA PAYMIR" (the distinctive second line) across all 65 cached files: exactly 3 matches -- the two known standalone Bilateral samples (Conservator Wildlife Peshawar Zoo, `schdule_of_charges_or_bilateral`) plus `TMA_Thall_Agreement` -- and zero of the other 62, including 10 genuine Tripartite-only samples that mention "Paymir" only as incidental defined-terms prose ("the Paymir Digital Payment Platform...") without ever carrying this exact heading. "AGREEMENT" alone was deliberately not used as the anchor: it's also the literal e-stamp Description field on every one of these stamp papers regardless of type (confirmed on both Bilateral samples and 3 Tripartite samples), which would false-positive on every stamp-paper cover page.
  **`TMA_Thall_Agreement` is the useful case, and the 2026-08-25 entry's reading of it was wrong.** That entry described this file's Bilateral content as an "embedded PAYMIR-agreement section" inside one Tripartite document -- implying it should stay merged, not split out. Rereading the actual cached text (no page markers in this cache entry, but the content itself is unambiguous): it is not a clause or sub-section, it is a second, complete, independent Agreement -- its own "made and entered into on this ___ day of ____, 20__" preamble, its own First Party/Second Party definitions, its own WHEREAS section, appearing immediately after a separate Authority Letter and a separate Participation Memorandum in the same combined source file. This is exactly the kind of real multi-instrument bundling the splitter exists to pull apart (the same shape as every other bulk-PDF splitting case in this codebase), not a reason to keep two distinct agreements merged into one Tripartite record.
  Fixed: `app/preprocessing/splitter.py`'s `_FULL_PAGE_STRONG_PHRASES` gained `(DocumentType.BILATERAL_AGREEMENT, ("FOR DIGITAL PAYMENT COLLECTION VIA PAYMIR",))`, in the same header-zone-exempted list as Tripartite's entry and for the same reason (on `schdule_of_charges_or_bilateral` this title sits behind the same STAMPING/verification/payment-fields boilerplate block). Validated with a before/after whole-corpus classification diff (not just a grep) across all 65 cached files: exactly 2 files changed, both the two standalone Bilateral samples going from unclassified (`None` -> falls through to `OTHER_SUPPORTING_DOCUMENT`) to correctly `BILATERAL_AGREEMENT`; zero of the other 63 changed, including `TMA_Thall_Agreement` itself (its whole-file classification stays `TRIPARTITE_AGREEMENT`, since `_FULL_PAGE_STRONG_PHRASES` checks Tripartite's entry first and this is a single-blob classification, not a per-page one -- real per-page splitting is what will actually separate its two agreements once it goes through the real bulk pipeline).
  3 new tests in `tests/preprocessing/test_document_splitter.py`: the Tripartite-mirroring case (Bilateral title past boilerplate, own group), and a bundled-instrument case modeled directly on `TMA_Thall_Agreement`'s real structure (Authority Letter, then Participation Memorandum, then this new Bilateral title, in one source) asserting all three split into three separate documents. `BilateralAgreementExtractor` already exists and is already real-sample validated against exactly this content (2026-08-22) -- this was purely a routing gap, extraction should already work correctly once a document reaches it. Full backend suite, ruff and mypy confirmed at the identical pre-existing baseline (zero new findings) before committing.
  **Still not attempted, and still correctly not attempted**: nothing here discriminates Tripartite from Bilateral when a page carries neither title (there is no such real case on file) or, hypothetically, both on the same page (not observed on any of the 65 real files) -- the party-count approach remains rejected for that scenario, and no new evidence changes that.

- **2026-08-26: Checked whether fixing Bilateral's routing unblocks Master Rules §3's AMC↔Tripartite/Bilateral branch-name consistency rule (line 38 above, "still blocked, not built"). It does not -- the rule was blocked on two independent fronts, and only one of them was ever about routing.** That entry named two blockers: Tripartite's `branch_code` being unfilled on its one real sample, and Bilateral's "apparent total absence of any branch field" -- the second reads ambiguously as either "routing never reached Bilateral" or "the field genuinely isn't there." Now that Bilateral routes correctly, re-checked which it actually is by grepping "branch" directly in both real Bilateral samples' cached OCR text (not re-deriving from the extractor, which never had a `branch_code` field to begin with -- confirmed by reading `BilateralAgreementExtractor`'s own field list, `organization_name`/`platform_name`/`transaction_charges`/`account_holder`/`account_number`/`iban`/`effective_date`, no branch field).
  Conservator Wildlife Peshawar Zoo: zero mentions of "branch" anywhere -- its bank-details table's "Bank Name" column is a bare `"Allied Bank"`, no branch name of any kind. `schdule_of_charges_or_bilateral`: two mentions, but both are the same "Bank of Khyber, Civil Secretariat Branch" string sitting *inside* the free-text "Bank Name" table column, not a separately labeled "Branch:"/"Branch Code:" field the way Tripartite's own `branch_code` pattern expects (`r"(?:Branch Code|Branch)\s*:\s*(.+)"`). So it's the second reading: real absence, not a routing artifact. The two real samples don't even agree with each other on whether a branch name appears at all, let alone in a matchable, labeled form.
  **Conclusion: still correctly blocked, for the original field-availability reason, now confirmed rather than assumed.** Routing was fixed; that was real progress, but it doesn't manufacture structured data that was never in the source documents. Building this rule would still mean guessing a parsing strategy against zero real confirmed-filled samples on either side -- exactly the `CrossBranchCodeRule`/`CrossPeriodRule` mistake this file has warned against all along. Not built. Needs a real sample where either document type states a branch code/name in a genuinely labeled, parseable way before this is safe to attempt.

- **2026-08-26: Bilateral Agreement splitter fix confirmed end-to-end on a real application through the real running API, not just the corpus dry-run above.** The dry-run only proves the classification function returns the right enum value in isolation; it doesn't prove a document actually reaches `BilateralAgreementExtractor` through the real bulk-split -> queue -> OCR -> analysis chain. Re-ran `Individual PDFs/_combined_kp_ppra_test.pdf` (the same file that produced application 20307, no longer in the DB after the session's data wipe) as a fresh application (20322) via the live backend, the real watchdog-supervised worker, and a real HTTP client (login as Employee, create application, bulk-upload, `processing/start`) -- no direct DB/script bypasses.
  Split into 9 documents (previously 7 on this exact file before tonight's fixes -- Tripartite, Bilateral and Formal Request Letter were the 3 missing pieces, all 3 now present): `AUTHORITY_LETTER`, `ACCOUNT_MAINTENANCE_CERTIFICATE`, `CNIC_FRONT`, `ONE_LINK_LETTER` x2, `TRIPARTITE_AGREEMENT`, `BILATERAL_AGREEMENT`, `FORMAL_REQUEST_LETTER`, `BUSINESS_REQUIREMENT_DOCUMENT`. All 9 reached `COMPLETED` OCR status and the application reached `PENDING_REVIEW` automatically (`PipelineRunnerService`'s auto-chain), no manual per-stage calls.
  The Bilateral document (id 42978) has a real, non-stub `document_analysis_results` row: `confidence_score=0.771`, `verification_status=PARTIALLY_VERIFIED`, and genuine `BilateralAgreementExtractor` output -- `iban`/`account_number`: `PK51KHYB0015002000883401`, `platform_name`: `"Paymir"`, `organization_name`: `"Khyber Pakhtunkhwa Private Schools Regulatory Authority"`, and the full 7-tier PKR `transaction_charges` fee table -- this is genuinely the `schdule_of_charges_or_bilateral` content (matches its real IBAN/organization/branch text), confirming the combined test file does carry it and it now reaches real extraction, not just correct routing. `account_holder` is honestly absent, matching the already-documented structural miss (no Account Title column in this table shape) -- expected, not a regression.
  **One live-only observation, not a regression from this fix**: this run's `transaction_charges` capture trails off into what reads like an unrelated stamp-paper boilerplate block ("...CamScanner\n\n--- Page 5 ---\n\nSTAMPING...") past the real fee table, rather than stopping cleanly at "6. DEPOSIT & DISBURSEMENT OF FUNDS" the way the two originally-validated samples do. Not investigated further tonight (out of scope for a routing fix) -- worth a closer look at whether this document's real page range genuinely continues past the fee table on this particular source file, or whether the boundary regex needs a second real sample to tighten against.
  Application 20322 and its documents left in place (not cleaned up), same convention as application 20307 before it, in case anyone wants to inspect it directly.

- **2026-08-26: `BilateralAgreementExtractor.transaction_charges` trailing-boilerplate bug (the "one live-only observation" flagged in the entry above) root-caused and fixed -- it was latent on the two originally-validated real samples all along, just never observed because the OCR cache tool's own page-join marker accidentally matched where a real page-join marker was needed.** Pulled document 42978's actual `raw_ocr_text` from the DB (application 20322, still in place) and diffed it against the cached `schdule_of_charges_or_bilateral` sample it's the same physical document as: both genuinely contain a second, complete e-stamp/STAMPING cover page bound mid-Section-5, between the real fee table and "6. DEPOSIT & DISBURSEMENT OF FUNDS" -- a real, expected structural feature of Pakistani stamp-paper agreements (multiple stamp-duty sheets bound into one document), not scan noise or a one-off artifact. The `transaction_charges` pattern's own docstring already predicted this exact failure mode ("the header alone would still capture the whole intervening stamp page as noise") and believed a second stop condition -- the literal string `"--- page break ---"` -- prevented it.
  That string is `backend/scripts/ocr_cache.py`'s own page-join marker (`"\n--- page break ---\n".join(page_texts)`, line ~111), written only by the offline caching tool for `Confidential Data/.ocr_cache/*.txt` files -- never by production code. What `DocumentAnalysisService.analyze()` actually receives as `document.raw_ocr_text` is joined by `app.document_processing.constants.PAGE_SEPARATOR` (`"\n\n--- Page {page_index} ---\n\n"`, formatted with a real page number, e.g. `"\n\n--- Page 5 ---\n\n"`) via `document_processing/processors.py::_join_pages` -- a completely different string. The 2026-08-22 validation ran `extract_fields()` directly against the two cached `.txt` files (which do carry the cache tool's marker), so it accidentally worked there and was never run against anything shaped like real `raw_ocr_text` -- confirmed by re-running the pre-fix code against both: `schdule_of_charges_or_bilateral` (cached format) correctly stopped before the stamp page even before today's fix (length 412, no `STAMPING`); document 42978's real text (production format) did not (length 1309, `STAMPING` present). This is the mirror image of the `CrossBranchCodeRule`/`CrossPeriodRule` mistake this file has warned about all along -- not overfitting to a thin real sample, but validating against a fixture-only artifact that never reflects real production input.
  Fixed in `app/document_analysis/extractors.py`: a new module-level `_PAGE_SEPARATOR_PATTERN`, derived from the real `PAGE_SEPARATOR` constant by splitting it on its `{page_index}` placeholder and `re.escape`-ing each literal half around a `\d+` (so the pattern can never silently drift from the real constant), added as a *third* alternative in the `transaction_charges` stop-condition lookahead -- alongside, not instead of, the existing all-caps-header condition and the cache-tool marker. Both markers are kept because both are genuinely still needed: the cache-tool marker for anything validated directly against `Confidential Data/.ocr_cache/*.txt` (this project's established real-sample validation workflow), the new pattern for real `raw_ocr_text`.
  **Verified safe and generalizable across all 3 real samples, not shipped from the live sample alone**: Conservator Wildlife Peshawar Zoo (no intervening page at all) is byte-identical before and after (857 chars, unaffected either way -- its trailing prose reaches "6. DEPOSIT..." with no page boundary in between); `schdule_of_charges_or_bilateral` (cached format) is byte-identical before and after (412 chars, `--- page break ---` already handled it); document 42978 (real production format) goes from 1309 chars with full stamp-page noise to 412 chars, clean of `STAMPING`/`Non-Judicial`/`Vendor Information`/the signatory block -- matching the cached sample's own clean length almost exactly, since they're the same underlying document. Document 42978's real `raw_ocr_text` cached to `Confidential Data/.ocr_cache/combined_kp_ppra_test__BILATERAL_AGREEMENT_copy1.txt` (gitignored, not committed, same as every other cache entry) as a permanent third real regression sample -- the first one in this project's cache carrying the real production `PAGE_SEPARATOR` format rather than the cache tool's own marker.
  **One small, separate, pre-existing cosmetic tail deliberately left alone**: the fixed value still ends with `"CS\nCamScanner"` -- a known CamScanner scanner-app watermark (already documented elsewhere in this file, `preprocessing/splitter.py`'s `_MIN_NATIVE_TEXT_CHARS` comment) that appears at the end of scanned pages from this app. Two lines, not the reported bug (an entire second document's worth of STAMPING/party/signatory noise), predates this fix, and stripping it was out of scope for what was actually reported -- noted here rather than silently left in place unremarked.
  **Checked and confirmed not a shared-code issue**: grepped the whole `extractors.py` file for both the cache-tool marker and `re.DOTALL` before touching anything -- both occurred exactly once, only in this one pattern. `TripartiteAgreementExtractor` and `AccountMaintenanceCertificateExtractor`'s shared `_extract_bank_account_block` parser is architecturally immune to this bug class: it's a discrete line-by-line structural scanner (splits text into lines, pattern-matches specific labeled values), not an open-ended `(.*?)...(?=stop-marker)` greedy span capture -- there is no unbounded capture in that code path for a page boundary to corrupt.
  **PaddleOCR non-determinism, checked and it's real but not the cause here**: the live and cached OCR of the same physical stamp-paper cover page genuinely differ in exact wording/line count in several places (e.g. "Yerily Your Stamp Paper" vs "Verify Your Stamp Paper", a dropped "3. You will be redirected..." instruction line) -- confirmed by direct diff, not assumed. This is real recognition variance run-to-run, worth keeping in mind for anything that pattern-matches this boilerplate block specifically, but it did not cause or mask this particular bug: the actual fee-table content and the "6. DEPOSIT..." boundary text are identical in both runs, and the bug is in which separator string the boundary regex looked for, not in what either OCR pass actually read.
  4 tests updated/added in `tests/test_document_analysis_engine.py`: tightened `test_extract_bilateral_agreement_fields`'s `transaction_charges` assertion from a substring check to an exact match (the substring check is exactly why this went uncaught for 5 days); new `BILATERAL_AGREEMENT_TEXT_WITH_INTERVENING_STAMP_PAGE` fixture (synthetic, fabricated placeholder values, real `PAGE_SEPARATOR` structurally mirroring both real samples) and `test_extract_bilateral_agreement_transaction_charges_stops_before_intervening_stamp_page`, asserting the exact expected value and the explicit absence of `STAMPING`/`Non-Judicial`/`Vendor Information`/`Sample Vendor`. Not run against the corpus dry-run script used for the splitter fixes above -- this change is entirely inside `document_analysis/extractors.py` field-extraction logic, doesn't touch `preprocessing/splitter.py` classification at all, so it cannot affect document-type routing; the dry-run measures routing, not extraction, so it isn't the right tool here and running it would only confirm zero output change, which the splitter's own untouched-file status already guarantees.
  Ruff/mypy: identical pre-existing baseline on both changed files (94 ruff findings, 4 mypy findings, confirmed via stash-diff, zero new). Full backend suite: 885 passed / 1 failed (the known `test_health` environmental artifact, unrelated) -- exactly the prior 884 plus this fix's one new test, zero regressions.
  **Honest status, not overclaimed**: this is closed for the structural bug (real page-boundary marker mismatch, evidenced across all 3 real samples, generalizable, not overfit) but not for OCR non-determinism -- if PaddleOCR reads this same stamp-paper boilerplate block differently on a future run (already observed once, see above), the exact trailing content could vary even though the fix's actual stop condition (a real page separator, not fragile text matching inside the boilerplate) does not depend on that wording at all and will keep working regardless. The `PAGE_SEPARATOR` marker itself is a stable, engine-inserted structural constant, not OCR output -- that's what makes this fix safe against exactly the non-determinism observed, not something the fix had to get lucky about.

- **2026-08-26: Audited the whole backend for the same bug class as the `transaction_charges` fix above (a stop-condition validated against `scripts/ocr_cache.py`'s cache-only page-join marker instead of the real production `PAGE_SEPARATOR`) -- found it nowhere else, and fixed one small separate leftover from that fix along the way.** Scope, per the request: (1) grep the whole backend for the literal `"page break"` outside `ocr_cache.py` and its own tests; (2) grep `extractors.py` for every open-ended span/boundary capture, not just ones already mentioning that string; (3) check what every extractor's own test suite is actually validated against (hand-written literal fixture vs. cache file vs. real live `raw_ocr_text`).
  (1) Exactly 3 files contain `"page break"`: `scripts/ocr_cache.py` itself (the marker's origin), `tests/test_document_analysis_engine.py`, and `extractors.py` -- both of the latter only via the already-fixed `transaction_charges` pattern (the literal marker kept intentionally alongside the new `PAGE_SEPARATOR`-derived pattern, per that fix's own reasoning) and a comment in the test file. No other file references it at all.
  (2) Grepped `extractors.py` for `re.DOTALL` and open `.*?`-style spans: `re.DOTALL` appears exactly once in the whole file -- the already-fixed `transaction_charges` pattern. Every other multi-line-*looking* capture in the file is actually bounded, not marker-dependent, so none of them can hit this bug class: `BilateralAgreementExtractor.organization_name` and `AuthorityLetterExtractor.organization_name` allow at most one extra `\n` inside the capture group (`[^,\n(]+(?:\n[^,\n(]+)?`), not open-ended; `BusinessRequirementDocumentExtractor.revenue_services_listed` is capped at exactly 7 following lines (`(?:\n[^\n]+){0,7}`) regardless of content, never scanning for a stop marker at all; `TripartiteAgreementExtractor.account_number`'s `.*?` has no `re.DOTALL`, so `.` structurally cannot match `\n` and the capture is single-line no matter what. `_extract_bank_account_block` (the shared structural parser behind Tripartite/AMC/Bilateral's account fields, already checked once during the original fix) is a discrete line-by-line scanner with its own stop rules (next recognized label, blank line, paren-balance) -- confirmed again here it contains no reference to any page-break marker of either kind. **Conclusion: the wrong-marker bug class is fully isolated to the one instance already fixed; there is nothing else in `extractors.py` capable of exhibiting it.**
  (3) Every one of the 12 `_EXTRACTORS` types' automated tests in `tests/test_document_analysis_engine.py` (and the splitter's own tests) run against hand-written literal Python string fixtures -- confirmed by grepping the whole `tests/` directory for `get_ocr_text` (the only function that actually loads a `.ocr_cache/*.txt` file at runtime): it has zero callers anywhere under `tests/`, only inside `scripts/ocr_cache.py` itself. So **no extractor's automated suite falls into the cache-file bucket either** -- real-sample validation for every extractor in this codebase's history (documented at length in every extractor's class docstring) has been a manual, ad-hoc process during development: read a real cached `.txt` file, hand-transcribe the relevant real shape into a literal fixture, assert against that. This is why the original `transaction_charges` bug happened -- there was no automated test of any kind that ever loaded a real cache file, so the fixture bucket (bucket "a" per this audit's own framing) and the cache-file bucket (bucket "b") collapse into the same thing here: **every extractor test in this codebase is bucket "a," including the ones that predated this fix.** That gap is real and worth naming plainly, but per (1)/(2) above it is not currently hiding a live bug the way it did for `transaction_charges`, because no other extractor has a marker-dependent or otherwise open-ended stop condition for it to go uncaught on. Logged here as a standing coverage gap rather than invented busywork: a future extractor change that *does* introduce an open-ended, marker-dependent capture (the same shape `transaction_charges` had) would have nothing in the automated suite to catch it either, the same way this one didn't, until it's specifically built to load real cache text (`get_ocr_text`) rather than a hand-transcribed mirror of it.
  **Small separate fix, not the main audit finding**: the "CS"/"CamScanner" watermark tail flagged as a known, deliberately out-of-scope cosmetic leftover in the `transaction_charges` fix above was cleaned up. Confirmed generalizable before touching anything: grepped `CamScanner` across all 3 real Bilateral cache files (`combined_kp_ppra_test`, `Conservator_Wildlife_Peshawar_Zoo`, `schdule_of_charges_or_bilateral`) -- dozens of occurrences total, and in every single one the shape is identical: `"CS\nCamScanner"` immediately before that page's boundary marker, on every page, not just the fee-table page. This is the CamScanner scanning app's own per-page watermark, not fee-table-specific content, so stripping it generalizes safely rather than overfitting to one sample. Added `_strip_trailing_scanner_watermark` as a `_post` processor for `transaction_charges` (a post-extraction cleanup, deliberately *not* folded into the stop-boundary regex itself, so the already-fixed and already-verified boundary logic stays untouched) -- strips a trailing `\nCS\nCamScanner` if present, no-op otherwise. Verified directly against all 3 real cache files (not just the synthetic test fixture): `combined_kp_ppra_test` and `schdule_of_charges_or_bilateral` both previously ended their real captured value in `CamScanner` and now come back clean (no `CamScanner`/`STAMPING` in the value at all); `Conservator_Wildlife_Peshawar_Zoo`'s real captured value is unaffected (byte-identical) since its fee table happens to be followed directly by more agreement prose rather than a page boundary. `test_extract_bilateral_agreement_transaction_charges_stops_before_intervening_stamp_page`'s expected value updated to no longer include the watermark tail.
  Ruff/mypy: stash-diffed against the pre-audit baseline -- identical 299 ruff findings, 54 mypy findings (only line-number shifts in the diff output, zero new findings, zero fixed findings beyond what was already there). Full backend suite: 885 passed / 1 failed both before and after (the known `test_health` environmental artifact) -- no new test was added (the CamScanner fix only tightened an existing test's assertion), so the count is identical, confirming zero regressions.
  **Honest status on the question actually asked**: yes, this bug class is closed across the whole codebase now, not just for the one instance already fixed -- confirmed by direct inspection of every span/boundary capture in `extractors.py`, not by assumption. What remains open, named plainly rather than swept under the fix: every extractor's test suite is hand-fixture-only (bucket "a"), so a *future* marker-dependent capture bug in a different extractor would have the same blind spot this one did, until that extractor's tests are rebuilt to load real cache text via `get_ocr_text` rather than mirror it by hand. That's a real, scoped, future test-infrastructure task -- not something this audit invented a fix for, since no extractor currently has the code shape that would make it exploitable today.

- **2026-08-26: Closed the real-sample test-coverage gap named in the audit entry above -- 8 of the 12 registered extractor types now have real-cache-backed regression tests; 4 remain a named, tracked gap for lack of any real sample; 2 genuine new extraction bugs surfaced along the way, confirmed but deliberately not fixed in this pass.**
  **Enumeration first, per the request.** `app/document_analysis/services.py::_CHECKLIST_TYPE_MAP` names 8 real-onboarding-checklist types (BILATERAL_AGREEMENT, AUTHORITY_LETTER, ACCOUNT_MAINTENANCE_CERTIFICATE, TRIPARTITE_AGREEMENT, BUSINESS_REQUIREMENT_DOCUMENT, ONE_LINK_LETTER, CNIC_FRONT, FORMAL_REQUEST_LETTER) reachable through the splitter's real routing; `_EXTRACTORS` additionally registers 4 generic types (BANK_STATEMENT, PAYSLIP, ID_DOCUMENT, TAX_DOCUMENT) reachable only through `detect_document_type`'s independent keyword scorer, never through checklist routing. Cross-referenced against every file in `Confidential Data/.ocr_cache/` (69 files, one new since the routing-fix session added `combined_kp_ppra_test`): all 8 checklist types have at least one real cached sample (counts: Bilateral 2 by filename + 1 more whose filename predates the 2026-08-26 splitter fix and still says `OTHER_SUPPORTING_DOCUMENT` despite genuinely being Bilateral content, Authority Letter 9, Account Maintenance Certificate 7, Tripartite 1, BRD 8, One-Link Letter 23, CNIC Front 4, Formal Request Letter 1). **The 4 generic types have zero real cached samples anywhere** -- confirmed by grepping every cache filename for those 4 type strings, zero hits -- because no real onboarding document has ever been a bank statement, payslip, personal ID card, or tax return; this checklist is a merchant/department onboarding flow, not a personal-KYC one. Logged as a standing, named gap: real coverage for these 4 is not achievable today at zero cost, and fabricating a synthetic "real-shaped" fixture and calling it real coverage would be exactly the kind of dishonest test this task explicitly said not to write.
  **Confirmed directly, not trusted from the prior summary**: grepped `tests/` for `get_ocr_text` again (zero callers, as the prior entry found) and, more directly, confirmed none of the fixture constants in `test_document_analysis_engine.py` read from `Confidential Data/` at all -- every one is a Python string literal.
  **Investigating real samples for test selection surfaced two genuine extraction bugs, not synthetic ones -- stopped, named, not fixed, per the task's own instruction.** Both share the same root cause, confirmed independently on two different extractors:
  1. `TripartiteAgreementExtractor.account_holder` (its one real sample, `TMA_Thall_Agreement`) returns `"TMA T"` -- truncated from the real value, which the text states as "...titled as TEHSIL GENERAL ACCOUNT TMA THALL". Traced to `_HOLDER_SENTENCE_PATTERNS`' "on behalf of X" fallback matching an unrelated "...on behalf of TMA Thall." sentence in an Authority Letter section bundled earlier in the same combined source file; that pattern's capture group, `[A-Z0-9 ,.&'()\-/]`, is uppercase-only, so it stops dead at the first lowercase letter -- "Thall" breaks after "T" (the following "h" is lowercase) and the fallback settles for "TMA T".
  2. `OneLinkLetterExtractor.organization_name` on `TMA_Khal_Dir_Lower__ONE_LINK_LETTER_copy3.txt` returns `"IL MUNICIPAL ADMINISTRATION KHALL"` -- missing "TEHS" off the front of the real "TEHSIL MUNICIPAL ADMINISTRATION KHALL". The real OCR text reads "...TEHsIL\nMUNICIPAL ADMINISTRATION KHALL hereby authorizes 1LINK..." -- a single stray lowercase "s" (real OCR noise, not a transcription error) inside "TEHSIL". `organization_name`'s capture group, `[A-Z][A-Z,.\s]{3,60}?`, is likewise uppercase-only: the lowercase "s" blocks any match starting at "T", so Python's leftmost-match `.search()` instead finds its first valid start two characters later, at "IL".
  **Both are the identical bug shape**: an all-caps-only character class in a sentence-fallback capture, silently vulnerable to any lowercase letter landing inside the captured span -- whether from ordinary title-case text (Tripartite) or a single-character OCR misread (One-Link Letter). Confirmed on two independent real samples across two independent extractors, which is exactly the kind of generalizable, non-overfit evidence this project's own real-sample discipline asks for before attempting a fix -- but per the task's explicit instruction, not fixed in this pass; both are pinned as today's actual (known-bad) output in the new regression tests below, with inline comments pointing back to this entry, so a future fix has concrete before/after material and the tests don't silently start "passing wrong" without explanation. **Worth its own dedicated follow-up prompt.**
  One more thing noticed in passing, not a bug: roughly 14 of 23 real `ONE_LINK_LETTER` cached copies across every organization on file (including every copy for some organizations, e.g. DG Sports, GDC Madyan Swat) extract zero fields. Investigated enough to confirm this is the extractor's own documented deliberate narrowness (it only detects one specific "(x) ... hereby authorizes 1LINK to take actions" sentence) combined with a real onboarding document routinely splitting into several cached "copies" (separate pages of the same physical multi-page form) where only one copy happens to carry that sentence -- an honest per-copy miss, not a broken pattern. Not chased further since it isn't a mismatch, just noted with one deliberately-empty test case included below as a documented example of normal behavior.
  A third minor observation, also not a bug: `TMA_Thall_Hangu__AUTHORITY_LETTER_copy1.txt` is cached under the Authority Letter type but its real content is a "MASTER CHECK LIST" cover page, not an authority letter at all -- correctly extracts nothing. This is a splitter/document-grouping question (why a checklist cover page landed in this type's copy slot), out of scope for an extraction-coverage audit; noted here rather than silently treated as "the extractor is broken."
  **18 new real-sample regression tests added to `tests/test_document_analysis_engine.py`**, one section at the end of the file, covering all 8 checklist types: 3 Bilateral (the 2 originally-validated real samples plus document 42978, closing the loop on the earlier `transaction_charges` fix as a permanent regression test rather than a one-off verification script), 3 Authority Letter (the "Dr." honorific case, the bare "the\nAuthority" backward-reference case, and the honest-empty checklist-cover-page case), 2 Account Maintenance Certificate, 1 Tripartite (the only real sample, both known bugs pinned and commented), 3 BRD (two typical real samples plus the honest-miss-on-services-list case), 3 One-Link Letter (two orgs where the clause is present, one known-bug case, one honest-empty case), 2 CNIC Front (clean and scrambled, matching `CnicFrontExtractor`'s own documented real-sample behavior), 1 Formal Request Letter (the only real sample). All guarded by a module-level `requires_real_cache = pytest.mark.skipif(not _OCR_CACHE_DIR.is_dir(), ...)` -- `Confidential Data/` is gitignored and does not exist in CI, so these tests run locally (where they closed today's gap) and skip cleanly everywhere else, rather than failing a checkout that has never had real data. Loads text via a direct file read, not `scripts.ocr_cache.get_ocr_text()`: every referenced file is already cached (so a direct read and a cache-hit `get_ocr_text()` call return byte-identical content), and a direct read avoids pulling that module's `pymupdf`/`PaddleOCREngine`/`DocumentSplitter` import chain into a plain-text-fixture test file that has never needed it.
  **Non-determinism caveat stated explicitly in the new test-section's own header comment** (not just here): each cached file pins one specific OCR pass of one real document, not a guarantee of what a fresh live OCR of the same physical document would always produce byte-for-byte -- document 42978's own two OCR passes already showed genuine wording differences in the same physical boilerplate region. A passing test here means "this extractor handles this specific captured text correctly today," nothing stronger.
  Ruff: one new E501 (line too long) introduced while writing the tests, fixed by wrapping the line -- confirmed via rule-code-count diff against the pre-change baseline that this was the only new finding (1003 findings before and after wrapping, identical). Mypy: 54/54, unchanged. Full backend suite before/after: 885 passed / 1 failed -> 903 passed / 1 failed (18 new tests, the same known `test_health` artifact, zero regressions elsewhere).
  **Honest status**: 8 of 12 extractor types now have real-sample-backed regression coverage (BILATERAL_AGREEMENT, AUTHORITY_LETTER, ACCOUNT_MAINTENANCE_CERTIFICATE, TRIPARTITE_AGREEMENT, BUSINESS_REQUIREMENT_DOCUMENT, ONE_LINK_LETTER, CNIC_FRONT, FORMAL_REQUEST_LETTER). 4 remain gap-flagged for a real, structural reason (BANK_STATEMENT, PAYSLIP, ID_DOCUMENT, TAX_DOCUMENT -- no real onboarding document has ever been one of these), not an oversight, and closing that gap needs a real sample appearing in the corpus before it's honest to attempt, the same standing rule this file has applied everywhere else. Two genuine, evidenced, generalizable bugs were found in the course of this coverage work (Tripartite's `account_holder` truncation, One-Link Letter's `organization_name` truncation, same uppercase-only-character-class root cause) and deliberately left unfixed, each pinned as a known-bad regression baseline with an inline comment -- **this needs its own dedicated follow-up prompt**, not folded into this coverage pass.

- **2026-08-26: Fixed the shared truncation bug flagged in the coverage audit above (`TripartiteAgreementExtractor.account_holder`, `OneLinkLetterExtractor.organization_name`) -- both confirmed fixed against real text; the fix process itself caught and corrected a real regression before it shipped, and one prior-session assumption about the "correct" expected value turned out to be wrong.**
  **Traced both bugs to their exact real text before touching anything, per the request.** Confirmed `TripartiteAgreementExtractor.account_holder`'s "TMA T" comes from `_HOLDER_SENTENCE_PATTERNS`' "on behalf of X" pattern matching the real sentence "...on behalf of TMA Thall." (from an Authority Letter section bundled into the same combined source file) -- **not**, as the prior session's finding assumed, from "...titled as TEHSIL GENERAL ACCOUNT TMA THALL", a different real sentence none of the 5 patterns in `_HOLDER_SENTENCE_PATTERNS` actually anchor on (no literal "account titled"/"in the name of"/etc. precedes it). Confirmed `OneLinkLetterExtractor.organization_name`'s "IL MUNICIPAL ADMINISTRATION KHALL" comes from a genuine single-character OCR misread in the real source text, "TEHsIL\nMUNICIPAL ADMINISTRATION KHALL hereby authorizes..." (lowercase "s"), which blocks the uppercase-only capture class from ever starting at "T", so Python's leftmost-match search starts two characters later instead.
  **The "just widen the character class" instinct was tested and rejected twice, on real evidence, before landing on the actual fix** -- the same discipline as the `transaction_charges` fix:
  1. First attempt: add lowercase letters to `_HOLDER_SENTENCE_PATTERNS`' class, nothing else. Verified against all 3 real Bilateral samples (not just Tripartite's) and this immediately surfaced a genuine new regression: all 3 now produced `account_holder: "Party B"` / `"Party B 6"` -- a real sentence, "...revenue collected on behalf of Party B[.] 6.4. If Party B wish to change...", that the old (buggy) class had accidentally been blocking outright (every "Party " capture failed the moment it hit the lowercase "arty"). "Party A"/"Party B" are well-documented generic defined-term placeholders in this exact corpus (`BilateralAgreementExtractor`'s own class docstring: "...hereinafter referred as First party or Party A or Party 1" / "...Second Party or Party B or Party 2"), never real organization names. Fixed with a targeted, evidence-grounded guard, `_GENERIC_PARTY_LABEL`, added to `_extract_holder_from_sentence` alongside its existing bank/branch guard (the same established convention already used there): rejects a captured value that *is* one of these known placeholders (optionally with trailing OCR/section-number noise, e.g. "Party B 6"), anchored at the start of the value so a real org name merely mentioning "party" later is never affected.
  2. Second attempt, after the placeholder guard: exclude the literal `.` from the class so the greedy match stops at the first sentence-ending period instead of running through it. This alone fixed Tripartite's case ("TMA Thall.") -- but running it against every existing hand-written AMC fixture in this file (as the task explicitly asked, to catch tests quietly relying on old truncated behavior) broke a previously-passing one: `test_extract_first_of_parallel_account_generations`, whose fixture mirrors real DG_Sports AMC content ("the account of SAMPLE SPORTS AUTHORITY maintained with this branch is in good standing.") lost its `account_holder` field entirely. Root cause: that sentence has no period anywhere near the org name -- a period-only stop condition ran straight through "SAMPLE SPORTS AUTHORITY" into "maintained with this branch is in good standing", which the fallback's own bank/branch guard then rejected outright (the over-wide capture happened to contain the word "branch"), losing the field. Two real samples genuinely disagree on where the boundary is.
  **The fix that actually works, verified against both real shapes plus every real cached AMC/Tripartite/Bilateral sample and every relevant hand-written fixture**: keep the capture non-greedy (`{3,60}?`, was greedy) with lowercase allowed, and add a required lookahead, `(?=\.|\s+[a-z]|$)`, stopping the match at whichever comes first -- a literal period, whitespace immediately followed by a lowercase word (a genuine word boundary into unrelated prose), or end of the flowed text. This is grounded in a real, consistent property both shapes share: org/party names in this corpus are always written as a run of Title-Case-or-ALL-CAPS words, and the prose that resumes after one always starts with a lowercase word or the sentence just ends -- a stray lowercase letter *inside* a word (the original OCR-misread bug shape) never triggers the lookahead, since there's no whitespace immediately before it, so it doesn't reintroduce the truncation this fix exists to close.
  `OneLinkLetterExtractor.organization_name` needed a simpler fix, verified separately: its pattern already has a required, unchanged trailing literal anchor ("hereby authorizes...to take actions") and a non-greedy quantifier, so it was already structurally immune to the "swallow unrelated trailing text" risk -- adding lowercase to its class (`[A-Z,.\s]` -> `[A-Za-z,.\s]`) was sufficient and safe on its own, confirmed against all 23 real ONE_LINK_LETTER cached samples.
  **Verified fixed, byte-exact, against real text**: `TripartiteAgreementExtractor.account_holder` on `TMA_Thall_Agreement` now returns `"TMA Thall"` (was `"TMA T"`) -- the complete, correct capture of the sentence this fallback actually matches (not "TEHSIL GENERAL ACCOUNT TMA THALL", a different sentence this fallback was never anchored on; which sentence it *should* prefer is a separate, unrelated scope question). `OneLinkLetterExtractor.organization_name` on `TMA_Khal_Dir_Lower__ONE_LINK_LETTER_copy3` now returns `"TEHsIL MUNICIPAL ADMINISTRATION KHALL"` (was `"IL MUNICIPAL ADMINISTRATION KHALL"`) -- reproducing the source text's own OCR misread verbatim, since the fix stops the truncation, it does not correct OCR noise the document itself contains. 3 more real ONE_LINK_LETTER samples with the identical latent bug shape, not previously known to be broken, were fixed as a side effect of the same class change: `TMA_Thall_Hangu` ("IL MUNICIPAL ADMINISTRATION THALL" -> "TEHsIL MUNICIPAL ADMINISTRATION THALL"), `TMA_Samarbagh_Dir_Lower` ("TRATION, SAMARBAGH" -> "TEs MUNICIPAL ADMINIsTRATION, SAMARBAGH", two separate stray-lowercase misreads in the real text), and `DG_Sports_KP_Onboarding_Documents` copy 2, which went from a total empty result to `"Directorate General of Sports Khyber Pakhtunkliwa"` (a misread "Pakhtunkliwa") -- the org name's own stray lowercase had previously blocked the match from finding a valid start anywhere in the whole sentence, not just truncated it.
  **Confirmed zero regressions across the full corpus, not just the samples that found the bugs**: re-ran every real cached sample for all 4 affected types (ONE_LINK_LETTER x23, ACCOUNT_MAINTENANCE_CERTIFICATE x7, TRIPARTITE_AGREEMENT x1, BILATERAL_AGREEMENT x3) before and after, diffed -- only the 5 intended changes above appear; every other real value, and every real honest-empty result, is byte-identical. Re-ran the full existing hand-written-fixture suite in `test_document_analysis_engine.py` (the older tests, not last session's real-sample additions): all pass, including the AMC fixture that exposed the period-only regression during development.
  **Step-3 audit for other latent instances of the same bug shape, as requested**: grepped `extractors.py` for every uppercase-only character class. The remaining hits are all fixed-format structural matchers (IBAN shape, currency codes, account-number token shape) where uppercase-only is semantically correct (these formats *are* always uppercase), not sentence/value-fallback captures, plus the `transaction_charges` stop-boundary's own all-caps-header detector (deliberately left untouched, per the task's explicit instruction). No other instance of "uppercase-only class as the sole/primary stop condition for a free-text value capture" exists in this file.
  Two `test_document_analysis_engine.py` real-sample tests updated to assert the corrected values and lose their "known-bad" framing: `test_real_tripartite_agreement_tma_thall` (account_holder now asserted as `"TMA Thall"`; `party_subbiller`'s separate, unrelated known-bad capture -- the template-placeholder-swallowing bug -- is untouched and still flagged open, not part of this fix) and `test_real_one_link_letter_khal_dir_lower` (asserts `"TEHsIL MUNICIPAL ADMINISTRATION KHALL"`, with the docstring rewritten to explain the fix rather than describe a known bug).
  Ruff/mypy: stash-diffed against the pre-fix baseline -- rule-code counts identical, zero new findings on either file touched. Full backend suite before/after: see the commit for final numbers; no test count change beyond the 2 rewritten assertions (no new tests added, this was a fix pass).
  Did not touch: the `transaction_charges` regex, the CamScanner-stripping post-processor, anything else in `BilateralAgreementExtractor` unrelated to this bug class, and the 4 document types with zero real cached samples (unrelated, already-logged gaps).
  **Honest status**: both originally-found truncation bugs are fixed and verified against real text on every real sample of the affected types, not just the ones that surfaced them. The audit found no other latent instance of this exact bug shape anywhere in `extractors.py`. Nothing was left open because a safe fix couldn't be found -- the one field where a naive fix genuinely didn't generalize (the AMC "account of X maintained..." shape vs. Tripartite's "on behalf of X." shape) got a real fix instead of a shrug, once both real shapes were checked before shipping. `TripartiteAgreementExtractor.party_subbiller` remains open, but it's a separate, different, previously-flagged bug (a template-placeholder-swallowing capture, not a truncation), explicitly out of scope for this pass and unaffected by anything here.

- **2026-08-25/26: Six department-requested features relayed verbally/via a hand-photographed note (2026-08-25) captured as backlog -- not implemented, and the note's wording is treated as an approximate paraphrase, not a settled spec, per this project's standing norm against guess-building from underspecified input.** Four items below; item 6 (AMC "originality" check) and item 4 (conditional approval decision) get their own, more detailed entries further down since they needed real code investigation to scope honestly.

  **1. "Special approval" despite missing required items.** Read `app/human_verification/validators.py::_validate_approve`: today an `APPROVE` decision hard-requires every one of the 13 fixed `CHECKLIST_ITEMS` to be checked (`ChecklistIncomplete` raised otherwise) -- there is no override path at all. This request and item 4 (conditional approval) sound closely related but are not confirmed to be the same request: the note doesn't say whether "special approval despite missing items" means the same thing as "conditional approval," a distinct third option, or a relaxation of the existing `APPROVE` rule itself. Needs the department to confirm before this is scoped further; see the dedicated conditional-approval entry below for what's concretely known about the current decision model this would extend.

  **2. Per-deficiency remediation detail in the validation report.** Read `app/reports/constants.py::RECOMMENDATION_TEMPLATES`/`RECOMMENDATION_ORDER`: today's report already gives *some* deficiencies specific detail via a `{details}` slot (`MISSING_REQUIRED_DOCUMENT`, `DUPLICATE_REQUIRED_DOCUMENT`, `MISSING_SIGNATURE`, `MISSING_STAMP` all name the affected document type(s)), but several others are purely generic category-level nudges with no specifics at all: `"Review failed IBAN consistency across documents."` doesn't say which two documents disagree or what either value is; `"Correct low-confidence fields."` doesn't name which fields; `"Review account holder/account number/statement period consistency..."` and `"Review document dates and statement period."` are the same shape. This is a real, evidenced gap, not a vague ask -- half the recommendation table already does what's being requested, the other half doesn't. Scoping the fix (which of these should carry the actual conflicting values, and where those values would come from -- the report generator already has access to the underlying `validation_results`/`extracted_fields` rows, so the data likely already exists, just isn't surfaced in the message) is real, buildable work, not blocked on department clarification the way items 1 and 6 are.

  **3. Editable "AI-generated commentary."** Grepped the whole `app/reports` module for any AI-generated narrative/commentary concept: none exists. Every recommendation string in `RECOMMENDATION_TEMPLATES` (see item 2 above) is a fixed, deterministic template expanded with rule-engine output -- there is no LLM or other generative step anywhere in the reports pipeline, and nothing called "commentary" appears in the codebase. Either the department is picturing a feature that hasn't been built yet (and "editable" would need to wait until whatever generates it exists), or they are referring to the existing deterministic recommendation text and using "AI-generated" loosely/incorrectly to describe it. This needs the department to clarify which before any design work -- there is nothing to make "editable" yet if the first reading is correct.

  **5. Editing rights "within the analysis of report document" -- resolved to a specific, concrete finding, not left ambiguous.** Two real, distinct things exist in this codebase, and the department's note doesn't say which they mean:
  - Editing the *extracted field values* that feed the report: **this already exists and is fully built.** `app/human_verification/schemas.py::CorrectionItem`/`CorrectionItemRead` (field_name, corrected_value, reason, document_id) plus `validators.py::_validate_correct` implement a full reviewer correction workflow, already wired to the frontend (`frontend/src/components/humanReview/ReviewFields/ReviewFields.jsx`, referenced in the 2026-08-19 "field-level review widget" entry below). If this is what "editing rights in the analysis" means, there is nothing new to build.
  - Editing the *generated report document itself* (the rendered narrative/layout, as opposed to the field data behind it): **this does not exist.** `app/reports/services.py` renders a printable HTML report from `backend/app/templates/validation_report.html` (its own docstring: "a future PDF exporter can consume either the model or the rendered HTML" -- confirming no PDF export exists yet either, only HTML rendering), and there is no endpoint or workflow anywhere that lets a reviewer edit that rendered output's own text.
  Given the code strongly suggests the first interpretation (field correction) is what "editing" a report's *analysis* most plausibly means, and that's already shipped -- but this is a real ambiguity worth a one-line confirmation from the department before assuming either way, per the AuditLog-viewer investigation's own lesson (line 133 area, "do not wire something up reactively without checking what already exists first" applies here in the opposite direction too: don't *close* a request as "already done" without the department confirming that's actually what they meant).

- **2026-08-25/26: "Originality of AMC" check -- mechanism genuinely unknown, cannot be scoped further without the department's input.** The sixth relayed item asks for some kind of authenticity/originality verification specific to the Account Maintenance Certificate document type. Checked what already exists that's adjacent: `CHECKLIST_ITEMS` in `app/human_verification/constants.py` already includes a manual checklist line, `"Bank Maintenance Certificate originality confirmed"` -- a human-attested checkbox, not an automated check -- and the rule engine's existing `VIS_SIGNATURE_AMC`/`VIS_STAMP_AMC` visual-detection rules (see `app/reports/constants.py::VISUAL_TYPE_BY_RULE`) check for signature/stamp *presence*, not authenticity or forgery. None of these amount to an "originality" check in any sense beyond a manual attestation checkbox. At least three genuinely different mechanisms could satisfy the department's request and this project has no way to know which without asking:
  - Visual/structural forgery or tampering detection (comparing the AMC's layout/watermarks/print characteristics against a known-genuine template) -- would need a reference corpus of confirmed-genuine AMCs, which doesn't exist in this codebase today.
  - Cross-referencing against an external system of record (e.g. confirming with the issuing bank, or against a bank-provided registry) -- would need an integration this codebase has no groundwork for.
  - Duplicate-submission detection across applications (flagging the same physical AMC, or the same account number, appearing on more than one application) -- the closest to something buildable from existing data (account numbers are already extracted and normalized per application), but this is a guess at intent, not a confirmed requirement.
  Per this project's standing norm (the `CrossBranchCodeRule`/`CrossPeriodRule` history, and the AuditLog-viewer investigation before it): do not guess-build a mechanism and call it "originality verification." This stays an open item pending the department naming what "originality" is actually meant to check.

- **2026-08-25/26: "Conditional approval" decision option -- scoped against the real current decision-state code, not implemented.** Read `app/database/models/enums.py::ReviewDecision` (a native PostgreSQL enum: `APPROVE`, `CORRECT`, `REJECT`, nothing else today) and `app/human_verification/constants.py::DECISION_TO_STATUS` (`APPROVE`->`ApplicationStatus.APPROVED`, `CORRECT`->`CORRECTED`, `REJECT`->`REJECTED`) plus `validators.py::validate_decision_rules`, which enforces each decision's own internal-consistency rules (approve needs the full checklist and carries neither corrections nor a rejection reason; correct needs at least one correction; reject needs a mandatory reason). There is no fourth state and no notion of a decision carrying open conditions today.
  Adding a genuine "conditional approval" decision would concretely touch, at minimum, all of the following -- named here as the real surfaces this would need to change, not designed:
  - **Database**: `ReviewDecision` and `ApplicationStatus` are both native Postgres ENUM types (confirmed, `README.md`'s "All domain enums ... are native PostgreSQL ENUMs" line), so adding a new value to either requires an Alembic migration (`ALTER TYPE ... ADD VALUE`), not just a Python-side enum edit.
  - **Backend**: a new `ReviewDecision` member, a new `DECISION_TO_STATUS` mapping (to an existing status, or a genuinely new `ApplicationStatus` value if "conditional" needs to be its own lifecycle state, e.g. distinct from `APPROVED`/`CORRECTED`), and a new `_validate_*` rule function in `validators.py` defining what a conditional approval actually requires to submit (does it need a stated condition/reason field? does it still require the checklist, or exactly which items?) -- this last question is itself unanswered by the relayed note and would need product input, not just an engineering decision.
  - **Frontend**: `frontend/src/components/humanReview/ReviewDecision/ReviewDecision.jsx` hardcodes the 3-option decision selector (`APPROVE`/`CORRECT`/`REJECT`) and their per-decision validation (checklist-complete / has-correction / has-rejection-reason) -- a fourth option needs a new selector entry plus whatever new required field a "condition" carries.
  - **Downstream**: anything that branches on `ApplicationStatus`/`ReviewDecision` today (dashboards, the report's `ReportOverallStatus` derivation, audit logging, any RBAC-gated view keyed on status) would need to be checked for whether it needs to learn about the new value or can safely ignore it.
  This is real, scoped, buildable engineering work once the department confirms what a "condition" actually needs to carry (a required note? a set of items still pending? an expiry?) -- that product question is not answered by the relayed note and is not this session's to decide.

- **2026-08-26: Multi-application batch queue (10-20 combined PDFs -> 10-20 independent applications, submitted and processed together) scoped against real code and real capacity data -- not implemented.** Named deliberately distinctly from the existing `app/bulk_queue` module in this entry and everywhere else it's discussed: `app/bulk_queue` already means "one application's bulk-uploaded PDF, split into many *documents* within that one application" -- this request is "one submission producing many *applications*," a different problem one level up. Reusing the name for both would risk exactly the kind of misdiagnosis this project has hit before conflating the splitter and the extractors -- keep them named and discussed separately.

  **What's already real and reusable, confirmed by reading the code, not assumed**: `POST /applications` (`app/upload/routes.py::create_application`) creates one application; `POST /applications/{id}/bulk-upload` uploads and splits one combined PDF for that one application; `POST /applications/{id}/processing/start` enqueues OCR, and `PipelineRunnerService.run()` (`app/bulk_queue/pipeline_runner.py`) auto-chains analysis -> confidence -> normalization -> rule validation once OCR finishes, landing at `PENDING_REVIEW` with zero manual per-stage calls. This *is* already "one combined PDF -> one fully processed, independent application" end to end -- exactly what the department describes needing 10-20 of. The per-PDF pipeline logic does not need to be reinvented.

  **What's confirmed missing, by reading every route in `app/bulk_queue/routes.py` and `app/upload/routes.py`**: every endpoint in both modules is scoped to a single `application_id` -- there is no endpoint anywhere that accepts multiple PDFs and creates multiple applications from one submission. Today, running 10-20 organizations through the system means a human manually repeating the create-application/bulk-upload/processing-start sequence 10-20 separate times. What would need to be newly built is a batch-submission entry point: accept N PDFs in one request/session, create N applications, and auto-drive each through the existing per-application flow above -- an orchestration layer over already-working pipeline logic, not a new pipeline.

  **Real capacity findings (dev DB/dev machine, 2026-08-26, not assumed)**:
  - **Storage**: 5 already-processed applications on disk total 50 MB (`storage/applications/APP-*`), ranging 3.9-21 MB each, ~10 MB average. 20 applications extrapolates to roughly 200-400 MB -- trivial on any modern disk; storage capacity is not a real constraint at this scale.
  - **Database**: `pg_database_size` for the dev DB is 20 MB for those same 5 applications (41 `documents`, 46 `queue_jobs`, 34 `ocr_results`, 75 `extracted_fields`, 846 `validation_results` -- the rule engine writes far more rows per application, ~169 on average, than any other table). 20 applications extrapolates to roughly 80 MB and a few thousand rows across these tables -- also trivial for Postgres; database capacity is not a real constraint at this scale either.
  - **Throughput is the real constraint, not storage.** Queried real completed `queue_jobs` timestamps: average `DOCUMENT_OCR` job duration is **104.6 seconds** (n=39 real completed jobs), ranging from near-instant (~0.1-0.4s for documents with usable native PDF text, no OCR needed) up to ~294s for a heavily scanned page. At ~8.2 documents/application (41 documents / 5 applications, real average), that's roughly **14-15 minutes of OCR time per application**. `bulk_queue_workers` defaults to **1**, and CLAUDE.md/`app/core/config.py` document why: `PaddleOCREngine` is a process-wide singleton with no internal locking around `.predict()`, and concurrent workers have a real, previously-confirmed crash risk -- this is a deliberate safety default, not an arbitrary scaling knob (the setting technically accepts up to 16, but raising it reintroduces that confirmed crash risk). At `workers=1`, processing 20 applications serially through OCR alone extrapolates to roughly **20 x 14-15 min ≈ 4.5-5 hours** of continuous, single-threaded OCR work for one batch submission. This is the number that actually matters for scoping this feature -- not whether the disk or database can hold the data (they clearly can), but how long a 10-20-PDF batch realistically takes to fully process and whether that's an acceptable wait.

  **Open product/architecture questions this raises -- listed for Abdul/the department to decide, not answered here**:
  - Should the 10-20 applications in a batch process strictly serially (safe, ~4.5-5 hours for 20, given the documented single-worker PaddleOCR constraint above) or is bounded concurrency worth the real, already-confirmed crash risk that `bulk_queue_workers=1` exists specifically to avoid?
  - How should a mid-batch failure be surfaced -- if PDF #7 of 20 fails (a corrupt file, an OCR crash, a validation exception), should the other 19 continue automatically, and how does the department find out #7 needs attention without babysitting the whole batch?
  - How should a reviewer experience 20 reports landing over an extended multi-hour window rather than one application to review at a time -- a queue/notification of some kind, or is checking back on the batch's status an acceptable interim workflow?
  These are product decisions this session is not positioned to make, and per this project's standing norm against guessing, none of them are answered here.

- **2026-08-26: `TripartiteAgreementExtractor.party_subbiller` fixed -- root cause was a wrong anchor entirely, not a boundary/truncation issue like the `account_holder`/`organization_name` fixes in `f0df45a`, so the "Party B" denylist-guard approach did not transfer here and a different mechanism was used instead.**
  **Root cause, confirmed in real text, not assumed.** The old pattern, `(.{2,}?)\s*\([^)]*?[Ss]ub[-\s]?[Bb]iller[^)]*?\)`, searched the whole document for the first "(...)" parenthetical mentioning "sub-biller" and captured everything before it. Pulled the real `TMA_Thall_Agreement__TRIPARTITE_AGREEMENT_copy1.txt` text directly: the document contains exactly two such parentheticals, and neither names the sub-biller. The first (and the one the old pattern actually matched, confirmed by printing the match span, not just reasoning about it) is fixed legal boilerplate: "...and a copy of which **(barring commercials, at discretion of sub biller/bill aggregator)** will be provided to 1LINK." The second, appearing right after it, is the document's own unfilled template field: "**(Sub Biller Name)** confirms that a 30 (thirty) days prior notice shall be provided..." -- literally never replaced with a real name in this signed, real document. (Correction to the record: the 2026-08-25/26 coverage-audit entry and the `f0df45a` entry both described the match as landing on/near the "(Sub Biller Name)" placeholder -- confirmed by direct re-inspection that this was imprecise; the actual match is the *first* parenthetical, the boilerplate one, not the placeholder. Both were always known-wrong outcomes for the same underlying reason, so the practical conclusion those entries reached was still correct, just this one detail wasn't verified precisely enough at the time.)
  **Mechanism, not just symptom**: this isn't a too-loose or too-tight character-class boundary like the two bugs fixed in `f0df45a` -- the anchor itself (any parenthetical mentioning "sub-biller") is structurally the wrong thing to search for, since this real document's own defined-terms convention for the sub-biller is simply never filled in. No boundary fix could have produced a correct value here; the pattern needed to search for something else entirely.
  **Where the real name actually is, and why the fix is grounded in more than one sample despite the single-real-Tripartite-sample constraint.** The real sub-biller name is stated in plain prose, not a parenthetical: "The Tehsil Municipal Administration THALL **hereby irrevocably undertakes**, agrees and acknowledges that, they: ...". This document's Participation-Memorandum-style sub-biller section is built from the same real, shared 1LINK template already relied on elsewhere in this codebase (`OneLinkLetterExtractor.organization_name`'s "hereby authorizes 1LINK to take actions" anchor, from the same template's clause (x)) -- and this "<org> hereby irrevocably undertakes" clause (from clause (i) of the same template) is confirmed present, and correctly naming the real organization, across **11 other real cached files spanning at least 8 independent organizations** (DG Sports, GDA Abbotabad x3, GDC Madyan Swat, TMA Khal Dir Lower x2, TMA Lal Dir Upper, TMA Lal Qilla Dir Lower, TMA Samarbagh Dir Lower, TMA Thall Hangu) -- currently cached under the unrelated `ONE_LINK_LETTER` type due to splitter-routing history, not under `TRIPARTITE_AGREEMENT`, but the underlying template text is identical. So while this is still genuinely the only real sample cached under the `TRIPARTITE_AGREEMENT` type (the single-real-sample constraint from the 2026-08-25/26 coverage audit is real and stated plainly, not glossed over), the *anchor phrase itself* is cross-validated far beyond one sample.
  **Fix**: replaced the pattern entirely with `(?-i:([A-Z][A-Za-z0-9 ,&'()\-/]*?))\s+hereby irrevocably undertakes`, matching the same safe shape as `OneLinkLetterExtractor.organization_name`'s already-fixed pattern in `f0df45a` -- non-greedy with a required, unchanged literal continuation immediately after the capture, so (as established there) the match cannot run past the real name into unrelated text without needing a lookahead or character-class exclusion tricks. Confirmed by direct grep that "hereby irrevocably undertakes" appears exactly once in the whole combined `TMA_Thall_Agreement` source file, so there is no ambiguity risk from the file bundling an Authority Letter and BRD alongside this Participation Memorandum section.
  **Why the Party-B guard doesn't transfer here, addressed directly per the request**: that guard exists because a *correct, working* anchor ("on behalf of X") occasionally matched a real but semantically-wrong sentence, and the fix was to reject specific known-bad *values* after a structurally-sound match. Here the anchor itself never had a chance of reaching a real name -- there's no "right mechanism landing on the wrong value" to guard against, only a wrong mechanism to replace. A denylist guard would have been the wrong tool even if I hadn't checked; checking confirmed it, rather than assuming either way.
  **Verified**: `extract_fields()` on the real cached file now returns `party_subbiller: "The Tehsil Municipal Administration THALL"` -- the real sub-biller organization's name, confirmed directly against the source text, not just "no longer the boilerplate string." Re-ran every existing `TripartiteAgreementExtractor` hand-written-fixture test: `test_extract_tripartite_agreement_fields` broke, and rather than papering over it, checked why -- its `TRIPARTITE_AGREEMENT_TEXT` fixture used a "(hereinafter referred to as the 'Sub-biller')" convention for the sub-biller that was **never confirmed present in any real sample** (unlike `party_1link`/`party_kpitb`'s identical-looking convention, which real samples do confirm). Now that real evidence contradicts it, rewrote the fixture's sub-biller clause to the real-confirmed "hereby irrevocably undertakes" shape instead of leaving a spec-guessed convention in place once it's known to be wrong -- the same discipline this codebase has applied to every other extractor's stale spec-guessed fixture (e.g. `BilateralAgreementExtractor`'s original patterns, rewritten wholesale in an earlier session once checked against real samples). `test_extract_tripartite_agreement_compound_table_layout` and every other Tripartite fixture test don't reference `party_subbiller` at all (confirmed by grep) and were unaffected.
  Ruff/mypy: stash-diffed against the pre-fix baseline -- ruff rule-code counts identical (empty diff), mypy 54/54 unchanged, on both files touched. Full backend suite before/after: 903 passed / 1 failed both times (the known `test_health` environmental artifact) -- net one updated real-sample assertion plus one updated hand-fixture assertion, no new tests, identical count confirming zero regressions.
  **Honest status, single-real-sample caveat stated plainly, not glossed over**: `party_subbiller` is fixed and verified against the one real Tripartite sample on file, and the anchor phrase itself is cross-validated across 11 other real documents sharing the same template -- meaningfully more confidence than a true single-sample fix would carry, but still short of a second real sample confirmed under the `TRIPARTITE_AGREEMENT` type specifically exercising this exact field mapping end-to-end. If a future real Tripartite sample states its sub-biller without this "hereby irrevocably undertakes" clause (a genuinely different real department's document, not yet seen), this pattern will honestly miss rather than guess -- consistent with this codebase's standing preference, but worth knowing this is the boundary of what's been checked. Not treated as fully closed the same way a 2+-real-sample Tripartite fix would be; treat any future Tripartite real sample as a chance to confirm or correct this, not as a formality.
  **Bug class status, the other question asked**: grepped `extractors.py` and `test_document_analysis_engine.py` for every remaining "known-bad"/placeholder/garbage-capture marker after this fix -- none remain describing a currently-open issue (the one hit in the test file is this fix's own past-tense note that it *was* fixed). As far as anything previously flagged and tracked in this codebase's history, the placeholder/template-value-swallowing bug class is closed. This is not a claim that no other undiscovered instance could exist anywhere in `extractors.py` -- only that nothing currently named and open remains.

- **2026-08-26: AMC `issue_date` gap closed -- the old note ("a bare Date label fixes 2, breaks 2") was re-derived from scratch against the real corpus and turned out to understate both the risk and the eventual fix; 6 of 7 real samples now resolve correctly, the 7th has a specific, named reason it can't.**
  **Real baseline established first, not assumed.** Ran the *pre-fix* extractor against every real cached AMC sample (Confidential Data/.ocr_cache/, 7 files across 4 independent banks): all 7 returned `None`, not "2 correct / 2 wrong / 2 missing" as the old note implied -- the old "Date of Issue"/"Issue Date"/"Issued On"/"Issuance Date" pattern matched literally nothing on any real sample as they exist today.
  **Three genuinely different real shapes, not one inconsistency.** Reading the real text directly around every date (not guessing from the label alone): (1) 3 samples label it with nothing more specific than a bare `"Date:"`/`"Dated:"` (GDA_Abbotabad copy2/copy3, GDC_Madyan_Swat) in three different formats -- dot (`21.04.2026`), dash (`21-04-2026`), slash (`17/02/2026`); (2) 2 samples state it completely unlabeled, alone on a line in the bank's letterhead (DG_Sports: `29-04-2026`; account_maintennace_certificate/kppra: `18 MAY, 2026`); (3) GDA_Abbotabad copy1's real date is present in the source but OCR-mangled into single digits split across 8 consecutive lines (`2\n2\n0\n4\n2\n0\n2\n6`) *before* the `"Date of Issue:"` label instead of after it -- an OCR-quality problem, not a labeling one, with no generalizable reconstruction (a shape this specific would be a one-sample-only hack, exactly what this project's norms warn against).
  **The two real "breaks" a naive widen hits, confirmed by name, not by assumption.** Widening the label to a bare `"Date"` without more isn't safe on its own: real cached text contains `"Date of Account Opening"` (a different field, further down in every sample's account-detail block) and, on GDA_Abbotabad copy4 specifically, a second real document -- a scanned CNIC copy -- embedded later in the same OCR text, with its *own* `"Date of Issue"` field and a completely different value (`23.08.2023`, the CNIC holder's card-issue date, not the certificate's). A whole-document label search genuinely risks matching either. Also confirmed a second, unrelated false-positive shape: a ledger-table `"DATE"` column header appears later in 2 real samples (GDA copy1, kppra), which a bare-label search would also reach without protection.
  **Fix, addressing both real risks at once.** Two changes, not one: (1) bound every issue_date search to a **header zone** -- text before the certifying sentence (`"This is to certify..."`/`"It is certified that..."`/`"We hereby certified..."`/`"This certificate is/has been issued..."`, matched via a `"certif"` boundary with a lookbehind skipping the document's own title, since `"...Maintenance Certificate"` also contains that substring and would truncate the zone too early otherwise) -- which structurally excludes both the account-detail block and any later-embedded document, since every real sample's genuine issue date sits before that sentence and both real decoys sit after it; (2) require the captured *value* itself to be date-shaped (`_AMC_DATE_TOKEN`: dash/dot/slash numeric, or a month name with a 4-digit year in either order), not just "text up to the next line break" -- this is what lets a bare `"Date"` label safely fail to match `"Date of Account Opening"`'s non-date continuation and keep searching, rather than needing a hand-maintained denylist of every field name that happens to contain the word "date". Within the header zone, tries a labeled match first (`_AMC_ISSUE_DATE_LABEL_RE`, now including bare `"Dated"`/`"Date"`), then an unlabeled fallback (`_AMC_ISSUE_DATE_FALLBACK_RE`) for the 2 samples with no label at all. A new dedicated parser (`_parse_amc_issue_date`, deliberately separate from the shared `_parse_date`/`_as_iso_date`, same pattern as the existing `_as_iso_date_dotted`) adds the two real formats those don't cover: dot-separated and comma-joined month names in either order.
  **Verified byte-exact against all 7 real samples, not just the previously-mentioned 4**: GDA_Abbotabad copy2 `-> 2026-04-21`, copy3 `-> 2026-04-21`, copy4 `-> 2026-04-21` (correctly ignoring the embedded CNIC's `23.08.2023`), GDC_Madyan_Swat `-> 2026-02-17`, DG_Sports `-> 2026-04-29`, kppra `-> 2026-05-18`, GDA_Abbotabad copy1 `-> None` (correctly, genuinely unrecoverable). Also confirmed `account_holder`/`account_number`/`iban`/`branch_name` are byte-identical to the pre-fix baseline on every file -- this fix touches only `issue_date`.
  **Test coverage**: updated both pre-existing real-sample AMC tests (`gda_abbotabad` now asserts the key is correctly *absent*, not present-with-`None` -- the old buggy pattern's garbage match is what produced a present-with-`None` key before; `kppra` now asserts `2026-05-18`) and added 3 new real-sample regression tests -- `dg_sports` (unlabeled fallback, dash format), `gdc_madyan` (widened bare-label, slash format), and `gda_abbotabad_copy4_ignores_embedded_cnic_date` (the single most safety-critical case: locks in that the embedded CNIC's date is never reachable).
  **Three separate synthetic fixtures, not one, needed the same real-evidence-driven correction -- found only by re-running the full suite, not by inspection alone.** All three placed `"Date of Issue"` inside the post-certification detail block alongside Account Number/IBAN/Bank Name/Branch Name -- a placement never confirmed against any real sample and, once checked, not how any real AMC is laid out (every real sample states it in the header, before the certifying sentence). `test_document_analysis_engine.py`'s and `test_document_analysis_api.py`'s own `ACCOUNT_MAINTENANCE_CERTIFICATE_TEXT` each broke their own direct `issue_date` assertion (`KeyError`, since the header-zone bound now correctly excludes that position). `test_rule_engine_api.py`'s `ACCOUNT_MAINTENANCE_CERTIFICATE_CROSS_DOC_TEXT` doesn't assert `issue_date` itself but is reused (via `build_single_statement_application`/`build_full_application`) by 4 downstream tests across `test_reports_api.py`, `test_continuous_learning_api.py` and `test_feedback_api.py` that assert exact confidence-score/count values computed from full field coverage -- losing `issue_date` silently dropped their confidence (e.g. one from `0.9587` to `0.9461`) rather than raising an assertion naming the field, which is why the full-suite run (not just the direct AMC test files) was essential to catch these; a partial `-k account_maintenance` run would have missed all 4. All three fixtures moved the label to before the certifying sentence, matching the real corpus; no other field or number needed correcting in any of them.
  Ruff/mypy: stash-diffed against the pre-fix baseline across all 4 files touched (`extractors.py` + the 3 test files). Ruff: same total count both before and after (96/96 on the 2-file diff, 127/127 on the full 4-file diff); the only non-cosmetic change is one new `DTZ007` (naive-datetime) hit in the new `_parse_amc_issue_date` -- already present 4 other times in this exact file for the identical `datetime.strptime(...).date()` shape (`_parse_date`, `_as_iso_date_dotted`, twice in the textual-month fallback), so this is the same accepted pre-existing pattern, not a new category. Mypy: identical error set (28 errors, same file/line/message), confirmed via a sorted diff. Full backend suite before: 903 passed / 1 failed (`test_health`, the known environmental artifact). First full-suite run after the extractor fix alone (before the 3 fixtures were corrected): 901 passed / 6 failed -- 5 genuinely new failures (all fixture-placement fallout, described above) plus the pre-existing `test_health`. After fixing all 3 fixtures: full backend suite **906 passed / 1 failed** -- the +3 over baseline is exactly the 3 new real-sample regression tests added above, `test_health` is the same pre-existing environmental artifact, confirming zero net regressions once the fixtures matched real-document structure.
  **Honest status**: `issue_date` is fixed and verified against all 7 real samples on file, with the fix's safety grounded in two independently-confirmed real false-positive risks (account-opening-date bleed-through, embedded-CNIC-date bleed-through), not just the formats it now parses. The 7th sample's `None` is not a residual gap in this fix -- it's a different, correctly-identified problem (OCR corruption) that a labeling/regex fix cannot address, stated plainly rather than left as a vague "no safe fix found yet." Separately: 3 synthetic fixtures across the test suite turned out to encode the same never-real-confirmed layout assumption this fix corrects, a reminder that a fix validated only against the real cache and its own direct tests can still miss collateral fixture damage elsewhere -- the full-suite run is what caught it, not real-sample verification, which is exactly why that step stays mandatory even after real-sample checks pass clean.

- **2026-08-26: `detect_document_type`'s `ID_DOCUMENT` collision re-investigated -- closed, but resolved incidentally by earlier unrelated splitter fixes this session, not by a direct fix to the collision itself; no code change to the classifier was made or was needed.**
  **Old note re-derived from scratch, not trusted.** The standing note ("collides with 2 real `OTHER_SUPPORTING_DOCUMENT` samples, not fixed") had been repeated across sessions without ever being re-checked against the current corpus -- same discipline as the AMC `issue_date` note above. Confirmed the two named files (`GDC_Alpurai_Shangla`, `schdule_of_charges_or_bilateral`) were genuinely real and harmful *when originally flagged*: calling `extract_fields(text, ID_DOCUMENT)` on them today still produces garbage if invoked directly -- `full_name` of `"), late fees, fines, security deposits and applicant"` for one, `"& Contact,"` for the other -- confirming the collision was never a false alarm.
  **But re-deriving the current scope from the real corpus, not the old note, shows it's now 0, not 2.** `detect_document_type` is only ever invoked (`DocumentAnalysisService`, `app/document_analysis/services.py:268`) when a document's splitter-assigned storage type has no entry in `_CHECKLIST_TYPE_MAP` -- i.e. only for documents the splitter itself classified as `OTHER_SUPPORTING_DOCUMENT`/`BULK_UPLOAD`. Running the splitter's own classifier (`DocumentSplitter._classify_text`, the same per-cached-file dry-run method this session's Bilateral/Tripartite splitter fixes already established and validated) against every one of the 66 real files in `Confidential Data/.ocr_cache/` today finds only 2 files that still classify as unclassified (`1-link_application_form`, `on_boarding_sub_biller`) -- and neither of those is misclassified as `ID_DOCUMENT` by `detect_document_type` (both correctly score `UNKNOWN`). Both originally-flagged files now title-match to a real checklist type before ever reaching the generic fallback: `schdule_of_charges_or_bilateral` -> `BILATERAL_AGREEMENT` (via `35c0317`'s "FOR DIGITAL PAYMENT COLLECTION VIA PAYMIR" title match, landed earlier this same session), `GDC_Alpurai_Shangla` -> `TRIPARTITE_AGREEMENT` (this cached file is a large multi-document bundle whose text literally contains the string "TRIPARTITE AGREEMENT" among several other real document titles -- `_STRONG_TITLE_PHRASES` has always recognized that literal phrase, since the original splitter implementation; this specific bundle simply didn't reach that match at the time the "2 real samples" note was written, most plausibly because of upstream splitter/absorption behavior since changed by `040b4f6` -- not something this pass re-derived precisely, since it predates and is unrelated to the fixes made tonight, and pinning down that exact prior mechanism is a separate historical question, not required to confirm today's current, directly-tested outcome).
  **Precise, honest framing -- what was actually fixed vs. what wasn't.** `_DETECTION_KEYWORDS`'s `ID_DOCUMENT` weights (`"passport"` at 3, `"expiry date"` at 2, etc.) are completely unchanged and, called directly against either file's raw text, *still* return `ID_DOCUMENT` -- confirmed, not assumed (`GDC_Alpurai_Shangla` still scores via `"passport"`, `schdule_of_charges_or_bilateral` still scores via `"expiry date"`). Nothing about the keyword table's underlying fragility was fixed. What changed is that the *splitter* now recognizes both documents as real checklist types before the vulnerable fallback path is ever reached, so the dormant fragility currently has zero real samples that can trigger it. This is a real, current closure of the collision as it actually manifests in this application, not a claim that the classifier mechanism itself was hardened -- if a future real document reaches the `OTHER_SUPPORTING_DOCUMENT` fallback while genuinely mentioning "passport" or "expiry date" in unrelated prose, the same collision would still fire. No fix was made for that theoretical case, consistent with this project's standing norm against fixing what no real sample currently exercises -- logged here, plainly, as the residual latent (not active) risk.
  **Blast radius confirmed precise, per the requested scoping**: `detect_document_type` is exclusively the generic-fallback classifier for documents outside the 8 checklist-routed types (`_CHECKLIST_TYPE_MAP`'s own module comment states this explicitly -- a checklist document's own field labels, e.g. an AMC's "IBAN", must never reach this table). No checklist-routed type can be affected by this collision or by today's finding; confirmed by reading the actual call site, not assumed from the function's docstring alone.
  **Verification**: no production code was touched (`git diff --stat` against `backend/app/` is empty for this change) -- there was nothing safe or necessary to fix, per the explicit instruction not to manufacture a fix for a collision that no longer exists on the current corpus. Added 2 real-cache-backed regression tests (`tests/preprocessing/test_document_splitter.py`, new `requires_real_cache` infrastructure mirroring `test_document_analysis_engine.py`'s existing pattern) asserting both files now classify to a real checklist type via `DocumentSplitter._classify_text` -- protecting the actual current fix mechanism (splitter routing), not a misleading direct assertion against the still-unchanged `detect_document_type` keyword table. Ruff/mypy stash-diffed against the pre-change baseline on the one file touched -- zero new findings (same 23 ruff findings, same mypy output, both only line-shifted). Full backend suite before: 906 passed / 1 failed (`test_health`, the known environmental artifact, from the `issue_date` fix immediately above). After: **908 passed / 1 failed** -- the +2 is exactly the 2 new regression tests added, `test_health` is the same pre-existing artifact, confirming zero regressions from a documentation-and-tests-only change.
  **Honest status**: the `ID_DOCUMENT` collision is real no longer, on the current corpus -- but it was resolved incidentally by unrelated splitter fixes landed earlier this session, not by any direct fix to the collision itself, and the classifier mechanism that caused it remains theoretically fragile and untouched. This is not the same as "fixed" in the sense every other fix tonight was fixed (a mechanism corrected and verified); it is "no longer reproducible on any real sample today," which is what was asked to be confirmed, stated precisely rather than either overclaiming a fix or leaving the stale "still open" framing standing.

- **2026-08-26: Master Rules §13's organization-name cross-document consistency check re-investigated -- still genuinely blocked, but the reason has changed since the original 2026-08-13 note, and is now precisely a normalization gap, not an extraction gap or a coverage gap.**
  **Went to the actual source spec, not the paraphrase.** `docs/Master_Rules_Combined.md` Section 13, "Organization Consistency" (not "§3" as an earlier CONTEXT.md line mislabeled it -- §3 is the Account Maintenance Certificate section, home to the *separate*, already-tracked Account Consistency sub-check): "The system shall compare organization names across all relevant documents to identify: Missing organization names / Different organization names / Inconsistent spelling or unexpected abbreviations / Incomplete names." Notably this literally asks to *flag* abbreviation inconsistency, not silently tolerate it -- worth knowing precisely, since it means the real spec is stricter on paper than "just normalize and compare" would satisfy; in practice, see below, treating every legitimate "TMA X" vs "Tehsil Municipal Administration X" pairing as a flag would make the check fire on nearly every real application, which is why any future version of this check should almost certainly resolve to WARNING/MANUAL REVIEW REQUIRED (§15's classification exists exactly for this), not a hard FAIL.
  **Extraction landscape mapped precisely, not assumed** -- confirmed by reading every extractor class, not grepping for the field name alone: `organization_name` genuinely identifies the applicant organization in `BilateralAgreementExtractor` (the real Party-B name), `AuthorityLetterExtractor` (the "on behalf of <organization>" clause), `FormalRequestLetterExtractor` (1 real sample only), and `OneLinkLetterExtractor` (fixed for truncation in `f0df45a` earlier tonight). `TripartiteAgreementExtractor.party_subbiller` (fixed for its wrong anchor in `073f19b` tonight) is the applicant-identifying field for that type -- its sibling fields `party_1link`/`party_kpitb` are explicitly *not* the applicant and must never be compared here. Two exclusions confirmed directly, not assumed from the task's own framing: `BusinessRequirementDocumentExtractor` has no `organization_name` field at all (only two presence detectors, per its own class docstring -- "neither field extracts a *value* in the usual sense"), and `AccountMaintenanceCertificateExtractor.account_holder` is a *bank account title*, a different concept already governed by the separate (also still-blocked) Account Consistency sub-check, not this one -- so AMC and BRD are correctly out of scope for this specific check, not an oversight.
  **Real multi-document coverage is actually decent -- 7 real application groups, not the near-zero coverage this type of gap sometimes turns out to have.** Enumerated every real cached source-file prefix with 2+ organization-identifying document types present (`Confidential Data/.ocr_cache/`): `GDA_Abbotabad`, `DG_Sports_KP_Onboarding_Documents`, `TMA_Khal_Dir_Lower`, `TMA_Lal_Dir_Upper`, `TMA_Lal_Qilla_Dir_Lower`, `TMA_Samarbagh_Dir_Lower`, `TMA_Thall_Hangu` -- each has Authority Letter plus One-Link Letter (and `TMA_Lal_Dir_Upper` additionally has Formal Request Letter). Notably `TRIPARTITE_AGREEMENT` and `BILATERAL_AGREEMENT` each have only 1 real sample on file, standalone, with no sibling document from the same real source -- so this check's coverage is real and decent for the Authority Letter/One-Link Letter/Formal Request Letter trio specifically, but genuinely zero for cross-validating `party_subbiller`/Bilateral's `organization_name` against anything.
  **The real blocker, confirmed directly against extracted values, not guessed at**: laid every group's real extracted organization value side by side. Every single one diverges in ways an exact-string (or even a simple case-folded) comparison would flag as "different," despite being the same real organization: `TMA_Khal_Dir_Lower` -- Authority Letter `"TMA Khall Dir Lower"` vs One-Link Letter `"TEHsIL MUNICIPAL ADMINISTRATION KHALL"` (abbreviation vs full expansion, the core pattern -- repeats in `TMA_Lal_Dir_Upper`, `TMA_Lal_Qilla_Dir_Lower`, `TMA_Samarbagh_Dir_Lower`, `TMA_Thall_Hangu`); `GDA_Abbotabad` -- `"Galiyat Development Authority, Abbottabad"` vs `"GALIYAT DEVELOPMENT AUTHORITY, ABBOTTABAD"` vs `"DEVELOPMENT AUTHORITY, ABBOTTABAD"` (a dropped word) vs `"GALIA DEVELOPMENT AUTHORITY, ABBOTTABAD"` (an OCR misread) across its own 3 One-Link Letter copies alone, before even comparing to the Authority Letter. Checked the normalization side precisely, not assumed: `organization_name`/`party_subbiller` are registered in **no** normalizer today (`grep` across `app/normalization/`, zero hits), so `NormalizerRegistry` falls them through to `GeneralTextNormalizer` -- "case is deliberately preserved," per its own docstring -- meaning even the *case* differences above would register as mismatches today, before getting anywhere near the harder abbreviation/OCR-noise problem. `TitleNormalizer` (case-fold only) would close the case gap but do nothing for `"TMA X"` vs `"Tehsil Municipal Administration X"` or a dropped/misread word. The one precedent for bridging exactly this kind of gap, `BankNameNormalizer`'s `BANK_ALIASES` table, has no organization-name equivalent yet -- and even a hand-built alias table wouldn't fix OCR noise (`"GALIA"` for `"GALIYAT"`, a dropped `"GALIYAT"` entirely, `"TEs"` for `"Tehsil"`), which would need fuzzy/token-overlap matching, a materially bigger and riskier piece of new infrastructure than an alias table alone.
  **Confirmed against the actual rule-engine mechanics, not just reasoned about in the abstract**: the existing cross-document rule base class (`_CrossDocumentRule`, `app/rule_engine/rules/cross_document_rules.py`) does exact-string comparison after normalization (`len(distinct) == 1`) and hard-`FAIL`s on any disagreement -- registering a naive `CrossOrganizationNameRule` on top of today's normalization would `FAIL` on every one of the 7 real groups checked, not a rare edge case. This is the same failure shape already on record for `CrossBranchCodeRule`/`CrossPeriodRule`, and exactly what the standing "don't add a rule before extraction exists for the field it targets" note was trying to prevent -- except the precise mechanism has moved: extraction is no longer the gap (it exists, and tonight's fixes specifically improved two of the five relevant fields), normalization is.
  **A separate, related, newly-surfaced finding -- flagged, not fixed, out of this task's scope**: 3 of the real Authority Letter values pulled during this check look like extraction artifacts rather than genuine organization names -- `DG_Sports` (`"Directorate"`, truncated), `TMA_Lal_Dir_Upper` (`"Tehsil Municipal Administration Dir please"`, a garbage trailing word), and `TMA_Lal_Qilla_Dir_Lower`/`TMA_Samarbagh_Dir_Lower` (`"OFFICE OF THE TEHSIL MUNICIPAL OFFICE(R)"`, traced to a letterhead-derived fallback firing because these two real samples use a labeled-block template `AuthorityLetterExtractor`'s own class docstring already documents as "not yet turned up in a real sample" -- it has now, this is real evidence that variant exists, but no organization_name in the class's primary prose pattern will ever match it). Not touched -- `organization_name`/`account_holder` fixes are explicitly out of scope for this task, and this is a different field/document type than the `073f19b`/`f0df45a` fixes already shipped tonight.
  **No code was changed.** Nothing was safe to build without either weakening the check into something that wouldn't satisfy the real spec's intent, or fabricating an untested normalization layer beyond this task's scope. Full backend suite: unchanged from the immediately prior commit's confirmed 908 passed / 1 failed (`test_health`, the known environmental artifact) -- re-run to confirm, not assumed, since no production code was touched (`git diff --stat` against `backend/app/` empty).
  **Honest status**: still genuinely blocked, but the blocker is now named precisely -- **normalization infrastructure, not extraction completeness and not insufficient real coverage.** Extraction exists and, for 2 of the 5 relevant fields, was specifically improved earlier tonight. Real multi-document coverage is decent (7 groups) for 3 of the 5 fields, though genuinely absent for Tripartite/Bilateral cross-validation specifically. What's missing is a way to compare `"TMA Khall Dir Lower"` and `"TEHsIL MUNICIPAL ADMINISTRATION KHALL"` as the same real organization without also silently accepting a genuinely different one -- an alias table alone (the `BankNameNormalizer` precedent) would handle the abbreviation half of that but not the OCR-noise half, and building either piece requires the same real-sample-first discipline as every extraction fix tonight, not a rule shipped ahead of it.

- **2026-08-26: 2 of 3 `AuthorityLetterExtractor.organization_name` bugs surfaced by the org-name cross-document investigation above (`413deb7`) fixed and verified; the 3rd (TMA Lal Dir Upper) confirmed as a genuine OCR-corruption limitation, not an extraction bug, and left honestly unfixed.**
  **Case 1 -- DG Sports (`"Directorate"`, FIXED).** Pulled the real text directly rather than assuming truncation: the letter's own "on behalf of" clause genuinely, completely reads "...on the behalf of Directorate." (period right after) -- the old pattern wasn't cutting off mid-capture, it was faithfully capturing an already-complete but generic self-reference, the exact same phenomenon as the already-solved "this Authority"/"the Authority" cases (`f0df45a`-era fixes), just without a leading demonstrative. Added bare `"directorate"` to `_GENERIC_ORG_REFERENCE`'s alternation on that basis -- a genuine extension of the existing mechanism, not a new one. The existing letterhead fallback would have picked the WRONG line, though: this letterhead's first substantive line is an unrelated slogan ("Sports are essential for the development of a happy, healthy & vigorous society"), not the org name. Fixed by giving the fallback an optional anchor word (the real word the generic reference used instead of the name -- "directorate" here) to search for across letterhead lines, rather than blindly trusting positional order; landed correctly on "DIRECTORATE GENERAL OF SPORTS". Deliberately did not also append the following letterhead line ("KHYBER PAKHTUNKHWA") -- no well-grounded, multi-sample-confirmed continuation signal existed for this specific ending, unlike case 3 below, and "DIRECTORATE GENERAL OF SPORTS" is already a non-garbage, specific, complete-on-its-own-letterhead-line value (the same standard the already-accepted GDA Abbotabad fix was held to).
  **Case 3 -- TMA Lal Qilla Dir Lower and TMA Samarbagh Dir Lower (`"OFFICE OF THE TEHSIL MUNICIPAL OFFICE(R)"`, FIXED, real bug not a template-variant dead end).** Read the extractor's fallback logic precisely before touching it: both real samples DO correctly reach the existing "the\nAuthority" backward-reference fallback (not a new gap in the primary pattern) -- the bug is that the fallback's letterhead then picks a job-title heading line instead of the real organization name one or two lines below it. Confirmed this is a job title, not a legitimately-different real org name, two ways: (a) both departments' own real `ONE_LINK_LETTER` samples independently state the full real name ("TEHSIL MUNICIPAL ADMINISTRATION, LAL QILA" / "...SAMARBAGH"), and (b) the exact "OFFICE OF THE TEHSIL MUNICIPAL OFFICE(R)" shape, followed by "TEHSIL MUNICIPAL ADMINISTRATION \<place\>" on the next line(s), is structurally consistent across every real TMA sample checked, not just these two (TMA Khal Dir Lower's and TMA Thall Hangu's own letterheads and TMA Khal's account-detail block all independently confirm the same "...ADMINISTRATION \<place\>" shape) -- a real, multi-sample-grounded signal, not a guess from one file. Fixed with two additions: a new `_OFFICE_TITLE_HEADER` skip pattern (scoped precisely to a line that both starts with "OFFICE OF THE" AND itself ends in "OFFICE"/"OFFICER" -- confirmed this does NOT also skip the Zoo sample's legitimately-org-naming "OFFICE OF THE CONSERVATOR WILDLIFE, PESHAWAR ZOO" line, which ends in "ZOO" not "OFFICE"/"OFFICER"), and a continuation rule joining the next letterhead line whenever the candidate ends in "Administration" (the same real, multi-sample-confirmed structural signal). Verified this doesn't touch GDA Abbotabad's already-correct single-line fallback (`"Galiyat Development Authority, Abbottabad"` never ends in "Administration").
  **Case 2 -- TMA Lal Dir Upper (`"Tehsil Municipal Administration Dir please"`, confirmed NOT a regex bug, left unfixed.** This is the answer to the "is it a real fixable bug or a genuine template-variant limitation" question, but for a third, different reason than either: it's neither a boundary bug nor an unhandled template variant -- the pattern faithfully captures this real sample's entire real (OCR'd) sentence up to its period, and that sentence itself, as OCR'd, genuinely reads "...on behalf of Tehsil Municipal Administration Dir please." The letterhead ("TEHSIL MUNICIPAL ADMINISTRATION LAL DIR UPPER") and the letter's own signature block ("TEHSIL-MUCNICIPAL ADMINISTRATION\nDIR UPPER") both independently confirm the real suffix is "DIR UPPER", so "please" is a genuine OCR misread of "Upper" (or the whole tail), not a drafting error and not something the extraction pattern got wrong. This doesn't fit the `_GENERIC_ORG_REFERENCE`/letterhead-fallback mechanism at all -- that mechanism exists for "the sentence uses a placeholder instead of the real name," and this sentence's words genuinely *are* (mostly) the real name, just corrupted at one word by OCR. No safe, generalizable pattern exists to detect and strip a single corrupted trailing word without overfitting to this one real artifact (how would a pattern know "please" is wrong and "Upper" is right, without hardcoding this exact substitution). Left as a named, honest OCR-quality limitation, matching this session's AMC `issue_date` precedent for the same class of problem (GDA Abbotabad copy1's vertically-split digits).
  **Also confirmed, as instructed**: ran the fix against every one of the 9 real cached Authority Letter samples, not just the 4 named ones -- zero regressions on the 5 already-correct samples (Zoo, GDA Abbotabad, TMA Khal Dir Lower, both TMA Thall Hangu copies including the honest-empty master-checklist page), and no other field (`focal_person_name`/`focal_person_designation`/`account_number`/`account_holder`) changed on any file -- this fix touches only `organization_name`.
  **Fixture fallout, found and fixed the same way as tonight's other pattern changes**: two hand-written fixtures (`test_document_analysis_engine.py` and `test_document_analysis_api.py`, both apparently modeled on the real DG Sports sentence) asserted the old, now-known-incomplete `"Directorate"` value with no letterhead at all above the title -- once `"directorate"` became a recognized generic reference, both broke with `KeyError` (empty header -> no fallback candidate -> field correctly omitted). Fixed by adding a one-line letterhead to each fixture (matching the real DG Sports shape) and updating both assertions -- not by weakening the fix, consistent with every other fixture correction tonight (party_subbiller, AMC issue_date).
  **Tests**: updated the existing `test_real_authority_letter_tma_samarbagh` real-sample test to the corrected value; added 3 new real-sample tests -- `test_real_authority_letter_tma_lal_qilla` (confirms the case-3 fix generalizes to a second independent department, not tuned to one file), `test_real_authority_letter_dg_sports_bare_directorate_reference` (case 1, including the anchor-word letterhead search), and `test_real_authority_letter_tma_lal_dir_upper_ocr_corrupted_tail_is_honest_miss` (locks in case 2's left-alone value with the full real evidence in its docstring, so a future session doesn't mistake the unchanged value for an unnoticed bug).
  Ruff: stash-diffed across all 3 files touched -- one new finding, a `PLR2004` magic-number lint suggestion (`len(words) == 2` in the new `_anchor_word_for` helper); consistent with dozens of pre-existing, unaddressed `PLR2004`/`RUF012` instances already throughout this exact file (same precedent as tonight's `DTZ007` call on the AMC fix), not treated as a real problem. Mypy: identical, confirmed via a sorted diff. Full backend suite before: 908 passed / 1 failed (`test_health`). After: [pending -- see below].
  **Honest status**: 2 of the 3 cases (DG Sports, and both TMA Lal Qilla/Samarbagh under case 3) are fixed and verified against real text, with zero regressions across all 9 real Authority Letter samples. Case 3 is a real, generalizable bug -- confirmed by a second independent real sample beyond the one that surfaced it, not a template-variant dead end (the class docstring's old "not yet turned up in a real sample" caveat about a labeled-block variant was a separate, different concern; this was a positional-fallback bug within the already-handled prose template, not a new template shape). The 3rd case (TMA Lal Dir Upper) is genuinely unfixable at the regex level -- an OCR-corruption problem, named precisely and left honest rather than forced. **Does this change the still-blocked org-name cross-document consistency picture?** No, confirmed rather than assumed: the blocker there was never "how many samples currently extract correctly" -- it was "no normalization exists to bridge legitimate abbreviation/full-form variance," and that's untouched by tonight's fix. If anything, this fix mechanically *widens* the real-world surface-form variance the normalization gap would need to cover (one more now-correct value, `"DIRECTORATE GENERAL OF SPORTS"`, that still won't string-match its sibling One-Link Letter's `"Directorate General of Sports Khyber Pakhtunkliwa"` without exactly the same normalization work already identified as missing) -- not a reason to revisit that conclusion, a confirmation of it.


- **2026-08-26: Six department-requested features relayed verbally/via a hand-photographed note (2026-08-25) captured as backlog -- not implemented, and the note's wording is treated as an approximate paraphrase, not a settled spec.** 
  - **1. "Special approval" despite missing required items.** Read `app/human_verification/validators.py::_validate_approve`: today an `APPROVE` decision hard-requires every one of the fixed `CHECKLIST_ITEMS` to be checked (`ChecklistIncomplete` raised otherwise). No override path exists. Needs the department to confirm if this means a relaxation of `APPROVE` or if it's identical to the "conditional approval" ask (Item 4).
  - **2. Per-deficiency remediation detail in the validation report.** Read `app/reports/constants.py::RECOMMENDATION_TEMPLATES`. Half the templates already use a `{details}` slot (e.g., naming the missing document type), but others are purely generic (e.g., `"Review failed IBAN consistency across documents."` without naming the documents or values). This is a real, actionable scope: wire existing `validation_results`/`extracted_fields` data into the remaining generic templates.
  - **3. Editable "AI-generated commentary."** Grepped the whole `app/reports` module: nothing LLM/generative exists. Every recommendation is a deterministic template. This needs clarification on whether they are requesting a new unbuilt generative feature, or incorrectly referring to the deterministic text.
  - **5. Editing rights "within the analysis of report document."** Resolved to a specific finding: Editing *extracted field values* is already fully built (`CorrectionItem` workflow in `human_verification`). Editing the *rendered HTML report narrative itself* does not exist. The former is most likely the intent, but requires a one-line confirmation before closing as "already done."

- **2026-08-26: "Originality of AMC" check (Item 6) -- mechanism genuinely unknown, cannot be scoped further without department input.** The current manual checklist asks for "Bank Maintenance Certificate originality confirmed", but automating this could mean visual forgery detection (needs a reference corpus), external bank API cross-referencing (needs integration), or duplicate-submission detection (buildable from existing data). Left open pending product definition.

- **2026-08-26: "Conditional approval" decision option (Item 4) -- scoped against the real current decision-state code, not implemented.** `ReviewDecision` is a native Postgres ENUM (`APPROVE`, `CORRECT`, `REJECT`). Adding a conditional state requires: an Alembic migration (`ALTER TYPE`), a new `DECISION_TO_STATUS` mapping, backend validation rules (does it require a specific condition note? does it require the full checklist?), UI updates to the hardcoded 3-option selector in `ReviewDecision.jsx`, and verifying downstream status dependencies (dashboards/reports). Actionable engineering work, but blocked on product rules for what a "condition" actually carries.

- **2026-08-26: Multi-application batch queue scoped against real code and real capacity data -- not implemented.** (Note: Distinct from the existing single-application `app/bulk_queue`.)
  - **Reusable logic**: `PipelineRunnerService.run()` already successfully auto-chains a single PDF through split -> OCR -> extract -> validate. The per-PDF pipeline logic does not need to be reinvented.
  - **Missing logic**: A batch-submission entry point (e.g., accepting 10-20 PDFs, creating N independent applications, and auto-enqueueing each) needs to be built as an orchestration layer over the existing flow.
  - **Real Capacity Baseline**: 
    - Storage: 5 processed applications take ~50 MB (~10 MB avg). 20 applications = ~200 MB. Trivial.
    - Database: 5 applications use ~20 MB (41 documents, 46 queue_jobs, 34 ocr_results, 75 extracted_fields, 846 validation_results). 20 applications = ~80 MB. Trivial.
    - Throughput (The bottleneck): Average `DOCUMENT_OCR` job takes **104.6 seconds** (n=39). At ~8.2 documents/app, that is **14-15 minutes of OCR per application**. With `bulk_queue_workers=1` (deliberate PaddleOCR stability limit), 20 applications will take **~4.5 to 5 hours** of serial processing.
  - **Open product questions**: Should the 20 apps process serially (5 hours) or is concurrency worth the PaddleOCR crash risk? How should mid-batch failures (e.g., PDF #7) be surfaced? How does a reviewer handle 20 reports dripping in over 5 hours?

## What's actually left to build

1. **Extraction support for the rule-engine gaps above**, if those checks are wanted — this is the real remaining scope, not UI work.
2. ~~Field-level review widget for the `validation` module~~ — **resolved 2026-08-19: this was stale, and directly contradicted the finding above (line 133: the `validation` module "is vestigial, not awaiting integration," reconciling it with `human_verification` is "a product decision... not a code fix — do not wire this module up reactively").** The old wording here treated wiring `POST /validation/fields/{id}/verify|correct` into a UI as legitimate open work, which is exactly what line 133 says not to do reactively. Investigated before building anything: the equivalent feature already exists, complete and live, on the module actually used in production — `human_verification`'s `ReviewFields.jsx`/`HumanReviewPage.jsx` already lets a reviewer correct any extracted field inline (value + optional reason), submitted as part of the final APPROVE/CORRECT/REJECT decision. The real gap was test coverage, not a widget: that component tree had zero frontend tests before tonight. Closed — 17 new tests added (`ReviewFields.test.jsx`, `HumanReviewPage.test.jsx`; commit `dedb606`), full frontend suite 31 passed/0 failed. Nothing remains open here. Do not resurrect this as `validation`-module work without first revisiting the product decision at line 133 — that decision is still unmade, this entry only fixes the contradiction, it doesn't answer the underlying question.
3. Whatever the team prioritizes next — the backend and the frontend are both stable and tested; there's no "broken foundation" blocking further work anymore.

---

*Update this file at the end of any session that changes what's true about the project — a stale context.md is worse than none, since the next session (any tool) will trust it.*
